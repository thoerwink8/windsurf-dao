# 破点清单（对抗审开审前照表列全，不靠现场想）

**来源**：2026-08-13 夜 #340 四轮对抗审实打——四轮里每一轮红的都是上一轮没想到的破点形态，
说明「开审时现场想」结构性漏项；本表是那四轮实打踩到的形态汇总，**不是穷举**。

**这份表会不会过期**：会。新一轮对抗审踩到本表五类之外的破点，是**补一行进本表**的信号，
不是重新起一张新清单；表内条目的命中率归各审官自己交付里报，本文件不自带复发计数
（条款元数据链已随 issue #324 B 批整体退役，见 `CLAUDE.md`「常用命令」节）——
表老不老，靠对抗审官交付时的实测反馈判断，不是靠这份文件自己统计。

| 类别 | 破点形态 |
|---|---|
| 路径 / 边界比较 | `..` 穿越、UNC 路径、`\\?\` 前缀、8.3 短路径、大小写、尾分隔符、尾点兄弟、同名前缀兄弟、相对路径 |
| 文本扫描与正则 | 三反引号围栏、跨行配对、落单定界符、全角标点、行首尾、代码块内示例、CRLF |
| 源码解析器 | 注释抢占、字符串抢占、嵌套括号、多同类结构、CRLF/BOM |
| 链接与文件系统 | 悬空链、错名 target、中间链、目录冒充文件 |
| 集合与投票判定 | 外来样本污染、平票、零样本、单元素 |

[自定@0813]
