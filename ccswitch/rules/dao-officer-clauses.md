# 官侧派单条款库 · 通用半边（帅侧 `dao-dispatch.md` 的对位档）

> **必经动作**：**你被派来干一件活、开工之前 = Read 本文件的「通用节」+ 你所属官种那一节**。
> 触发时刻是派单令的第一行（各项目的派单模板把这句话固定写在 prompt 开头，读到即生效，
> 不需要判断该不该读）。有冲突以盘上文件为准，不以派单令里的转述为准。

> **与 `dao-dispatch.md` 的分工**：那份是**帅侧**（「我正要派一个官」那一刻读），
> 本份是**官侧**（「我正被派来干活」那一刻读）。两份平级，互不包含。

> **项目侧还有一份**：各项目的 `docs/rules/dispatch-clauses.md` 装**项目特有**条款
> （那个仓的验证入口、账本文件、进程名、专有脚本、专有标签）。派单令首行应让官**两份都读**——
> 本份给与技术栈无关的判据，项目那份回答「在这个仓里具体跑哪个命令」。

> **本文件的来历（2026-08-02）**：这些条款此前只住在 `mousse-cli/docs/rules/dispatch-clauses.md`，
> 于是 ①第二个项目拿不到，只能从零重新发明；②dao 自己的 hook 与测试反过来把那个项目文件
> 当唯一语料与降级路径 —— **全局层被一个项目文件治理**。本次把与技术栈无关的那一半搬回 dao。
> **条款正文逐行原样搬运、元字段一字未改**，逐条归属判定与清单见引入本文件的那个 PR。

> **元字段（行尾那三格 `n` / `@` / `触发:`，以及独立的 `[基线:]` / `[自定@]` / `[#slug]`）**：
> **分类判据的真相源是 `ccswitch/rules/dao-legislation.md` 的 `## 📌 条款元字段` 节**——
> 八种 `触发:` 取值的定义、机检档与槽位档之分、填 `模板首行` 的四条判据、这套分类的四个已知
> 弱点，全在那里（2026-08-02 随本文件一同搬进 dao；此前那份词汇表只住在项目侧，于是第二个
> 项目拿不到它、只能从零重新发明同一套词汇——**那正是本文件立项要治的病，词汇表不搬就等于
> 在新档上原样复现一遍**）。**格式与闸的实现**（哪些行算条款、哪几种命中判红）见
> `ccswitch/scripts/check-clauses-structure.ps1` 与 `ccswitch/lib/clause-parser.mjs`——
> 那两份是**实现**的真相源，不是分类判据的。
> ⚠️ **部分条款的 `触发:` 取值指向某一个项目的机检道**（`verify-all/*`、`IsGate槽位`
> 之类）—— 那是它入库当时的实况。**换个项目读到这些值时按「无」读**：那个仓没有那道闸，
> 就没有那个触发点。刻意不改字面 —— 改了出处就断了，而「这条在我这个仓里到底有没有闸」
> 本来就该由读者核一遍，不该由一个字面值代答。

> 🔴 **跨仓读法（同一个理由的另一半，2026-08-02 补）**：条款正文里的**「本仓」「主仓」「全库」
> 指的是这条条款原出处的那个仓，不是你此刻所在的仓**（条款正文区实测 **17 处 / 12 行**——
> 2026-08-02 实算，判据是正文里「本仓 / 主仓 / 全库」的出现次数；**这个数随条款增删而变，
> 别当常量读**。逐处改写会把可复核的实证退化成无从核实的泛称，故不改）。同理，正文里写死的脚本名 / 账本文件 / 进程名 / 标签名
> （`verify-all.ps1`、`PROGRESS.md`、`qa-assets` 之类）也是出处仓的实况，**你这个仓可能根本没有
> 那个东西**——已知至少一处指向的脚本在 windsurf-dao 里并不存在。⇒ 读到这类指代词时，
> 先问「它说的是哪个仓」，再问「我这个仓里对应的是什么、有没有」。

> **行内 slug `[#<域>-<短名>]` 与台账**：本文件每条条款行尾带一个 slug，它是条款与
> `ccswitch/clause-ledger.json`（台账字段的真相源）的关联键。**域取 `官` + 官种一字**：
> `官通`（通用节）· `官复`（复审官）· `官实`（实现官）· `官抗`（对抗验证官）· `官侦`（侦察官）·
> `官食`（dogfood 官）。这么取是为了让 slug 单看就说得出「这是哪一节的哪一条」，与 dao.md 侧
> 既有的 `守-` / `法-` / `Shell-` / `归-` / `帅-`（按**所在档 / 节**取域）同一路数。
> 改条款正文改本文件，改台账数字改那份 JSON；两边都改完跑一次
> `node ccswitch/scripts/gen-clause-index.mjs`，对账不绿就是有一边没跟上。

> **本批刻意没搬的四类，照直写**（别把本文件读成「官侧条款的全集」）：
> ① **项目特有条款**（验证唯一入口 / 版本号四处 / LOC 红线 / 进程名 / 专有脚本 / 专有标签）
> 留在各项目侧；
> ② **正文已在 dao 别处的**不重复收 —— `$LASTEXITCODE` 判成败见 `ccswitch/rules/dao-powershell.md`
> 第一条（**判据等价、措辞不同**；原写「逐字覆盖」是笃定措辞且不实——那一版少了
> `$proc.ExitCode` 这一个 token，2026-08-02 已补进那一条。且那份档在 Read 任何 `.ps1` 时
> 由宿主自动送达，投递比本文件强），
> 截图落盘与「改配置认清源与投影」见 `ccswitch/dao.md` Shell 独有项，
> 「人读的载体用 STAR + 说人话 + 折叠证据」见 `ccswitch/dao.md` 知识归位节。
> ⚠️ **「禁 `2>&1`」与「编码铁律」曾被判为同类重复，复核后改判、照常收进本文件**：
> dao-powershell.md 那两句是**更弱的一版**（只点名「管道」形态、无 `NativeCommandError` 机制
> 与安全替代），而这两条正文记的正是「写侧全做对、毁在读侧」的两起事故 —— **把富的一版换成
> 弱的一版，名义上是去重、实际是降级**。dao-powershell.md 同批订正了射程并指回本文件；
> ③ **观察区**（判断类候选，复发即升格）本批未搬，仍在项目侧；
> ④ **已补（2026-08-02 合并态）**：本文件此前不在 `ccswitch/clause-index.json` 的源清单里，
> 于是 `dao-subagent-clauses.js` 那条自动注入通道渲染不出本文件的条款。现已登记进
> `ccswitch/lib/clause-parser.mjs` 的 `defaultSources()`（`all-top-level` + `dispatch-sections`），
> 索引里六个官种因此都有条款、台账也把它纳入了扫描面。**照直写它还没证到的那一半**：
> 「注册之后那个 hook 在真实 session 里真的响了」**至今无人有资格宣称**——本仓的接线注册是
> 用户动作（硬闸 G2），登记源清单只解决了「渲染得出东西吗」，解决不了「它有没有被调用」。

---

## 通用节（任意官种都应通读）

- 工具约束：搜索一律内置 Grep/Glob，读文件一律用 Read，禁止用 Bash/PowerShell 跑 grep/tail/cat/Select-String（含大小写变体）搜索或一次性读大文件。 [n=? @07-24 触发:权限deny] [#官通-工具约束]
- subagent 身份铁律：你是 subagent，回答对象是指挥官不是用户——禁止调用 AskUserQuestion，直接返回结果文本。 [n=? @07-24 触发:模板首行] [#官通-身份铁律]
- 后台长命令主动轮询到终态才准停手：发起后台/异步验证（build、dev server、外部资源等待）后必须亲手 Read 其日志直至出结果；挂 Monitor/装了任何 watcher、poller、monitor 都不算"已主动轮询"——armed 之后停手仍是停摆（出处：批16 停摆型"等自后台"第八例新变体，2026-07-24 09:1x）。 [n=10 @07-24 触发:无] [仅判据·无触发] [#官通-主动轮询]
- 等待类验证超时即降级交付：build/dev server/外部资源等就绪类等待超 15 分钟仍未就绪 → 放弃该验证手段，欠账显式化写进交付说明，直接带着已完成部分交付，不无限期卡等（完美验证卡死不如降级交付）。 [n=? @07-24 触发:无] [仅判据·无触发] [#官通-超时降级]
- 禁 `2>&1` 捕获 native 命令输出：会被 PowerShell 包成 NativeCommandError，在 `$ErrorActionPreference='Stop'` 下把正常 stderr 进度行（如 cargo 的 Locking/Updating 提示）误判成终止性错误中断脚本；需要捕获输出时用 `Start-Process -RedirectStandardOutput/-RedirectStandardError` 到真实文件（参照 `scripts/verify-all.ps1`/`scripts/bump-version.ps1` 手法）。 [n=? @07-24 触发:无] [仅判据·无触发] [#官通-禁重定向捕获]
- 实现官止步 `gh pr create`：不 `gh pr merge`、不删分支——终审与合并归派单方（帅）独立复验后执行，「不 merge」条款优先于项目 CLAUDE.md 里"实现官全流程自开自合"的默认描述。 [n=1 @07-24 触发:PR流程] [#官通-止步PR创建]
- 编码铁律：改动含中文注释的文件（尤其 Cargo.toml/PowerShell 脚本）禁 `Get-Content | -replace | Set-Content` 管道——PS 5.1 对无 BOM 文件按系统 ANSI 代码页解码会把中文写成乱码；用 `[IO.File]::ReadAllText/WriteAllText` + 显式 `[System.Text.Encoding]::UTF8` 读、`New-Object System.Text.UTF8Encoding($false)` 写（数据文件无 BOM，脚本文件本体依旧需要 BOM 才能被 PS 5.1 正确解析中文——两者编码目标不同，勿混淆）。**2026-07-27 补：真凶是 `Get-Content` 本身，不是那条管道**——本条原文只点名「`Get-Content | -replace | Set-Content` 管道」，于是两起事故都从它的字面射程外溜了过去：#117（07-20）是 `Get-Content -Raw` + `gh pr create --body $body`（无管道、无 `Set-Content`）；#269（07-27）是 `Get-Content -Raw` + `-replace` + `[IO.File]::WriteAllText(..., UTF8Encoding($false))`——**写侧完全照本条做对了，读侧照旧**。判据改述为：**`Get-Content` 的任何形态（含 `-Raw`、含只读不写）读无 BOM 文件时内容已经毁了**，写侧再规范也救不回来。实测坐实（本机 `[System.Text.Encoding]::Default.CodePage = 936`，这是 `Get-Content` 无 `-Encoding` 时用的那个）：`## 这是什么` 经 `Get-Content -Raw` 得 `## 杩欐槸浠€涔?`，与 #269 线上 body 首行逐字相同；同一文件用 `[IO.File]::ReadAllText($p,[Text.Encoding]::UTF8)` 读出正确中文。**顺带一个静默失败，比乱码本身更阴**：字符串已成乱码后，后续 `-replace '<含中文的模式>'` 一律不命中且不报错、`$LASTEXITCODE` 照样 0——#269 的 `📸 真机验证：见评论 <PLACEHOLDER>` 因此把 `<PLACEHOLDER>` 原样留在了线上 body 里，四个截图链接从未注入（截图本身已在 `qa-assets` 且评论里有导读，缺的只是 body 那一行）。**另一条因果值得记**：#117 的官先试了 `--body "$(cat <<'EOF' …)"`（PowerShell 里的 Bash heredoc，被 dao.md 明令禁止、实际也 ParserError），退回来才用了 `Get-Content` ⇒ **只禁一个形态而不给出安全替代，会把人推进另一个坑**；安全替代见下一条。 [n=3 @07-24 触发:无] [仅判据·无触发] [基线:全库 260 个 PR body 实扫，中招 2 例（#117、#269）＝0.77%；#175「verify-all GBK 乱码双修」是同族第三例但载体不同（进程输出重定向而非 `Get-Content`），故不计入 n] [#官通-编码铁律]
- **PR body / comment 正文永不经过 PowerShell 字符串，一律走 `--body-file`，发出后回读核一次**（2026-07-27 立，补的是上一条缺的那半——上一条只有判据、没有任何载体，两起事故都在它眼前发生）。三步：**①写**——正文落文件（`Write` 工具，或 `[IO.File]::WriteAllText($p, $s, (New-Object System.Text.UTF8Encoding($false)))`），然后 `gh pr create/edit/comment --body-file <文件>`，让 **gh 自己读字节**；gh 是 Go，直读文件、不过 ANSI 解码，#269 的 `create` 步走的正是这条路、**当时 body 是好的**，是随后那次 `gh pr edit` 的读侧把它毁的。**②改已发出的 body**——改那个文件再重发 `--body-file`，或用 `[IO.File]::ReadAllText($p,[Text.Encoding]::UTF8)` 读进来改；**禁 `Get-Content`（任何形态）、禁 `--body $var`**。**③回读**——发出后用 **Bash 工具**跑 `gh pr view <n> --json body -q .body > _tmp/pr-<n>-readback.md`（Bash 重定向是字节直落），再用 **Grep 工具**扫 `[锛銆馃鈥]`（这四个字分别是 `：` / `。`、`、` / emoji 前导字节 / `—` 被 CP936 误读的产物，正常中文里几乎不出现），**零命中才算发完**，有命中就是乱码、立刻重发，交付里贴那一行结果。**第二个实证是本条作者自己踩的，这个出处比外部实证硬**（2026-07-27 同一批）：修 #269 的 body 时用 `node -e "…"` 经 **Bash 双引号**传正文，里面写着的 `` `gh pr edit` `` 被 shell 当成**命令替换执行掉**——`gh` 空跑（无害），而那两个词**从写出的文件里消失了**，`node` 退出码 0、文件长度看着正常，是逐行 Read 才发现的。⇒ **正文过任何 shell 字符串都是同族**（PS 的 `Get-Content`/CP936、Bash 的反引号/`$(…)`，机制不同、后果同一类：**正文被静默改写而退出码干净**）。**本条第①步「用 `Write` 工具写正文」因此不是形式主义**——它的价值就是绕开所有 shell 的引号语义，而这个价值是由它的作者违反它一次才被验到的。**#117 的账 = issue #280**（body 仍乱码、CP936 有损不可反解、其会话不在本机可查范围；成因、与 #269 的差别、两个待定项都写在那张 issue 里，本条不重述）。**射程与弱处照直写**：㈠这是**槽位档不是机检档**，没有任何程序在核你回读了没有。**2026-07-29 挂账已还一半**：`scripts/check-pr-body-mojibake.ps1`（观察线，挂 `verify-all`）现在每次打印「`_tmp/` 顶层的正文/回读记录扫了几个、有没有乱码签名字或未替换占位符」，判据库在 `scripts/lib/pr-body-scan.ps1`。**照直说它还不了的那一半**：它核不到「你走没走 `--body-file`」，也核不到「你回读了没有」——只核得到「回读记录在不在扫描面里」，没有记录时它分不开「本批没发 PR」与「发了没回读」（那一行报的是**零样本**，不是通过）。故本条 `触发:` 仍是 `PR流程` 而非 `verify-all/*`：改成后者会宣称一个它并不具备的覆盖面。**同批还捞出一件事，值得记**：那个检测器的首版在 47 条合成断言下全绿，拿主仓 `_tmp/` 的 16 份真实正文一跑**一份都没扫成**（正文里的空行触发 PowerShell 集合参数的逐元素非空校验，绑定失败被 `Continue` 吞掉），而它照报「均零命中」、退出码 0 ——**要防的病在防它的那个东西自己身上原样重演了一遍**，靠真实语料才现形；㈡零命中只证明「没有这四个签名字」，不证明「编码一定对」，反向也有假阳性（#175 的 body 是好的，只因**引用了**一段乱码样例而命中一次 ⇒ 判据是「零命中，或命中处是你有意引用的样例」）；㈢**PowerShell 侧 `gh … > file` 本机实测是安全的**（`[Console]::OutputEncoding` = 65001，与 `Encoding::Default` 的 936 是两个互相独立的设置，别混为一谈），但那是**机器级设置**、且 PS 5.1 的 `>` 会加 BOM，故第③步指定用 Bash 工具而非 PS 重定向。**第 4 例（2026-07-29，帅自己踩，且它把本条的性质看清楚了）**：帅代走查官回写 issue 评论时用 `gh issue comment --body "$(cat <<EOF …)"` —— **不带引号的 heredoc**，于是正文里的 `` `literal` `` 被 bash 当命令执行、替换为空，发出去变成「未确证：**档**」。同一批的另两条评论因为**碰巧转义了反引号**而完好，所以这不是「批量失败」而是「**逐条抽奖**」。三点值得记：㈠**我事前读过本条**（它就在我改的同一个文件里，且我当天已经引用过它两次），仍然违反了——**这不是射程问题，是「知道」与「做到」之间的那道缝**；㈡`literal: command not found` 只出现在 stderr 一行里，**而评论那一刻已经发出去了**，是事后回读才发现的 ⇒ **本条第③步「发出后回读」是唯一抓住它的东西，而它是槽位档、没有任何程序在核**；㈢我在订正评论的第一版里写了「条款只点名 PowerShell，射程缺口，随后补」——**那句话本身是错的**，本条正文明写「正文过任何 shell 字符串都是同族」并列了 Bash 反引号。**把「我违反了条款」误判成「条款不够」是更危险的一种错**：前者的处方是加载体，后者的处方是改条文，而改条文对一条已经写对了的条款毫无用处。⇒ 那笔挂账（做成 `check-*.ps1` 观察线）**2026-07-29 已落地**，形态与它还不了的那一半写在上面 ㈠。**落地之后本条的性质没有变**：第 4 例那种「知道且当天引用过两次，仍然违反」，新检查一样拦不住——它扫的是**已经落到磁盘上的正文**，而那一次的正文是被 bash 在发出的**同一瞬间**吃掉的，磁盘上从来没有过一份原文。**能抓住它的仍然只有第③步回读，而回读仍然是槽位档。** 别把「有了一道检查」读成「这条条款现在有人管了」。 [n=4 @07-27 触发:PR流程] [基线:同上条 2/260（#117 / #269）+ 本条作者自己一次（shell 命令替换吃掉正文）+ 帅 2026-07-29 一次（bash 不带引号 heredoc 吃掉反引号内容），共 4 例；本条要求的回读步骤在前 2 例中执行 0 次、第 4 例中**事后**执行并抓到——都是「`gh` exit 0 就算发完」] [自定@07-27] [#官通-body-file回读]
- 派单令勿内嵌危险命令字面例：安全规则/拦截清单类任务的派单 prompt 一律引用 spec 文档章节而非把 `rm -rf` 类字面串写进 prompt——auto 分类器会把危险字面派单令当危险指令拦下（2026-07-25 批6 实证，改引用法二发即过）。 [n=1 @07-24 触发:分类器拦截] [#官通-勿嵌危险字面]
- **`gh pr create` 撞 5xx 的降级路径**：重试 3 次仍失败即止——分支照常推送（SSH），PR body 存 worktree `_tmp/pr-body-<slug>.md`，交付注明「PR 待创建」由指挥官逢心跳重试，不无限重试也不因此扣住已完成工作（2026-07-25 批11 实战，repo 侧故障 ~47 分钟，降级路径零损）。 [n=1 @07-24 触发:PR流程] [#官通-PR创建5xx]
- **合并前先拆 worktree，否则 `--delete-branch` 会半途而废且不报错**：`gh pr merge --delete-branch` 删本地分支时若该分支正被 worktree 占用，git 报 `cannot delete branch ... used by worktree`，**gh 就此中止、远端分支不删**——而 PR 已经合并成功，命令整体看起来"只是有个 warning"。于是远端悄悄攒下已合并分支，只能靠 dao-verify 的孤儿分支扫描回溯式捞。正确顺序：**核 worktree 干净 → `git worktree remove` → `gh pr merge --merge --delete-branch` → `git fetch --prune`**；若已踩，补 `git push origin --delete <branch>`。 [n=1 @07-27 触发:PR流程] [基线:本窗同一动作两次实测——#253 未拆先合，远端残留 1 分支需手工补删；#254 先拆后合，本地/远端/worktree 三处一次清干净] [#官通-先拆worktree]
- **禁笃定措辞（含派单方自己写的契约文档）**：近似手段（正则/字符类/启发式）必须标注为近似且写明两向都可反例，禁「已补全/全覆盖/无遗漏/零行为改动」类把近似说成判定的措辞——本条对帅同样生效，帅自己写的契约文档同样要自查（2026-07-26 通宵窗实证：契约里帅自己写的一句笃定表述被验证官 4 组 mutation 证伪）。（**2026-07-27 一度被挂成 `完成流水线③`（未尽处段），同日由对抗验证官判为挂错、已退回 `无`**。判错的地方：那个槽位强制的是**未尽处段存在**，而本条要的是**另一件事**——dao.md 说两者「同源」，但**同源 ≠ 同槽位**。决定性反问是「结果可指认」那一条：**你能贴出什么来证明"本批没用笃定措辞"？** 什么也贴不出来；一份交付完全可以既有合规的未尽处段、又满篇笃定措辞，而**没有任何位置会因此空着**。**若将来要给它挂载体，已知可行的形态是把承载的那半改写成槽位内容**——例如「未尽处段里必须逐条点明本批哪些手段是近似的」，那样就有东西会空着、也就指认得出；本批**刻意不这么改**，因为改法与"守住占比"同向，而自己给自己的形态判及格线属结构性利益冲突，该由下一个人或用户裁。） [n=4 @07-26 触发:无] [仅判据·无触发] [自定@07-27] [#官通-禁笃定措辞]
- **性能类改动需性能哨兵断言**：正则量词/集合规模/缓存边界类改动除行为断言外须补一条性能哨兵测试——纯行为断言看不见悬崖（行为不变、测试全绿，只有延迟劣化两三个数量级）。三条配套铁律：①条款/注释禁记绝对耗时数字，只记"悬崖存在、位置、留余量"这类可移植结论（同一曲线换台机器绝对值可差数倍）②哨兵须标明 profile 依赖并按最快侧标定（debug 变红不代表 release 也变红）③哨兵阈值对通过侧和失败侧都留够余量，不只验"现在能红"（2026-07-26 批45/47 实证）。 [n=2 @07-26 触发:无] [仅判据·无触发] [#官通-性能哨兵]
- **调参三问（不是双问）**：数值参数取值必须回答三个问题——①改小会怎样 ②当前值够不够 ③已知需求上限到二者之间那段有无真实需求。"引擎能吃多大就取多大"不是 fail-closed——fail-closed 的含义是"相同收益下选更保守的一侧"，收益为零的那段代价不该付（同一参数上连犯两次单向漏问，出处：windsurf-dao 叙事档 N7）。 [n=3 @07-26 触发:无] [仅判据·无触发] [#官通-调参三问]
- **对照组必须验证它自己真的被关掉了**：A/B 对照改源码时，依赖数组没变的 `useEffect` 不会随 HMR 重跑，旧的注册/监听/handler 仍挂在旧实例上——症状是对照组与实验组结果相同，极易误读为"功能本来就没生效"。解法：整页 reload + 重建被测对象，不指望 HMR 卸载副作用（2026-07-26 批6 实证）。 [n=1 @07-26 触发:无] [仅判据·无触发] [#官通-对照组自验]
- **测试里构造控制字符必须用转义写法，禁写字面控制字符**：git 只要在前 8000 字节里看见 NUL 就把整个文件判为 binary，diff 不可读、审查看不见内容。改用语言级转义（TS 的 `"\0\x01"`、Rust 的 `"\0\u{1}"`）——运行时是同一字符串，源码却是纯 ASCII。判定命令：文件里 `[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]` 计数应为 0（2026-07-26 批6 实证）。 [n=1 @07-26 触发:无] [仅判据·无触发] [#官通-控制字符转义]
- **字节级/整文件覆写操作，写入前必须校验缓冲区**：PowerShell 数组切片返回的是 `Object[]` 而非 `byte[]`，类型不符异常在默认 `$ErrorActionPreference='Continue'` 下被吞、残缺缓冲区照样落盘（2026-07-26 **帅亲手把一个 8661 字节测试文件写成 2 字节**的实证，靠 `git checkout --` 零损失恢复）。三条硬要求：①脚本开头 `$ErrorActionPreference='Stop'` ②写入前断言新缓冲区长度符合预期+一条绝对下界 ③写入后重新读回校验。目标文件需要字节级操作时说明已超出"一次 Edit 级微调"范畴，应派将而非亲手上。 [n=1 @07-26 触发:无] [仅判据·无触发] [#官通-缓冲区校验]
- **收账必须双向对齐 PROGRESS 与 TODO，「部分完成型」池目最易漏勾**：PR 合并时记了 PROGRESS 却忘勾 TODO 对应池目 ⇒ 池里留下幽灵待办，下次选题被当真待办派人去修已经修好的代码。最危险的是"首批已清 ✅、余量待续批"这种自带 ✅ 的条目，扫一眼像是处理过了。两条硬要求：①收账逐条问"这条现在还剩什么没做"，不只看有无 ✅ ②派实现批前写明"清单可能已过期，以你实测为准"（2026-07-26 实证：这条要求让实现官当场识破一个三天前已合并 PR 造成的幽灵待办）。 [n=1 @07-26 触发:无] [仅判据·无触发] [#官通-收账双向对齐]
- **实现官接到「按清单修 N 处」类任务时，先证明这 N 处还存在**：清单来自历史记录而仓库在推进，记录写下那一刻为真不等于此刻为真。零发现也是合格交付，且应顺带核实历史记录本身（查 PR/commit 是否已覆盖）并回报编排侧修账，不沉默地"没找到就不改"。 [n=1 @07-26 触发:无] [仅判据·无触发] [#官通-先证存在]
- **改/迁/删任何被别处引用的规则或文案，同批 Grep 引用面**（2026-07-27 由「改用户可见文案时必须 grep QA 清单」泛化而来，收割 H1-5）：判定标准里写着期望文案原文的走查清单、指向某条条款的"兜底"说明、引用某文件某节的指针——**被引用方一改，引用方静默失效**，失效形态是"照着旧内容做会得到假结论"，极难归因。**留一个指向空气的指针比没有指针更糟**（读者以为有兜底）。硬要求：改 `src-ui/src/i18n/locales/*.json` 里出现在 `docs/qa/dogfood-regression-checklist.md` 判定标准里的文案、或迁移/删除一条规则/条款时，同批 Grep 全仓引用并同步（清单留新旧对照段，指针改指新址或删掉）。**⚠️「全仓」不等于「本仓」（2026-07-28 补，本条自伤第三例）**：mousse-cli 与 windsurf-dao 双向引用密集（dao.md / skills / workflows / stacks 互指），**被引用方若可能被另一个仓引用，Grep 面必须含那个仓**。判据：改的东西**有没有名字会被别人写下来**（文件名 / 命令名 / 条款标题 / 节标题）——有，就跨仓 Grep。跨仓改动进不了同一个 PR，所以正确形态是**同批开一个跨仓 issue**，不是「顺手记一下」。实证三例：2026-07-26 批7 v1.53.0 确认卡改版后 R16/R19 路径 B 预期仍写旧卡文案（dogfood 官当场发现修正）；2026-07-27 windsurf-dao `710ba5b` 迁走 dao.md 两条静默自检条款后 `stacks/README.md` 强制执行链仍指着它；**2026-07-27 PR #272 把 `check-harvest-due.ps1` 改名，本仓 10 个引用点同批改完、Grep 全仓零残留，而 windsurf-dao `ccswitch/workflows/README.md:238` 滞后一天指着一个已不存在的路径（issue #274，由 PR #272 的对抗验证官捞出）——那个 PR body 明写「扫了本仓」，这一处恰在它自己声明的射程之外**。**⚠️ 第四例，射程再补一格（2026-07-28 收割 H3-10）：「引用面」被读成了「引用这条规则的地方」，而没覆盖「这条规则所描述的那个对象自己的注释」。** 实证：2026-07-27 因为同一个病订正过下面那条「新增机检项先判闸位」——原文写死「三条观察线（`check-core-loc` / `check-version-tag-sync` / `check-harvest-due`）」，订正为「一律以 verify-all 汇总表的闸位列打印为准，此处刻意不点名、不写条数」。**那次只扫了条款库**；而 `scripts/verify-all.ps1` 里 `check-core-loc` 那一项的行内注释至今写着「**IsGate=$false：本条是本仓唯一的观察线，其余九条皆硬闸**」——2026-07-28 实况是 **5 条观察线 + 14 道硬闸**，一句话两个数字全错，且它就住在被描述的那个对象里。⇒ 判据补一格：**改一条「关于某个对象的陈述」时，Grep 面要含那个对象自己的源文件与头注**——被描述者往往也在描述自己，而它比文档更少被人回头看。 [n=4 @07-26 触发:无] [仅判据·无触发] [基线:入库时该类问题近 3 次同类动作中 3 例] [#官通-同批查引用]
- **⚠️ always-on 规则的投递有「有界延迟」**：中途写入的条款要等下一次 compaction 才进得了 subagent 快照（刷新事件是 compaction 而非文件写入，实测延迟 ≈12 小时）。**原表述「当轮及本会话后续所有 agent 都读不到」已被证伪，勿再犯该过强表述**——它可测、可绕：绕法见本文件开头「派单令必带首行」（subagent 自己 Read 盘上文件，读到的永远是最新版）；`dao-rule-echo.js` hook 挂 PostToolUse 兜底回灌，自身失效用 `--selfcheck` 检出（双路核验：注册串+真实触发心跳，自测心跳带 synthetic 标记不予采信）。完整调查过程见 windsurf-dao 叙事档 N8。 [n=2 @07-26 触发:无] [仅判据·无触发] [#官通-投递延迟]
- **诊断「没被遵守」前，先证明「已被投递」**：观察到规则未被执行时先分清是「读了不遵守」还是「从未读到」——这两种病的处方相反，前者该加机器闸，后者该修投递通道。归因前问一句"这条当时在不在我/执行者的上下文里"，查不清就不做删除类决定（2026-07-26 帅诊断错误实证：混杂数据导致向用户误报"原则表达不被遵守"）。 [n=1 @07-26 触发:无] [仅判据·无触发] [#官通-先证投递]
- **查前提比照做更值**：用户或上游给出思路时先花几个只读探测验证前提，而非直接照做——照做只得到一个不生效的动作，查证反而能得到正确的地图（2026-07-26 实证：用户提"先上行同步把 live 推到源"，实测前提不成立，一查即修正了数据流认知）。 [n=1 @07-26 触发:无] [仅判据·无触发] [#官通-查前提]
- **切目录跑验证必须带目录守卫 + CWD marker，否则会静默跑错仓**：`git worktree add` 若因分支已被占用而失败，后续 `Set-Location` 到不存在目录同样失败，末尾脚本却在"当前目录"执行且输出退出码 0、看起来完全正常。硬要求：①脚本开头加目录守卫（特征文件不存在即 exit）②打印 `VERIFY_CWD`/`VERIFY_BRANCH` marker，复验先核对这两行 ③`git worktree add` 前先 `git worktree list` 查该分支是否已被占用（2026-07-26 帅假验证实证：靠日志路径不对才发现验的是主仓旧代码）。 [n=1 @07-26 触发:无] [仅判据·无触发] [#官通-CWD守卫]
- **`ln -s` 在 Windows Git Bash 下会静默降级为复制**：执行后 `ls -la` 看权限位——`lrwxrwxrwx` 才是真符号链接，`-rw-r--r--` 说明得到的是副本，副本不跟随源更新即是新的漂移面。需要真链接用 PowerShell `New-Item -ItemType SymbolicLink` 或设 `MSYS=winsymlinks:nativestrict`；但先问这个位置到底该不该用链接（本仓 `dao.ps1` 对 styles 的设计就是刻意用复制，2026-07-26 实证）。 [n=1 @07-26 触发:无] [仅判据·无触发] [#官通-软链降级]
- **写 `docs/research/` 前先过门槛三问**（用户拍板 2026-07-26：「保留但提高门槛」）：①谁会读它、在什么场景读——答不出具体场景就别写 ②结论能不能压进一条 TODO 或一条派单条款——能压就压，条款有投递机制（rule-echo 回灌、派单令引用）而 research 文档没有 ③它会不会过期——会过期的内容（仓库现状盘点/依赖版本/竞品行为）必须写明盘点时刻。三问全过才建文档；反例：某 spike 提交信息声称"证据见该文档附录"，附录从未存在——调研承诺了落档却没兑现。 [n=1 @07-26 触发:无] [仅判据·无触发] [#官通-研究门槛三问]
- **写给用户照做的命令，必须按用户的执行环境写并实跑一遍**（2026-07-27 帅实证：用户跑**帅在对话里递给他的**验收命令当场 MODULE_NOT_FOUND。**2026-07-27 与 `docs/USER-ACTIONS.md` §11.5 那句「用户实跑踩到」统一措辞并裁定**：两处记的是同一件事，`git log` 实算——引入那句话的 `fa8ffd7`〔07-27 00:46:02〕commit body 自己写的就是「用户实跑**我给的**验收命令」，本条所在的 `f44ab00` 是它 36 秒后同会话的姊妹提交，**无冲突，命令是对话里递的**。此事因此**不构成「用户读过某个账本」的正面证据**）。判据：Claude Code 的 `!` 前缀走 **Git Bash**——**Windows 反斜杠路径会被当转义吃掉**（`D:\frank\x` → `Dfrankx`）。故面向用户的文档一律用正斜杠+引号或 `/d/` 形式。**更隐蔽的同族**：代码块标着 ```bash 里面却是 PowerShell 语法（`$v = "v$((Get-Content ...))"`）——标注与内容不符，照抄必崩。**硬要求**：写进「可照做清单」类文档的每条命令，落笔后**自己实跑一遍**再提交；跑不了的（需用户凭据/需交互）至少验证路径可达与语法形态。本条与「禁笃定措辞」同源——没跑过就写「照这个做」，是把未验证当已验证。 [n=1 @07-27 触发:无] [仅判据·无触发] [#官通-用户命令实跑]
- **别把「检测器已覆盖」当兜底承诺，先读它的判据**（2026-07-27 帅对用户说错话实证）。帅曾向用户宣称「刀F 修好的漂移检测器从此能报出竞品 hook 复发」——**该结论未经查证即被当作兜底**。清尾官读码证伪：`settings-drift.js` 的 `daoScriptOf()` 对无 `.js`/`.ps1` 后缀的命令串返回 null 即跳过，且 `tests/settings-drift.tests.js` 有**负例断言明确钉住「第三方 exe 不得报」**——这是刻意设计（否则用户装的每个工具都会制造噪音），不是缺陷。**危害**：用户据此以为有自动兜底，实际那一面永远静默。**判据**：说「X 会被 Y 检出」之前，读 Y 的判据代码或跑一次真实反例；检测器有意不覆盖的面要显式写进文档并给手动复查命令，不要靠口头承诺。与「禁笃定措辞」同源。 [n=1 @07-27 触发:无] [仅判据·无触发] [#官通-检测器非兜底]
- **`git checkout -- <file>` 复原 mutation 前先确认基线是哪一态**（2026-07-27 清尾官自曝实证）。做 mutation 验证后用 `git checkout --` 复原，**复原到的是 HEAD 不是「工作区改前态」**——若该文件同时含尚未提交的本批实现，一并被冲掉。清尾官因此把已写好的 ②实现全部丢失，靠会话内原文重建（**重建不是恢复**，须复审官重点核该 diff）。**正确做法**：mutation 前 `git stash` 或先 commit 本批实现，再做 mutation，再 `checkout --` 复原；或直接用编辑工具反向改回。**通用形态**：任何「复原」动作都要先问「复原到哪个基线，那个基线包含我刚做的事吗」。 [n=1 @07-27 触发:无] [仅判据·无触发] [#官通-复原前确认基线]
- **多官在途时收账一律 `git add <具体路径>`，禁 `-A` / `.`**（2026-07-27 收割 H2-3 升格）：**主仓在多官并行期间与 windsurf-dao 一样是共享仓**——别人的半成品、被截断的 shell 命令产生的垃圾文件都落在同一棵工作树上，`git add -A` 会把它们一并收走并推上 main。实证（帅本人）：一个 89KB 的 `et VTPROBE_HOST=mousse` 进了 main 并推送，来源是某位在途官的 `set VTPROBE_HOST=mousse` 被截断——`s` 被吃掉，剩下的成了重定向目标文件名，落在仓根。**机检半是观察线不是闸**：`scripts/check-worktree-strays.ps1`（挂在 verify-all）打印「此刻 `git add -A` 会额外收走什么」，仓根层未跟踪文件单独高亮（那是这类垃圾的精确落点）；它**不拦**，因为「工作树里有未跟踪文件」本身没有对错、而机器读不到「本次改动清单」，做成硬闸会在任何人写新文件时变红随即被 `-Skip` 掉。**它也不是 pre-commit 钩子**，不保证「add 之前一定看到」——这是已知缺口。**附带的元教训（这条更值钱）**：**下级给的警告若带了适用范围，接收方缩小范围就等于没收到**——本例里体系改造官同窗早些时候已明说「在共享仓用 `git add -A` 是真风险」，帅把它读成了「windsurf-dao 的问题」。同型风险面 = 所有「A 报告了一个现象 + 一条建议，B 只采纳了现象所在的那个场景」。 [n=1 @07-27 触发:verify-all/check-worktree-strays] [基线:入库时该类问题已知 1 例（89KB 垃圾文件进 main）；「多官并行收账」这一动作在本仓的历史发生次数未统计，故命中率无从算，写「未测」更诚实] [#官通-禁add全量]
- **新增机检项先判闸位：「代码错了」用硬闸，「人该判断一件事」用观察线**（2026-07-27 收割 H1-7）：硬闸的语义是"不修不许过"，只适用于有客观对错的事（编译失败、断言红、结构缺字段）。凡结论形如"你该去做某件判断"（该退役这条条款吗 / 该收割了吗 / 该升格了吗 / 该补 tag 了吗）**一律做观察线**（恒 exit 0、只打印、不参与红绿）——做成硬闸会让人为了过闸而敷衍执行那件判断，而敷衍的产物（凑数的候选、补的空 tag）比不做更糟。观察线不是"弱化版硬闸"，是另一种东西：它的职责是让状态每次都在人眼前过一遍。落地形态即 `scripts/verify-all.ps1` 的 `IsGate` 字段（必填二选一），本仓**全部**观察线与 `check-clauses-structure` 的统计段一致如此设计。**此处刻意不点名、不写条数**（2026-07-27 订正：原文写死「三条观察线（`check-core-loc` / `check-version-tag-sync` / `check-harvest-due`）」，此后新增 `check-worktree-strays`、本轮又新增 `check-ledger-freeze` 并把第三个改了名，一句话三处同时过期——**当前清单一律以 verify-all 汇总表的闸位列打印为准**，这正是本文件通用节「改被别处引用的东西要同批 Grep 引用面」讲的那种静默失效）。 [n=1 @07-27 触发:IsGate槽位] [基线:未测] [#官通-闸位判断]

## 复审官节

- **派复审 range 前必须实算，禁目测**：用 `git merge-base <各分支 commit>` 求共同祖先，或 `git log --graph --all` 逐个 merge commit 取 `%p` 核对——禁凭"最近几个提交"估起点。两次踩坑同因：发布/收账提交位于 PR 合并点之后，`<收账提交>^..HEAD` 会把该收账所覆盖的 PR 全部排除（r6 漏 #209/#210，r7 漏 #212/#213，两次都靠复审官自行纠正才没出事）。派单令应显式写"自行核对 range，不要盲信我给的建议值"——但这只是兜底，帅仍须先实算。**2026-07-27 挂上载体（复审官这一侧）**：复审官必有一个 range，实算它是这个官种**定义性、无条件、且结果为具体 commit 串**的开工动作，而派单必带首行保证本节被读到——故填 `模板首行`。**帅那一侧仍是 `无`**：帅不经派单、不读本节，"派之前先自己实算"没有任何载体（两次踩坑都是帅估错、靠官自行纠正）。 [n=2 @07-25 触发:模板首行] [基线:未测] [自定@07-27] [#官复-range实算]

## 实现官节

- 契约引用：派单 prompt 必须引用具体契约文档路径（`docs/kit/**/spec.md`、`acceptance.md`、对齐矩阵、UX 定稿等），禁止凭会话记忆拼需求——上下文压缩后细节必丢（实证：邀请制解锁需求写在三处文档里仍被凭记忆派单漏掉）。**2026-07-27 挂上载体**：本条的执行者是**帅不是实现官**（它约束的是派单 prompt 怎么写），载体是 dao.md 帅节的**派单契约门**——那里逐字要求同一件事（「派单 prompt 必须引用契约文档路径」），而派单 prompt 是每次派将必然产出的东西。它放在实现官节只是因为验收时对照的是同一份文档，**别读成"实现官该做的事"**。 [n=1 @07-24 触发:派单契约门] [基线:未测] [自定@07-27] [#官实-契约引用]
- 覆盖矩阵：派单 prompt 必须列**本批覆盖 vs 显式不覆盖**两栏，验收时对照同一份文档，不允许实现官自行扩大或缩小范围而不声明。**2026-07-27 挂上载体**：同上条——执行者是帅，载体是 dao.md 帅节**派单契约门**的「列本批覆盖 vs 显式不覆盖两栏」，逐字同一要求。 [n=? @07-24 触发:派单契约门] [基线:未测] [自定@07-27] [#官实-覆盖矩阵]
- 开工前基点对齐：进 worktree 后先跑 `git -C <worktree> merge-base HEAD main` 核对是否等于当前 main tip；基点落后（worktree 与 main 分叉超预期）先 `merge main` 再开工，不带着陈旧基点交付（L24 两案实证：基点漂移是常态风险非偶发）。**2026-07-27 挂上载体**：本仓实现批一律 worktree + PR，「进 worktree」因此是这个官种定义性、无条件的第一个动作，核基点是它紧邻的一步且产出一个可贴的 commit 串；派单必带首行保证本节被读到——故填 `模板首行`。 [n=2 @07-24 触发:模板首行] [基线:未测] [自定@07-27] [#官实-基点对齐]
- **交付前基点复检：`MERGEABLE` 不是基点新鲜的证据**（2026-07-27 收割 H1-2，PR #244 实证：官自行发现并执行，非派单令要求）：并行批交付/终审前必须重查 main 是否在工作期间前进（`git fetch` + `merge-base HEAD main` 与 main tip 对比），前进了就合入 main **重跑整套验证**再交付——`gh` 报 `MERGEABLE` 只证明**无文本冲突**，语义冲突（同一批文件被另一 PR 改过、共享 i18n/常量表被并发追加）它一概看不见。与上一条「开工前基点对齐」是同一风险的两端，缺哪端都会漏。**2026-07-28 收割 H3-2 补一个亚型，它比「共享文件被并发追加」更难看见**：**阈值/预算型硬闸**（LOC 红线 / bundle size / 依赖数 / 复杂度）对**和**设限，而 PR 是按**增量**审的 ⇒ **两边各自都没越线，合起来越线**。实证 PR #290：本 PR 给 `session.rs` 加 52 行、main 侧 #282/#288 接线加 23 行，合并后 **308 > 300**，`check-command-loc` 硬闸红，而 `gh` 当时报的是 `MERGEABLE`。**这个失效模式只在两边都合规时发生**——任一侧单独提交都会被当场拦下，恰恰是「两边都守规矩」的情况才能悄悄把和越过线，**越是纪律好的仓库越容易撞上它**，且没有任何一侧是违规方。⇒ 预算型护栏必须在**合并态**求值，分支态的绿不构成证据。 [n=2 @07-27 触发:PR流程] [基线:2026-07-28 窗首次实测——本窗 6 个 PR 中 4 个执行了交付前基点复检（#290/#294/#288/#298），**1 次真命中硬闸红（#290，25%）**，另 3 次重跑后无变化；那 1 次 `gh` 报的是 `MERGEABLE`/`UNKNOWN`。此前长期 `[基线:未测]`] [#官实-基点复检]
- **幂等接续**：接手中断任务的官，执行任何有副作用动作（commit / `gh pr create` / 记账）前先查该副作用是否已由前任发起（`git log` / `gh pr list`），不得假设从零开始（出处：五镜侦察 outside 镜 F2，LangGraph 恢复幂等原则）。 [n=? @07-26 触发:无] [仅判据·无触发] [#官实-幂等接续]
- **加规则/别名/关键词前必须实测该词在其他语境的含义**：往拦截清单、动词集、关键词表里新增词条前，逐个查它在目标平台上是否另有含义（同名可执行文件、常见参数、日常词汇），有歧义的显式挂账说明为何不补，不硬塞。正向出处：`Get-Command <名> -All` 本机实测——`ac`（Add-Content 别名）无同名可执行文件⇒补；反向出处：`sc`（Set-Content 别名）同时是 `system32\sc.exe` 服务控制管理器⇒显式不补并落负控断言钉住（2026-07-26 批45/38）。 [n=2 @07-26 触发:无] [仅判据·无触发] [#官实-新词实测]
- **新增覆盖面须同时给出该形态的误伤反例**：每补一类命中形态，必须配一条"形似但不该拦"的负控断言；只证明"能拦住"不算完成——护栏的两侧代价不对称但都是真代价。 [n=1 @07-26 触发:无] [仅判据·无触发] [#官实-误伤反例]
- **判据类/护栏类改动不许先合后审**：改动落在以拦截/守卫/扫描/判定为职责的文件时（`crates/**/risk*.rs`、`scripts/check-*.ps1`、各类 guard/lint/scanner——可按路径机械判定），`gh pr create` 后**必须先过对抗验证官再合并**。判据正文与实证在 windsurf-dao `ccswitch/dao.md` 帅节（跨项目适用，此处只留执行侧一行）：实现官跑构建+单测，对抗验证官跑 mutation + 双向语料夹击，后者能发现的东西前者**结构上**发现不了。 [n=1 @07-27 触发:PR流程] [基线:本仓修复间隔呈双峰，24% 的返工发生在合并后 4 小时内] [#官实-禁先合后审]

## 对抗验证官节

- 独立复跑不信报告：实现官自报"全绿"不算数，验证官必须独立复跑构建/测试（不复用实现官的日志/截图作为证据），得出自己的真退出码。**2026-07-27 挂上载体**：这就是这个官种的定义本身（无条件、每批必做、产出一组可贴的真退出码），派单必带首行保证本节被读到——故填 `模板首行`。 [n=? @07-24 触发:模板首行] [基线:未测] [自定@07-27] [#官抗-独立复跑]
- 换靶 mutation 两态：对承重测试做 mutation 验证——故意改动被测逻辑（改名/改判据/翻转条件）确认测试真的变红，再复原确认变回绿，两态都要看到，不能只看"测试存在"就判定测试有效。**2026-07-27 挂上载体**：同上——mutation 是这个官种的核心动作，无条件且两态都是可贴的观察结果，故填 `模板首行`。 [n=? @07-24 触发:模板首行] [基线:未测] [自定@07-27] [#官抗-换靶两态]
- **mutation 报告需附加「调用点覆盖率」字段，不能只报「红绿两态都验证过」**（2026-07-30 收割 R4-Q4；紧接上一条「换靶 mutation 两态」，不新开条款，是给已经必然发生的报告加一格）：本窗（约 30 小时内）5 起独立审计（#310/#323/#338/#342→#343）均发现"多调用点行为，实际断言覆盖数远低于调用点总数"，覆盖率区间 0%～93.75% 不等，且全部靠事后专项审计发现——没有一次是在原始实现批里被自己的报告暴露出来的。对抗验证官的 mutation 报告沿用上一条既有的 `触发:模板首行`：被测 guard/dispatch 函数的**生产调用点总数 N**（`grep -rn '<函数名>('` 实数）vs **本批真实端到端覆盖数 M**，未覆盖的 N-M 个调用点逐一列出文件名。 [n=5 @07-30 触发:模板首行] [基线:本窗约 30 小时内 5 个独立实例，覆盖率分别为 60%（#310）、0%（#323）、75%首批/100%补齐后（#338）、93.75%单点/6.25%端到端（#343）] [自定@07-30] [#官抗-调用点覆盖率]
- **mutation 的「改坏」本身要试不止一种形态，否则会得到「自证已通过却仍然漏」的守护**（紧接上一条「换靶 mutation 两态」，2026-07-29 PR #312 实证）：同一段 `case`，**删掉 ⇒ 红，注释掉 ⇒ 绿**——而那份守护末尾的 mutation 自证 footer 记的**正是「删掉」那一种**，并据此宣告判别力已坐实。**它没说谎，它只是试了唯一有效的那种改法。** ⇒ 至少要覆盖**三**个方向：**①移除**（删整段）②**保留字面但使其不执行**（注释掉 / `if (false)` / 提前 return / 换到不会被调用的分支）③**保留调用与副作用，但结果不被消费**（`if (!ok) return;` → `void ok;`）。文本匹配型守护对 ② 天然失明；行为型测试对 ① ② 通常都敏感——**所以①②这一对对文本匹配型守护是必答项**。**③是 2026-07-29 PR #337 补的第三向，而它是那一批唯一有产出的一向**：同批 16 个 `confirmDialog` 调用点，①②红集**零差额、跑出 0 个缺陷**，③跑出 **1 个**——`HistoryPanel` 删会话，用户点「取消」⇒ **文件照删、终端照杀**，全仓 950 条零变红。**判据一句话：前两向验「门在不在」，第三向验「门的答案有没有人听」。** **为什么③比①危险**：整个摘掉，code review 一眼看得见；③**弹层还在、交互还在、动画还在**，人工走查从头到尾看不出任何异常——**它骗得过的不是测试，是人眼**。**同批另一个方法论结论（省掉后来人重走一遍）**：①②那个「注释掉→绿」的病是**守护类型**的病（文本匹配型），**不是消费方的病**——按消费方逐个去找它，找不到。**⚠️ ③的收益是结构依赖的，不是通例**（2026-07-29 同日阴性结果，照记）：另一靶上①②③**红集完全相同、差额为零**——因为被测变量唯一的可观测效果就是它喂出去的那个选择，「算了不用」与「压根没算」在行为上不可区分。**⇒ 三向都要跑，但别预期③必然有增量；差额为零也是结论。** **③的真实产出是「哪条断言在承担这个防护」，不是「发现新缺口」**（2026-07-29 第二例，PR #340）：另一批 4 道门上③的红集是①②的**子集**、差额为零，但它量出**唯一挡住③的是「取消侧」那一条**——若只写「正靶 + 确认侧 + 负控」这个最自然的组合，**四道门在③下红集全是 0**。**⚠️ 第四件事：检查你的 mutation 是不是全在一个方向上。** 同批实证：12 次 mutation 全在「让门变松」这一侧 ⇒ **5 条负控一次都没红过**，直到另跑 5 次**反向** mutation（把条件改恒真）才确认 17 条新断言**每一条都真的红过**。**「每条新断言要验它真会红」这句话，覆盖不到「你的改法方向单一，导致某一类断言结构上永远验不到」。** **同批实证的规模**：本仓 14 份读源码文本做断言的守护里，**8 份对 ② 失明**，而其中多份的 footer 都写着「已 mutation 验证」。 [n=1 @07-29 触发:模板首行] [基线:14 份同型守护中 8 份对「注释掉」形态失明，且其中至少 1 份的自证 footer 明确宣告过判别力已坐实] [#官抗-改坏多形态]
- **🔴 mutation 之前先验「变异体还活着」——否则「全红」与「夹得密不透风」长得一模一样**（2026-07-29 立，本条是「零检出≠零存在」的**镜像**：那条说绿信号会答错问题，**这条说红信号一样会**）：实证——某批首轮 7 个 mutation **全部给出同一个 59 条的最大红集**，看起来像「这份守护密不透风」，实为**把靶弄死了**（改写脚本时用 `UTF8Encoding.GetBytes()` 不输出 preamble ⇒ BOM 丢失 ⇒ PS 5.1 按 CP936 重读中文脚本，被测对象整个报废）。**一个杀死被测对象的 mutation 会让每一条断言都红**，而那正是「判别力强」的表象。⇒ **每次 mutation 后先跑一个 canary 确认变异体本身仍能正常执行**（能跑到被测逻辑、只是行为被改），再读红集；**canary 本身也要能自证**——本例作者第一版 canary 用中文标记判活，**被同一个编码坑打成 live=False**，第二版才可信。判据一句话：**读红集之前，先回答「这一版还跑得起来吗」**。 [n=1 @07-29 触发:模板首行] [自定@07-29] [基线:某批 7/7 次 mutation 的红集因靶被弄死而失真，且该失真形态与「判别力满分」不可区分] [#官抗-变异体存活]
- 字节级复原：mutation 验证结束后必须把改动精确复原到 mutation 前的字节状态（diff 干净），不得让验证过程本身残留改动。**2026-07-27 挂上载体**：它紧跟在上一条之后，而上一条对本官种无条件，故复原这一步同样必然到达且有客观判据（`git diff` 空）——填 `模板首行`。配套读通用节「`git checkout -- <file>` 复原 mutation 前先确认基线是哪一态」，那条讲的是复原到**哪个**基线。 [n=? @07-24 触发:模板首行] [基线:未测] [自定@07-27] [#官抗-字节级复原]
- 负控组：除正例外，构造反例/边界例验证判据不会误伤——如扫描器类改动要证明"合法形态不会被误判为违规"，护栏类改动要证明"不该拦的不拦"。 [n=? @07-24 触发:无] [仅判据·无触发] [#官抗-负控组]
- 承重字段完整输入：核验官读取待验证产出时，承重字段（如原始 findings/summary/evidence）禁止静默截断（`JSON.stringify(x).slice(0, N)` 类写法是已知缺陷来源——核验官看不到被截断部分会误裁），要么完整传递要么按字段分开传。 [n=1 @07-24 触发:无] [仅判据·无触发] [#官抗-承重字段完整]
- PASS 判据显式化：终审前验证官需给出明确 `{isReal/sound/pass, reason}` 结构化裁定，附独立证据锚点（文件+行/命令+真退出码），不用"看起来没问题"类模糊措辞。 [n=? @07-24 触发:workflow-schema] [#官抗-PASS判据显式]
- **比较基线必须先验证其本身是活的**：拿"改前 vs 改后"做对比时，先证明改前那一版真的在工作——一条已经失效的规则当然"零误伤"，拿它当基线会把修复的必然代价误判为退化。假基线比没有基线更危险——它看起来有数据支撑（2026-07-26 通宵窗 G-5 实证：三版本 A/B 才发现前几轮"零误伤"是假象）。 [n=1 @07-26 触发:无] [仅判据·无触发] [#官抗-基线是活的]
- **判别力缝的自检问句**：补完断言后问一句"任何把这个逻辑放宽或收紧的改动，是否都至少有一条断言会变红？"——语料只夹住它恰好含有的那些字符/取值。单向 mutation 不构成对双向调整的验证（2026-07-26 G-1/G-3 实证：窗口收窄方向被夹住、放宽方向零守护）。 [n=2 @07-26 触发:无] [仅判据·无触发] [#官抗-判别力自检]
- **近似手段的验证语料禁只来自本轮发现的形态**：核验正则/启发式类改动时，要查它的正控与负控语料**从哪来**——语料若由本轮发现的形态构造，只能证明「上一轮那些洞已补」，证不了「还有没有第 N+1 类」，结构上必然产出补漏—再漏循环；应从真实历史样本（命令历史/线上日志/竞品规则库/历史事故）采样。派单侧的必填形态与实证在 windsurf-dao `ccswitch/dao.md` 帅节·派单契约门附条（跨项目适用，此处只留核验侧一行）；配套停手信号：**同一文件 24h 内第 3 次返工即转设计评审**（`git log --since=24h -- <file>`）。 [n=11 @07-27 触发:派单契约门] [基线:本仓某护栏正则被 11 个 PR 触碰、其中 8 个集中在 19.8 小时内，每层修复面 100% 落在上一层刚写的行上（共享比例 0.92–1.00）] [#官抗-语料非自证]

## 侦察官节

- 只读红线：侦察全程不写不改，包括不新建文件、不 commit、不动配置——发现异常也只记录不擅自修复（修复需另派实现官）。**收工时必须在报告里写一行「本轮零写入（`git status` 空）」**——这一句是 2026-07-27 由对抗验证官加的，正是它让本条挂得住：原先它是**纯禁令**、没有"填它的那一刻"，加上这行自陈后它与同节「零增量明说」同型（报告里有个位置在等着填、填了就指认得出），`模板首行` 才站得住。**弱处照写**：没有任何程序在核那句自陈是否为真（写了「零写入」而实际写了文件，闸不会响）。 [n=? @07-24 触发:模板首行] [基线:未测] [自定@07-27] [#官侦-只读红线]
- 结论带出处：每条结论/发现必须带可复核的出处（文件路径+行号，或外部 URL），零出处的论断一律不采信，压制臆测。 [n=? @07-24 触发:workflow-schema] [#官侦-结论带出处]
- 零增量明说：与既有基线对比无变化时，必须显式写"零增量"/"零相关变化"，不能因为没发现就沉默不报（沉默会被后续误判为"没扫这一路"）。**2026-07-27 挂上载体**：执行时刻是**写侦察报告**——这个官种必然产出一份报告，而"每一路都要有一行结论"是那份产出物里的槽位；派单必带首行保证本节被读到——故填 `模板首行`。 [n=? @07-24 触发:模板首行] [基线:未测] [自定@07-27] [#官侦-零增量明说]
- 分层读取：功能请求类反馈按"处方可疑、痛苦真实"分层——频度/烈度证的是场景强度不是方案正确性，入池按场景归因，不照单抓药；bug/信任崩塌类可近字面采信。 [n=? @07-24 触发:无] [仅判据·无触发] [#官侦-分层读取]
- 竞品/外部实测优先于臆断：涉及竞品能力核查时，能实机走查/实测的不停留在"应该是"层面；启动已装竞品软件属自主动作范畴，只有凭据/安装/更新才是用户件边界（2026-07-23 desktop 自检检讨教训：未实测桌面即下"用户件"结论）。 [n=1 @07-24 触发:无] [仅判据·无触发] [#官侦-实测优先]

## dogfood 官节

- **走查期间不动那棵树**（原标题写作「不动那棵树的 **git**」，**2026-07-28 补射程：不只是 git**）：真机走查/dogfood 进行中，**禁止在走查实例所服务的那棵工作树上做任何会触发文件变更的动作**——**① git 操作**（`pull` / `merge` / `checkout` / 切分支 / `stash`）**② 直接编辑源码**（含 mutation 换靶、临时加日志、改一行看看）。**②是本条原文的射程缺口**（PR #311 实证：实现官在 mutation 阶段编辑源码 ⇒ 整页 reload，清空走查实例的会话标签，**一次观察作废重做**。⚠️ **2026-07-29 措辞订正**：原文写作「Vite **HMR** 触发整页 reload」，**说反了**——HMR 恰恰是**不**整页重来的那一种，组件文件改动时状态保得住；真正清空走查状态的是改动**落在无 HMR 边界的模块**上（入口 / barrel `index.ts` / store / util），那时 vite 只能整页重来）——dev server watch 的是**文件**，它不关心你是用 `git checkout` 改的还是用编辑器改的，**后果完全相同**。原条款只点名 git 是因为写它的那次事故恰好是 git 引起的，**而条款写下的边界就是它能防住的全部边界**（同型：「同批 Grep 全仓」被读成本仓 · 既有条款只点名管道形态 · 「同型面」按文件名组织而漏掉住在别人文件里的键）。⇒ **要 mutation 就等走查做完，或者用第二棵树**。原文接下去的判据不变：dev 实例在 watch 那些文件，一次 checkout 就是一次整页 reload，**被测状态（会话、终端、未保存输入）在你没看见的时候被重建了**，此后所有观察都可疑。两条正路二选一：①走查用**独立 worktree**（本仓正路：`scripts/start-isolated-dev.ps1`，起前先 `git worktree list` 确认那棵树没人在动）②走查期间**冻结该树的 git 动作**，并在开工交付里写明冻结范围（哪条路径、大约多久），让别人知道现在不能碰。**射程边界照直写**：本条挂在派单首行上，只覆盖**被派的官**；**帅自己发起的走查不经派单、不读本节，那一半没有任何覆盖**——而下面这个实证里的违例者正是帅。**2026-07-29 两格实测（一格证伪、一格新增）**：①**`_tmp/` 不在射程内，不要为它扩格** —— 有官推测「往 `_tmp/` 写探针也算」，专项实测**不触发任何重载**（检测器先自证 + 双正控都响 + 45 秒静默负控零漂移，且机制是直接打印出来的：vite root 是 `src-ui/`、tauri 只 watch `crates/mousse-app` 与 `crates/mousse-core`，**`_tmp/` 两个 watcher 都不在**）；那次事故的头号候选是 `git pull` 快进重写了 9 个前端文件**含 barrel `index.ts`**，**落在本条最初那格（git）射程内**——**射程不够三次里，这一次是"不够"本身被证伪**。②**但「我没动过」不等于"没发生过"** —— 同批撞到一次**无人触发**的 reload，排除法做到最后唯一签名吻合的是 vite 客户端 `vite:ws:disconnect` → `location.reload()`，**它只在客户端打印、服务端日志一片空白** ⇒ **冻结写入不足以保证走查状态稳定**。故承重观察必须能自证「这一帧与上一帧之间没有发生过重载」（记 `performance.timeOrigin` 或等价标记再比对），**不能只靠"这段时间没人碰"**。 [n=1 @07-27 触发:模板首行] [基线:1 次疑似实证且**未确证**——issue #266：走查进行中帅在同一工作树跑 `git pull --ff-only`，触发 vite 整页 reload，与随后观察到的「终端会话变成 Codex 会话」时间上相邻；因果**没有证据链**，它只是替代候选之一，不得被引用为「已确证的根因」] [#官食-不动那棵树]
- 第一人称抽验价值：不满足于"有数据/有响应"，要问"数据对不对"——如按日粒度视图曾被验证官只验"有数据"未验"数据是否符合预期分布"，第一人称抽验多次捞出隐藏缺陷。 [n=1 @07-24 触发:无] [仅判据·无触发] [#官食-第一人称抽验]
