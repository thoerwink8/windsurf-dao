<!-- canonical issue 派单中枢 playbook · 真相源在 windsurf-dao ccswitch/templates/dispatch-hub.template.md
     项目侧从本文件派生为 docs/ops/DISPATCH-HUB.md（约定文件名，scaffold-manifest 按它核对）。

     派生时要做的三件事，其余原样保留：
       ① 把所有 <大写占位符> 换成本项目的真值（清单见文末「占位符对照表」）
       ② 填「§二.5 本项目的判据」两格（真机 / 守卫类）——这两格是空的就等于这两个标签没生效
       ③ 在「§六 项目特有裁决」里记本项目自己的取舍，别写回本 canonical

     ⚠️ 条件化：本模式只对 **GitHub-backed + 持续多窗/多 agent 派单**的项目生效。
     本地仓、一次性小脚本不需要看板+标签体系，一个文件面板兜底即可（判据见 dao-dispatch.md §一）。

     ⚠️ 2026-08-12（issue #324 B 批，用户拍板「中枢保留、机检强制力不留」）：
     原先有一条清单项在核「声明了用 issue 做中枢的项目有没有派生出这份 playbook」，
     那一条已删。**本文件从此是纯手册**：要不要派生、派生得对不对，由人判断，没有任何东西会红。 -->

# issue 派单中枢 · 流程总览（单一真相源）

> issue 区是任务的**登记处与账本**，会话只是执行通道——**会话崩了任务不丢**。
> 纪律条款在 `CLAUDE.md`（always-on 精简版）；本文是全景手册，**改流程先改这里再同步条款**。

## 一、全景：一件事的一生

```
想法/发现 ──┬─ 低置信（AI 推测/dogfood 疑点/外部情报）→ 打 `候选` 标签，不进行动层
            └─ 准入（用户点名 / AI 确信有证据 / 缺陷坐实）→ gh issue create + 打标 ← 行动层入口
                    │
                    ▼
       队列（领活循环：自主窗选题与每次心跳对账都扫 P0→P1）
                    │ 帅派单：三节点之一「⚔️ 已派」评论 + `在途` 标
                    ▼
       官干活（worktree 隔离 → PR → 止步 gh pr create）
                    │ 官交付：三节点之二「交付结论」评论
                    ▼
       帅终审（独立复验 + 一键验证真退出码 + merge）
                    │ 帅销账：三节点之三「关单结论」评论 + 摘 `在途`
                    ▼
                 CLOSED（每单关闭都有证据链）
```

**三节点留痕是硬要求**：派单 / 交付 / 关单各留一条评论。少了任何一节，这张单在事后就只剩标题——
而「这件事当时为什么这么定」恰恰是最贵的部分。

## 二、label 体系 = 派单标准（四维正交）

**canonical 定义在 `windsurf-dao ccswitch/templates/labels.json`（本机 `D:/frank/windsurf-dao/ccswitch/templates/labels.json`），本文不重抄**——
抄一份就是双份真相源，改一处忘一处是必然而不是意外。这里只放一眼可读的速查与本项目的收窄。

| 维 | 基数 | 标签 |
|---|---|---|
| **类型**（建单必打） | 恰选 1 | `缺陷` `任务` `欠账` `候选` `待拍板` `需用户` |
| **优先级** | 缺陷/任务恰选 1；欠账与蓄水池不打 | `P0` > `P1` > `P2` |
| **派单约束**（决定「怎么派」） | 可多个 | `结构受阻` <PROJECT_DISPATCH_LABELS> |
| **状态** | 0-1 | `在途` |

**帅读标签 → 派单动作速查**（完整判据与派单含义见 labels.json 的 `dispatch` 字段）：

- `缺陷`/`任务` + `P0`/`P1` + 无 `在途` → 领活优先派，按类型选官种（多机/多 AI 并行时：`在途` 但租约过期**且**盘上零活动 ⇒ 走接管流程再领，不直接抢；见 `ccswitch/scripts/dao-claim.ps1`）
- 任意 + `守卫类` → 实现官止步 PR，**必接对抗验证官**才合
- 任意 + `真机` → 排实例队列串行，PR 证据不许走「随后补」
- 任意 + `结构受阻` → **先出解锁设计**，不派实现官
- `候选`/`待拍板`/`需用户` → 不派，进各自蓄水池；`候选` 升级走 **relabel 不是 re-create**

**用户只需要记三个标签**：`待拍板`（要他决策）+ `需用户`（要他动手）+ `候选`（酝酿层，他扫一眼有没有该提优先级的）。

### 二.5 本项目的判据（派生时必填，空着等于该标签没生效）

| 标签 | 本项目判据（一眼可判） |
|---|---|
| `真机` | <REAL_MACHINE_CRITERIA> |
| `守卫类` | <GUARD_PATH_CRITERIA> |
| `结构受阻` | <BLOCKED_EXAMPLES> |

> 为什么这两格必须由项目填：概念是通用的（存在「测试全绿但真机是坏的」这类缺陷、存在以拦截判定为职责的代码），
> 但**哪些改动算、哪些路径算**只有项目答得出。照抄别的项目会得到一个在自己项目里一眼判不了的判据，
> 而一眼判不了的判据等于没有——谁都能说服自己这次不适用。

## 三、常设单（永不关闭，置顶）

GitHub 每仓**置顶上限 3 个**。三个槽的分配是：两个最高频的可回复 inbox + 一个总览 hub。

| 单 | 是什么 | 置顶 |
|---|---|---|
| <PINNED_DECISION_ISSUE> 📌 待拍板总览 | AI 攒决策，用户在评论区回一条即算拍板 | ✅ |
| <PINNED_USER_ISSUE> 📌 需用户总览 | 只有用户能做的事集中入口，含耗时估计 | ✅ |
| <PINNED_HUB_ISSUE> 📌 总览 hub | 一眼对齐入口：看板链接 + 各 inbox + 在途 | ✅ |
| <CANDIDATE_HUB_ISSUE> 📌 候选总览 | 酝酿层全景 + AI 的按住理由与 promote 条件 | ❌（从 hub 进） |

**为什么候选总览不占 pin**：用户说「X 也要置顶」时先问「X 是不是已经能进 hub 的某一行」——
能进就进 hub，别去挤那三个槽。hub 由帅每批收官刷新一次「现况」列。

**蓄水池纪律**：`待拍板` / `需用户` / `候选` 三类**永不进清零目标**、不催、攒批。
任何「issue 清零」类目标一律读作「**除蓄水池外**清零」。

## 四、批次机制（父单 + sub-issues）

多子任务的活开**父单**写目标+验收，子单逐一挂上去，父单进度条即清偿进度，子单全关父单收官。

```
gh api repos/<OWNER>/<REPO>/issues/<父单编号>/sub_issues -f sub_issue_id=<子单的 database id>
```

🔴 **`sub_issue_id` 要的是 database id，不是 issue 编号**——传编号会挂错单或报错，
而挂错的那一刻没有任何提示。取法：`gh issue view <n> --json id -q .id` 拿到的是 node id（GraphQL 用），
database id 走 `gh api repos/<OWNER>/<REPO>/issues/<n> -q .id`。两个 id 长得完全不同，别混。

舰队批处理同型：一批开一个批次父单，处理对象挂子单，批完关父单。

## 五、全量核账的五向分流（triage verdict）

批量核账（如「把所有 open issue 过一遍」）时每单判一向。**判 `ghost` 门槛最高**——
要证明「确实不存在了」，拿不准一律降级 `keep`（判错方向的代价不对称：误判 keep 只是多留一张单，
误判 ghost 是把一个真问题关掉且从此没人再看见它）。

| verdict | 含义 | 后续动作 |
|---|---|---|
| `ghost` | 已修 / 已过时 / 不再成立 | 帅复核证据链 → 关单留结论 |
| `fixable` | 仍成立可修 | 进实现批（批次父单组织，并行拆官） |
| `blocked` | 结构受阻 | 记解锁依赖，解锁件排优先 |
| `keep` | 成立但不属以上 | 留队列按 P 序 |
| <PROJECT_VERDICT_ROW> |

**官只读不写**：判定与关单草稿交回，`close`/`comment` 动作归帅（**终审不可让渡**）。

## 六、既有约束的裁剪（领活时逐条过）

- **独占资源类**（真机 / 单设备 / 单账号）：同时刻只准一路，排队合批清
- **账本类**（进度/发布物/版本号）：冻结归帅收账，不并行——多路并行时账本是唯一共享写入面，抢改必冲突
- **判据/护栏类改动**：对抗验证前置到 merge 之前，不先合后审
- 派单契约门、军衔通报、验证分级：见 `windsurf-dao ccswitch/dao.md` 帅节（本文不重复）
- <PROJECT_CONSTRAINTS>

## 六.5 三态归位——什么进 issue，什么留文件，什么进看板

**判据一句话**：说得出「关闭条件 + owner」→ issue；是聚合视图 → 看板；是流水/叙事/跨切 → 文件。
（dao 通用条款「工作项三态归位」的项目落地，dao.md 只留判据+指针。）

| 载体 | 装什么 | 本项目现状 |
|---|---|---|
| **issue 区（分配中心）** | 有生命周期的单元 + `候选` 酝酿层 + 蓄水池 | <PROJECT_ISSUE_STATE> |
| **看板（观测中心）** | 全部单的聚合全景，标签驱动归列 | [<PROJECT_TITLE>](<PROJECT_URL>)，六列：候选/待办/在途/待验/蓄水池/完成 |
| **仓内文件（叙事层）** | 无 per-item 生命周期的密集内容 | <PROJECT_NARRATIVE_FILES> |

**判据自检**：一个「问题树看板」型文件（`WORKBOARD.md` 型）在 issue 中枢+看板建成后多半**冗余**——
退役归档、活账交 issue，是为道日损。留着它就是双份漂移。

🔴 **一次性人工动作**（Projects v2 自动化 gh CLI 建不了）：看板的「新 issue 自动入板」workflow
要在网页点一次（约 2 分钟）。步骤见 `<USER_ACTIONS_DOC>`。在那之前，新建 issue 由帅手动
`gh project item-add` 灌入。

## 七、用户视角（只需要知道这四件）

1. **有想法**：对帅说一句话即可，帅代建单打标（或自己开 issue 随手写，帅补标）
2. **有空了**：打开置顶的 <PINNED_DECISION_ISSUE>（待拍板）/ <PINNED_USER_ISSUE>（需用户），评论区回复即生效
3. **想看全景**：打开 [<PROJECT_TITLE>](<PROJECT_URL>)，六列一眼看完哪些待办/在途/待验/待你拍
4. **想看细账**：<PROGRESS_FILE> 总账；任何 issue 点开都有三节点留痕

## 占位符对照表（派生后本节可删）

| 占位符 | 填什么 | 从哪来 |
|---|---|---|
| `<OWNER>` / `<REPO>` | 仓库归属与名字 | `gh repo view --json owner,name` |
| `<PROJECT_TITLE>` / `<PROJECT_URL>` | 看板标题与地址 | bootstrap 脚本输出 |
| `<PINNED_DECISION_ISSUE>` / `<PINNED_USER_ISSUE>` / `<PINNED_HUB_ISSUE>` / `<CANDIDATE_HUB_ISSUE>` | 四张常设单编号（写成 `#123`） | bootstrap 脚本输出 |
| `<REAL_MACHINE_CRITERIA>` / `<GUARD_PATH_CRITERIA>` / `<BLOCKED_EXAMPLES>` | 本项目判据 | 你自己填，填法示例见 labels.json 的 `placeholders` 段 |
| `<PROJECT_DISPATCH_LABELS>` | 本项目采用的派单约束扩展标签 | 通常是 `` `真机` `` 和/或 `` `守卫类` ``；一个都不采就删掉这一格 |
| `<PROJECT_VERDICT_ROW>` | 本项目特有的 triage 分流向（没有就删这一行） | 例：某类欠账需要独占资源，单独成一向 |
| `<PROJECT_CONSTRAINTS>` / `<PROJECT_ISSUE_STATE>` / `<PROJECT_NARRATIVE_FILES>` | 本项目特有约束 / 现用标签 / 叙事层文件清单 | 你自己填 |
| `<PROGRESS_FILE>` / `<USER_ACTIONS_DOC>` | 流水总账文件 / 用户动作清单文件 | 通常 `PROGRESS.md` / `docs/USER-ACTIONS.md` |

## 未尽处（派生后按本项目实况重写）

- 三节点留痕在**存量单**上的覆盖率通常很低（中枢是某一天才上线的，此前的单没有这个习惯）——
  写清楚，别让后来的人以为每张老单都有证据链。
- 「用户在总览单评论、帅心跳扫到」这条通道**在首次真实往返之前都只是设计**，别当已验证的能力写。
