# <项目名> · 项目铁律

<!-- canonical 真相源：windsurf-dao `ccswitch/templates/CLAUDE.md.template`。
     这是**新项目 CLAUDE.md 的骨架**，由 `/dao-project-scaffold --init` 零编辑复制到项目根后**就地填写**。
     文件名固定为 `CLAUDE.md`（文件名契约，见 `ccswitch/scaffold-manifest.json` 的 _doc）。
     刻意用 `.template` 后缀而非 `.md`：`*.md` 的各类 Glob 不该把骨架当成真文件扫进来。 -->

> 全局 dao 场域（`ccswitch/dao.md`）已由 `~/.claude/CLAUDE.md` 的 `@import` 每轮常驻——
> 语言规则、commit 前缀、八句根基**不在此重复**。本文件只写这个项目**独有**的增量。
> （Grep-first 不是常驻文字，由 G7 硬闸 + `permissions.deny` 承载，见 `ccswitch/rules/dao-shell-search.md`。）
> 预算 80 行（`claude-md-size` 闸），写满就该往 `.claude/rules/` 下沉。

## 项目类型（必答题 · 二选一，答完把本节的注释删掉）

本仓是**产品型项目**。

<!-- ⬆ 上面那一行是给机器读的**自我声明**，判据是纯子串匹配那五个字
     （唯一定义处：windsurf-dao `ccswitch/lib/scaffold-manifest.js` 的 `PRODUCT_TYPE_WHEN`）。

     · 有外部用户 / 要发布 / 有 UI 可见面  ⇒ **留着那一行**，把本注释删掉即可。
     · 内部工具、基建仓、一次性脚本仓      ⇒ **把那一行整行换成**：本仓是**内部工具型项目**。
       此后 SessionStart 不再查那一档共性 rule（PR 真机证据三态 / PR 模板 / issue 模板目录）。
     · 🔴 负向**原样抄那六个字，别自己造否定句**：判据是纯子串，而「不是…」「非…」这类
       自然写法里整段含着正向那五个字 ⇒ 机器会读成「是」。这一向没有检测器兜着。
     · 两个都写                           ⇒ 按「是」处理（正向优先，只看正向那串在不在）。
     · 一个都不写                         ⇒ 每次 SessionStart 提醒你这道题还没答
                                            （info 级、不阻塞；清单条目 product-type-answered）。

     **不许留着不答。** 这一行决定三条共性 rule 查不查。
     **2026-08-02 变更**：负向此前的处方是「把那一行删掉」，而删完之后「答了，不是」与
     「从没答过」在机器侧长得一模一样——那正是本体系一直在治的那个病（零检出 ≠ 零存在），
     也让这道题**永远送不到存量项目面前**（提醒谁？所有没写那五个字的仓里，绝大多数
     已经答过「不是」了）。现在两个答案各有各的串，才分得开。
     为什么由项目自己声明而不是 dao 替你判：「这算不算产品型」是语义判断，
     而本体系明令共性清单只收机器可判的判据。 -->

## 这是什么

<!-- 一段话：做什么的 / 给谁用 / 技术栈。新会话的 AI 第一眼读这里对齐，写不清就是没想清。 -->

## 铁律

<!-- 只写「换个项目就不成立」的项目特有约定。跨项目成立的规矩属 dao 场域，
     **别在这里抄一遍**——抄 = 双份真相源 = 必漂移。需要引用时写一行指针即可。 -->

1.
2.

## 验证

<!-- 一条命令跑全套的入口。还没有就写「暂无」，别写一串要人现场拼的命令；
     有入口的话把**退出码语义**写清楚（只有 0 叫通过，别让「没跑」与「跑了没事」长得一样）。 -->

**默认验证档位：成长期档**——测试绿即合，不含对抗前置。只有当某些文件的产出物本身会被别处
当判据/守卫/闸/拦截用（例如规则正文、lint/scan 脚本、CI 门禁本体）时，才对**那些文件**单独升
重档（对抗前置，独立上下文复核）；不是整个项目一律升档。重档判据与出处见 dao 侧分级方案
（windsurf-dao issue #70 拍板记录「验证分级 · 新项目不继承」，2026-08-09）。

```
（例：pwsh scripts/verify-all.ps1）
```

## 知识归位（写到哪）

| 知识类型 | 归属 |
|---|---|
| 项目特有铁律 | 本文件 |
| 领域规范（按 `paths:` frontmatter 条件加载） | `.claude/rules/*.md` |
| 跨项目原则 / 方法论 | windsurf-dao `ccswitch/dao.md`（**不要往本文件抄**） |
| 待办 · 候选池 · 欠账 | `TODO.md` 或 issue 区（判据见 dao.md「工作项三态归位」） |
| 流水账 / 发布物 | `PROGRESS.md` / `CHANGELOG.md` |
