/* 绿样本：spawn dispatch 带 --dry-run，预览一针都不许烧。
const { spawnSync } = require('node:child_process');
spawnSync(process.execPath, [
  'scripts/dao.mjs', 'dispatch', '--executor', 'mirasim',
  '--issue', '1', '--spec', '短摘要', '--name', 'x', '--dry-run',
], {
  encoding: 'utf8',
  env: { ...process.env },
});
*/
