/* 绿样本：动态 import 默认导出解构赋值带 --dry-run。
let cp;
({ default: cp } = await import("node:child_process"));
cp.spawnSync(process.execPath, ["scripts/dao.mjs", "dispatch", "--dry-run"], {
  env: { PATH: "/bin" }
});
*/
