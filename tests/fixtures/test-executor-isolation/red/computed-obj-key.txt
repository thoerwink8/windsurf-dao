/* 审官对抗样本：计算属性键 { ['run']: cp.spawnSync }。只认 run: 标识符键会 scanned:0。
const cp = require("node:child_process");
const box = { ['run']: cp.spawnSync };
box.run(process.execPath, argv, { env: { PATH: "/bin" } });
*/
