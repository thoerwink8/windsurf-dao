/* 审官对抗样本：先声明再赋值 require(child_process)。只收声明时 require 会 scanned:0。
let cp;
cp = require("node:child_process");
cp.spawnSync(process.execPath, ["scripts/dao.mjs", "dispatch"], {
  env: { PATH: "/bin" }
});
*/
