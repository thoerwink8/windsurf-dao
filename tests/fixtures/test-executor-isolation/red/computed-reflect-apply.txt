/* 审官对抗样本：Reflect["apply"]。只认 Reflect.apply( 会 scanned:0。
const cp = require("node:child_process");
Reflect["apply"](cp.spawnSync, null, [
  process.execPath, ["scripts/dao.mjs", "dispatch"], { env: { PATH: "/bin" }
}]);
*/
