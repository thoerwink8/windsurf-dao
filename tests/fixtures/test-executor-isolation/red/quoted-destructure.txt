/* 审官对抗样本：对象解构字符串键。只认 ident: alias，{ "exec": run } 会 scanned:0。
const cp = require("node:child_process");
const { "exec": run } = cp;
run(`node scripts/dao.mjs ${getVerb()}`, { env: { PATH: "/bin" } });
*/
