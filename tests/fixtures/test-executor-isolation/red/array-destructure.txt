/* 审官对抗样本：数组解构。const [run] = [cp.exec] 不是 ident = 赋值，会 scanned:0。
const cp = require("node:child_process");
const [run] = [cp.exec];
run(`node scripts/dao.mjs ${getVerb()}`, {
  env: { PATH: "/bin" }
});
*/