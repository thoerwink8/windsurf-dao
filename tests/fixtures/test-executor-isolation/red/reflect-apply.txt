/* 审官对抗样本：Reflect.apply(spawnSync)。只认 spawnSync( 会 scanned:0。
const cp = require("node:child_process");
Reflect.apply(cp.spawnSync, null, [process.execPath, ["scripts/dao.mjs", "dispatch"], {
  env: { PATH: "/bin" }
}]);
*/
