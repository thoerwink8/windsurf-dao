/* 审官对抗样本：spawnSync["call"]。只认 .call( 会 scanned:0。
const cp = require("node:child_process");
cp.spawnSync["call"](null, process.execPath,
  ["scripts/dao.mjs", "dispatch"], { env: { PATH: "/bin" } });
*/
