orca orchestration task-update

Usage: orca orchestration task-update --id <task_id> --status <status> [--result <json>] [--run <run_id>] [--from <handle>] [--json]

Update a task status

Options:
  --help                 Show this help message
  --json                 Emit machine-readable JSON
  --pairing-code
  --environment
  --id <id>             Identifier for a target item or permission
  --status
  --result
  --run
  --from
  --retry-request

Notes:
  Valid --status values: pending, ready, dispatched, completed, failed, blocked.
