# Windsurf-Dao 使用手册

> **3 分钟看完。看不下去直接跳到「最少要知道的 3 件事」。**
>
> 你不需要懂道德经，不需要记 27 个 skill。你只需要正常说话，AI 自己知道该怎么走。

---

## 目录

- [一 · 提需求三种姿势](#一--提需求三种姿势)
- [二 · AI 内部怎么运转](#二--ai-内部怎么运转)
- [三 · 三个 workflow 区别](#三--三个-workflow-区别)
- [四 · 你的主动开关](#四--你的主动开关)
- [五 · FAQ](#五--faq)
- [六 · 最少要知道的 3 件事](#六--最少要知道的-3-件事)

---

## 一 · 提需求三种姿势

### 姿势 1 · 直说（最常用）

**怎么用**：直接描述你想做什么，不加任何 dao 关键词。

```
"帮我修一下登录页的 typo"
"给 user model 加个 email 字段"
"重构这个组件让它支持 dark mode"
```

**AI 跳出来的样子**：
- 任务小（≤2 文件 / ≤30 LOC）→ 直接动手 + 验证 + 报告
- 任务大（≥3 文件 / ≥100 LOC / 核心模块）→ AI 主动说「这事满足 X 条件，建议走 superpowers，要不要？」→ 你点头才走仪式

**这是默认姿势，覆盖 80% 场景。**

### 姿势 2 · 显式说重（要质量保证）

**怎么用**：说出触发词。

```
"走 superpowers 改这个支付逻辑"
"开 worktree 走完整流程"
"派 reviewer 审一下我刚写的代码"
"走 dao-dev 从需求做到交付"
```

**AI 跳出来的样子**：不管任务多小，按你说的仪式走完整流程（worktree → plan → execute → review → finish 五步，或谋造成三阶九步）。

**适合**：核心模块（auth/payment/security）、不可逆操作（DB migration）、跨服务改动。

### 姿势 3 · 显式说轻（要快速）

**怎么用**：说出降级词。

```
"轻一点，别开 worktree"
"不走流程，直接改"
"先快速实现，review 后面再说"
```

**AI 跳出来的样子**：跳过仪式，直接执行（但 mantra 心怀仍在场，dao-cycle 内部默默旋转，不会真的"裸奔"）。

**适合**：原型验证、急火、deadline 前最后一改。

---

## 二 · AI 内部怎么运转

```
┌──────────────────────────────────────────────────────┐
│  你说一句话                                            │
└─────────────────────┬────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────┐
│  第 1 层 · 心怀（always_on，每条消息自动加载）          │
│                                                       │
│  • global_rules.md   跨项目元规则                     │
│  • dao-mantra.md     道德经八句根基 + 场景速查表        │
│  • execution.md      项目铁律                          │
│  • superpowers-gate.md  仪式触发判定                   │
└─────────────────────┬────────────────────────────────┘
                      │ 内化为 AI 的"心境"
                      ▼
┌──────────────────────────────────────────────────────┐
│  第 2 层 · 判定（gate 按你的话和任务规模决策）          │
│                                                       │
│  显式触发词？  → 走对应 workflow                      │
│  ≥3 文件？    → 主动建议 superpowers                  │
│  否则         → 直接动手                               │
└─────────────────────┬────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────┐
│  第 3 层 · 落地（按需加载 workflow + skill）            │
│                                                       │
│  workflow：/dao-cycle / /dao-superpowers / /dao-dev   │
│  skill 镜头：dao-debug / dao-refactor / dao-test ...  │
│             （27 个 skill，按场景自动加载）             │
└──────────────────────────────────────────────────────┘
```

**简化记忆**：心怀 → 判定 → 落地。三层都在每条消息里同时运行。

---

## 三 · 三个 workflow 区别

这是最容易混的地方。一句话区分：

| workflow | 维度 | 一句话 | 触发方式 |
|---|---|---|---|
| `/dao-cycle` | 怎么**想** | 思考节奏：观→行→验→省→改升 | 默认静默 / 显式 `/cycle` |
| `/dao-superpowers` | 怎么**交付** | 工程仪式：worktree→plan→exec→review→finish | 显式 `/dao-superpowers` 或 gate 触发 |
| `/dao-dev` | 怎么**从 0 做出** | 产品管线：谋→造→成（三阶九步） | 显式 `/dao-dev` |

**它们不是互斥，是嵌套**：

```
/dao-dev (产品管线)
  └─ 第二阶「造」
      └─ /dao-superpowers (工程仪式)
          └─ 第 3 步 execute
              └─ /dao-cycle (思考节奏)
                  └─ 每个改动内部观行验省改升
```

**实际中你只需选最外层**：
- 单功能改动 → 不显式选，默认走 → AI 自己加 cycle
- 重要模块 → 选 `/dao-superpowers`
- 全新功能从 0 到 1 → 选 `/dao-dev`

其他 27 个 skill 是**镜头**，不是 workflow。AI 按场景自动加载（如调 bug 加载 dao-debug，重构加载 dao-refactor），不需要你显式调用。

---

## 四 · 你的主动开关

### 触发开关（让 AI 走重）

| 你说 | 效果 |
|---|---|
| 「走 superpowers」「开 worktree」「走完整流程」 | 强制走五步仪式 |
| 「走 dao-dev」「从头做」 | 走三阶九步管线 |
| 「走 cycle」「显式跑五相」 | 思考过程可见输出 |
| 「派 reviewer」「自审一下」 | 加 reviewer 自审环节 |
| 「@dao-philosophy」 | 加载道德经八条不变原则深度反思 |

### 降级开关（让 AI 走轻）

| 你说 | 效果 |
|---|---|
| 「轻一点」「快一点」 | 跳过 worktree 和 plan |
| 「不走流程」「直接改」 | 仅做最小动作 |
| 「跳过 review」 | 不开 reviewer subagent（不推荐） |
| 「退出 superpowers」 | 主动解除 superpowers 模式 |

### 中止开关

| 你说 | 效果 |
|---|---|
| 「停」「中止」「不做了」 | 立即停手 + 报告当前状态 + 提供回滚选项 |
| 「cleanup worktree」 | 删掉所有相关 worktree 和分支 |
| 「rollback」 | 撤回最近改动 |

---

## 五 · FAQ

### Q1 · 我什么时候应该说「走 superpowers」？

满足以下任一即建议显式走：
- 改 auth / payment / security 类核心代码
- 涉及 DB schema 变更
- 涉及多服务 / 多端共享逻辑
- 你需要 plan 留档（团队协作 / 审计）
- 你想要"撤回成本接近零"的安心感

### Q2 · AI 会不会在我没说的情况下自动走 superpowers？

**不会自动走，只会主动建议。**

AI 检测到任务复杂度满足阈值（≥3 文件 / ≥100 LOC / 核心模块）时会问：「这事满足 X 条件，建议走 superpowers，要不要？」你点头才走，你拒绝就走轻量路径。

这是 dao-mantra 第 5 句「**用户无为而 AI 无不为**」——AI 不替你拍板仪式重量。

### Q3 · /dao-cycle 我什么时候用得到？

99% 时候**用不到显式调用**，因为它默认静默运行。

显式调用 `/cycle` 的场景：
- 调试一个棘手的 bug，希望看 AI 思考过程
- 重构代码，希望多转几次（默认 1.5 转，cycle 模式 2-3 转）
- 复盘一个失败的设计

### Q4 · 我说「走 superpowers」之后能反悔吗？

可以。任何时候说「退出 superpowers」「不走流程了」即解除。已建的 worktree 让 AI 帮你 cleanup（说「cleanup」即可）。

### Q5 · 27 个 skill 我都要记吗？

**不要。** AI 按场景自动加载，你不需要知道它们的名字。

如果好奇可以看 `.windsurf/skills/` 目录，但日常使用不需要。

### Q6 · /dao-dev 和 /dao-superpowers 同时用吗？

可以嵌套：`/dao-dev` 第二阶「造」可以走 `/dao-superpowers` 仪式。但通常两者选一个最外层即可。

经验法则：
- 已有项目改东西 → `/dao-superpowers`
- 全新功能从需求做到交付 → `/dao-dev`

### Q7 · 我看到 AI 报「dao-meta 三关过审」是什么意思？

dao-meta 是 dao 体系自我守卫机制，改 `.windsurf/rules/**` 等核心文件时自动触发，确保改动满足：
1. 通用性（不是项目特殊巫态）
2. 内容边界（不与现有规则冲突）
3. 影响评估（其他文件是否需协同）

普通业务代码不会触发 dao-meta，只在改 dao 体系自身时出现。

### Q8 · AI 说「announce at start」是什么？

每个 superpowers 步骤开始时 AI 会明示「我现在在第 X 步，做什么，为什么」。这是仪式透明度，让你随时知道 AI 在哪个阶段，可以中途中止。

---

## 六 · 最少要知道的 3 件事

如果懒得读上面所有内容，记住这三条就够了：

### 1 · 直接说需求

不需要任何 dao 关键词。AI 会自己判断走多重。

### 2 · 大事 AI 会主动建议升级

涉及核心模块 / 多文件改动时，AI 会问「要不要走 superpowers」——你点头才走。

### 3 · 你随时能踩刹车

说「轻一点」「停」「不走流程」「cleanup」即可。AI 不会替你拍板仪式重量。

---

## 参考文档

想深入了解 dao 体系内部机制：

- 体系架构：[README.md](README.md)
- AI 自身指南：[AGENT_GUIDE.md](AGENT_GUIDE.md)
- 规则总览：[`.windsurf/rules/README.md`](.windsurf/rules/README.md)
- 道德经原文：[`references/道德经.md`](references/道德经.md)
- 演化记录：[`data/evolution-entries.csv`](data/evolution-entries.csv)

---

> 道法自然。最好的使用手册是你不需要看使用手册。
