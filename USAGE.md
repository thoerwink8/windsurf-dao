# Windsurf-Dao 使用手册

> ⚠️ **口径说明（2026-07-07）**：Windsurf 侧已于 2026-06-29 退役，本手册按**单栈 Claude Code** 口径维护；
> 文中涉及分层加载的段落已对齐现状（dao.md always_on + 9 skills + 10 commands）。
>
> **3 分钟看完。看不下去直接跳到「最少要知道的 3 件事」。**
>
> 你不需要懂道德经，不需要记 9 个 skill。你只需要正常说话，AI 自己知道该怎么走。

---

## 启用（一次性）

**Claude Code**：跑 `dao.ps1 link-claude`，然后**重启会话**（或 `/clear`）让新 skills/commands/agents 生效。详见 [README · 快速开始](README.md#快速开始)。

启用之后你正常说话就行——dao.md 经 `~/.claude/CLAUDE.md` 的 `@import` 每条消息常驻。

---

## 目录

- [一 · 提需求三种姿势](#一--提需求三种姿势)
- [二 · AI 内部怎么运转](#二--ai-内部怎么运转)
- [三 · 三个 workflow 区别](#三--三个-workflow-区别)
- [四 · 你的主动开关](#四--你的主动开关)
- [五 · FAQ](#五--faq)
- [六 · 最少要知道的 3 件事](#六--最少要知道的-3-件事)

---

## 一 · 提需求三种姿势

### 姿势 1 · 直说（最常用）

**怎么用**：直接描述你想做什么，不加任何 dao 关键词。

```
"帮我修一下登录页的 typo"
"给 user model 加个 email 字段"
"重构这个组件让它支持 dark mode"
```

**AI 跳出来的样子**：
- 任务小（≤2 文件 / ≤30 LOC）→ 直接动手 + 验证 + 报告
- 任务大（≥3 文件 / ≥100 LOC / 核心模块）→ AI 主动说「这事满足 X 条件，建议走 superpowers，要不要？」→ 你点头才走仪式

**这是默认姿势，覆盖 80% 场景。**

### 姿势 2 · 显式说重（要质量保证）

**怎么用**：说出触发词。

```
"走 superpowers 改这个支付逻辑"
"开 worktree 走完整流程"
"派 reviewer 审一下我刚写的代码"
"走 dao-dev 从需求做到交付"
```

**AI 跳出来的样子**：不管任务多小，按你说的仪式走完整流程（worktree → plan → execute → review → finish 五步，或谋造成三阶九步）。

**适合**：核心模块（auth/payment/security）、不可逆操作（DB migration）、跨服务改动。

### 姿势 3 · 显式说轻（要快速）

**怎么用**：说出降级词。

```
"轻一点，别开 worktree"
"不走流程，直接改"
"先快速实现，review 后面再说"
```

**AI 跳出来的样子**：跳过仪式，直接执行（但 mantra 心怀仍在场，深度迭代内部默默旋转，不会真的"裸奔"）。

**适合**：原型验证、急火、deadline 前最后一改。

---

## 二 · AI 内部怎么运转

```
┌──────────────────────────────────────────────────────┐
│  你说一句话                                            │
└─────────────────────┬────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────┐
│  第 1 层 · 心怀（always_on，每条消息自动加载）          │
│                                                       │
│  • ccswitch/dao.md   场域根基：八句根基 + 三才之机     │
│    + 续力 + 三管线门控 + 场景速查（@import 常驻）      │
└─────────────────────┬────────────────────────────────┘
                      │ 内化为 AI 的"心境"
                      ▼
┌──────────────────────────────────────────────────────┐
│  第 2 层 · 判定（按你的话和任务规模决策）               │
│                                                       │
│  显式触发词？  → 走对应管线                            │
│  ≥3 文件？    → 主动建议 superpowers                  │
│  否则         → 直接动手                               │
└─────────────────────┬────────────────────────────────┘
                      │
                      ▼
┌──────────────────────────────────────────────────────┐
│  第 3 层 · 落地（用户 /命令 手动触发 skill）            │
│                                                       │
│  管线：/dao-dev · /dao-loop · /dao-superpowers        │
│  skill：dao-brainstorm / dao-plan / dao-design ...    │
│         （9 个 skill，全部 /name 手动触发）             │
└──────────────────────────────────────────────────────┘
```

**简化记忆**：心怀 → 判定 → 落地。三层都在每条消息里同时运行。

---

## 三 · 两个 workflow 区别

一句话区分：

| workflow | 维度 | 一句话 | 触发方式 |
|---|---|---|---|
| `/dao-dev` | 怎么**从 0 做出** | 产品管线：谋→造→成（三阶九步），内置深度迭代引擎（§2.5 五相：观→行→验→省→改升） | 显式 `/dao-dev` |
| `/dao-superpowers` | 怎么**交付** | 工程仪式：worktree→plan→exec→review→finish | 显式 `/dao-superpowers` 或 gate 触发 |

> **3 分钟入门用户看到这里就够了。下面 3.1-3.4 是深入说明。**

### 3.1 决策树（最实用）

```
你心里要解决的是什么？
│
├─ 🆕 从无到有做一个功能/系统 ─────────────── → /dao-dev
│    典型："做个登录系统" / "搞个 MVP" / "从需求到上线"
│
├─ 🔧 解决一个具体问题 ──────────────────── → 直接说，AI 自动用深度迭代
│    ├─ bug               → dao-debug 镜头
│    ├─ 重构              → dao-refactor 镜头
│    ├─ 性能              → dao-optimize 镜头
│    └─ 不知道哪类        → AI 自动判断
│
├─ 🔀 小改动/维护 ─────────────────────── → 直接说（不显式调 workflow）
│    典型："改 typo" / "加个字段" / "调样式"
│
└─ 🤷 不确定 ──────────────────────────── → 直接说需求，AI 自己判断
```

### 3.2 具体场景对照

| 你说 | AI 跳什么 | 为什么 |
|---|---|---|
| "做个用户登录系统" | `/dao-dev` | 从 0 到 1 的完整功能 |
| "修复登录失败的 bug" | 深度迭代 + dao-debug | 具体问题深挖 |
| "写个 MVP 从需求到部署" | `/dao-dev` | 典型 dev 场景 |
| "home 页加载太慢" | 深度迭代 + dao-optimize | 具体问题 |
| "重构整个支付模块" | 深度迭代 2-3 转 + dao-refactor | 深度重构多转螺旋 |

### 3.3 嵌套关系

`/dao-dev` 内置了深度迭代引擎（§2.5）。当模块改动复杂时自动启动五相循环：

```
/dao-dev（整个功能生命周期）
 ├─ 一 · 谋（析 + 设）           ← 一句话 → 图纸
 │   🔒 用户确认方向
 ├─ 二 · 造（编 + 筑 + 部）       ← 图纸 → 实体
 │   │   └─ 编码某模块遇阻
 │   │       └─ §2.5 深度迭代（观→行→验→省→改升）
 │   🔒 用户预览
 └─ 三 · 成（试 + 验 + 书）       ← 实体 → 交付
     🔒 用户验收
```

`/dao-superpowers` 同理嵌入：dev 第二阶「造」如果是核心模块，可以走 superpowers 五步仪式包装：

```
/dao-dev (产品管线，最外层)
  └─ 第二阶「造」
      └─ /dao-superpowers (工程仪式，中间层)
          └─ 第 3 步 execute
              └─ §2.5 深度迭代 (思考引擎，最内层)
```

### 3.4 最简规则

```
新功能从 0 到 1   →  /dao-dev
具体问题深挖      →  直接说（AI 自动深度迭代）
小改动维护        →  直接说
```

**99% 时候你只需「直接说」**，AI 自己判断上哪个层级。显式调用 workflow 通常发生在你想要**高仪式感**（走 dev 的 3 个关卡）时。

其他 skill 用 `/name` 手动触发（如 `/dao-verify` 全面体检、`/dao-design` 设计入口）——但你不用背：正常描述需求，AI 会在合适时机给出交接提示「请输入 /dao-xxx」。调 bug 类横切能力走 subagent（dao-debugger），AI 自动派活。

---

## 四 · 你的主动开关

### 触发开关（让 AI 走重）

| 你说 | 效果 |
|---|---|
| 「走 superpowers」「开 worktree」「走完整流程」 | 强制走五步仪式 |
| 「走 dao-dev」「从头做」 | 走三阶九步管线 |
| 「深度迭代」「跑五相」 | 思考过程可见输出（dao-dev §2.5） |
| 「派 reviewer」「自审一下」 | 加 reviewer 自审环节 |
| 深度经文反思 | 直接让 AI Read `~/.claude/references/` 下的道德经（王弼本，人格注入同源）/阴符经全文；要与帛书本对读再点名 `帛书老子.md`（旧 @dao-philosophy 已随双栈退役） |

### 降级开关（让 AI 走轻）

| 你说 | 效果 |
|---|---|
| 「轻一点」「快一点」 | 跳过 worktree 和 plan |
| 「不走流程」「直接改」 | 仅做最小动作 |
| 「跳过 review」 | 不开 reviewer subagent（不推荐） |
| 「退出 superpowers」 | 主动解除 superpowers 模式 |

### 中止开关

| 你说 | 效果 |
|---|---|
| 「停」「中止」「不做了」 | 立即停手 + 报告当前状态 + 提供回滚选项 |
| 「cleanup worktree」 | 删掉所有相关 worktree 和分支 |
| 「rollback」 | 撤回最近改动 |

---

## 五 · FAQ

### Q1 · 我什么时候应该说「走 superpowers」？

满足以下任一即建议显式走：
- 改 auth / payment / security 类核心代码
- 涉及 DB schema 变更
- 涉及多服务 / 多端共享逻辑
- 你需要 plan 留档（团队协作 / 审计）
- 你想要"撤回成本接近零"的安心感

### Q2 · AI 会不会在我没说的情况下自动走 superpowers？

**不会自动走，只会主动建议。**

AI 检测到任务复杂度满足阈值（≥3 文件 / ≥100 LOC / 核心模块）时会问：「这事满足 X 条件，建议走 superpowers，要不要？」你点头才走，你拒绝就走轻量路径。

这是八句根基第 5 句「**道常无为而无不为**」（用户无为，AI 无不为）——AI 不替你拍板仪式重量。

### Q3 · 深度迭代（五相循环）什么时候会启动？

AI 在编码遇阻时自动启动（跨 ≥3 文件 / 尝试失败 / 复杂算法 / 不确定）。你也可以说「深度迭代」「跑五相」显式触发——用于调试棘手 bug、希望看思考过程、或深度重构多转时。

### Q4 · 我说「走 superpowers」之后能反悔吗？

可以。任何时候说「退出 superpowers」「不走流程了」即解除。已建的 worktree 让 AI 帮你 cleanup（说「cleanup」即可）。

### Q5 · 9 个 skill 我都要记吗？

**不要。** dao.md 场景速查表会把你的话路由到对应 `/命令`，你不需要背名字。

如果好奇可以看 `ccswitch/skills/` 目录，但日常使用不需要。

### Q6 · /dao-dev 和 /dao-superpowers 同时用吗？

可以嵌套：`/dao-dev` 第二阶「造」可以走 `/dao-superpowers` 仪式。但通常两者选一个最外层即可。

经验法则：
- 已有项目改东西 → `/dao-superpowers`
- 全新功能从需求做到交付 → `/dao-dev`

### Q7 · 我看到 AI 报「dao-meta 三关过审」是什么意思？

dao-meta 是 dao 体系自我守卫机制，改 `ccswitch/` 下核心文件（dao.md / skills / commands）时自动触发，确保改动满足：
1. 通用性（不是项目特殊巫态）
2. 内容边界（不与现有规则冲突）
3. 影响评估（其他文件是否需协同）

普通业务代码不会触发 dao-meta，只在改 dao 体系自身时出现。

### Q8 · AI 说「announce at start」是什么？

每个 superpowers 步骤开始时 AI 会明示「我现在在第 X 步，做什么，为什么」。这是仪式透明度，让你随时知道 AI 在哪个阶段，可以中途中止。

---

## 六 · 最少要知道的 3 件事

如果懒得读上面所有内容，记住这三条就够了：

### 1 · 直接说需求

不需要任何 dao 关键词。AI 会自己判断走多重。

### 2 · 大事 AI 会主动建议升级

涉及核心模块 / 多文件改动时，AI 会问「要不要走 superpowers」——你点头才走。

### 3 · 你随时能踩刹车

说「轻一点」「停」「不走流程」「cleanup」即可。AI 不会替你拍板仪式重量。

---

## 工具速览

| 工具 | 干什么 | 用法 |
|---|---|---|
| `dao.ps1` | 链接管理（部署 dao 到各宿主） | `.\dao.ps1 status` / `link-claude` / `link-codex-prompts` 等 |
| `dao.bat` | 配置同步（四合一） | `dao.bat export\|restore\|doctor\|inventory` |

完整命令见 [README · dao.ps1 命令一览](README.md#daops1-命令一览)。
配置同步详情见 [`config-sync/README.md`](config-sync/README.md)。

## 参考文档

- 体系架构：[README.md](README.md)
- 部署/迁移：[MIGRATION.md](MIGRATION.md)
- 新机器复刻：[NEW-MACHINE.md](NEW-MACHINE.md)
- AI 自身指南：[AGENT_GUIDE.md](AGENT_GUIDE.md)
- 规则总览：[`ccswitch/dao.md`](ccswitch/dao.md)（场域元规则 + skill 正文内嵌行为规则）
- 道德经原文：[`docs/classics/道德经.md`](docs/classics/道德经.md)

---

> 道法自然。最好的使用手册是你不需要看使用手册。
