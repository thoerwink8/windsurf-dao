# [pi] 看门狗收口：#452 复核 4 条收口建议落地（issue #454）

## 目标

#452 复核 review 末尾留档的 4 条不阻塞合并的收口建议逐条落地（复核官已用变异测试证明 ① 的 hash-stable-restart 用例无判别力）：

1. **hash-stable-restart 造出判别力**：现样本第 3 轮 incarnation 变的同时屏面也换，「屏面变」就能解释「第 5 轮才报」，去掉 epoch 里的 incarnation 重跑仍全绿。改造为「incarnation 变、屏面不变」的同 pane 重启样本（重启轮重新起算，不在第 3 轮报）；完工后自己跑同一变异验证：把 incarnation 逻辑改坏，测试必须红。
2. **live 分支补自动化覆盖**：read 结果规整抽成共用函数（normalizeReadResponse），快照样本打在 live 用的同一段逻辑上（现 read-malformed 只走快照解析分支，`!r.ok || !t` 的 live 路径只有肉眼 + live 实跑背书）。
3. **fixture README 改写清单补全**：at-capacity / read-error 的 ps.json 相对底稿（ps-0815.json）各改了**两处** state（#452 codex done→working + 同 worktree pi working→done 隔离），README 只记了一处；at-capacity read 的 returnedLineCount=146 与 tail=22 行不自洽，改为自洽并记录。
4. **README 写死测试命令**：`node --test tests/` 在 Node 24 报 MODULE_NOT_FOUND（首审、复核都复现），写死真实入口 `node tests/watchdog.tests.js` / `node scripts/dao-check.mjs`。

## 验收标准

- [ ] ① hash-stable-restart：5 轮同屏、第 3 轮 incarnation 变（同 pane 重启）→ 第 5 轮才报，第 3/4 轮必须 OK；变异测试（epoch 去掉 incarnation）→ 测试红
- [ ] ② normalizeReadResponse 共用：快照 read-malformed（成功缺 result.terminal）与 read-error（ok:false 原始错误）样本打在 live 同一段规整逻辑
- [ ] ③ fixture README 改写清单与文件实际改动逐字段对上（at-capacity / read-error 各两处 state；read returnedLineCount 与 tail 自洽）
- [ ] ④ README 看门狗条目写死两条测试命令 + 注明勿敲 `node --test tests/`
- [ ] `node tests/watchdog.tests.js` 全过；`node scripts/dao-check.mjs` 全绿
- [ ] （注：任务书写的 `node --test tests/` 在本机 Node v24.13.1 下必然 MODULE_NOT_FOUND——这正是 ④ 要消灭的坑；以真实入口为准）

## 进展

- [ ] 待开工（draft）

## 合并时应关闭的 issue 清单（合并者核对后关）

- #454 本任务卡 → 本 PR 落地，合并时关
