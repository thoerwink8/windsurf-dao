# dao.md 条款论证与订正史档

> **这个档承载什么**：`ccswitch/dao.md` 是 always-on 每轮注入面——它每多一个字，用户每一轮对话都付一次钱。
> 故 dao.md 正文的目标形态是 **判据句 + 载体/触发点 + `[元字段]` + 指针**，
> 而「**为什么这条是对的**」「**当初是怎么错的**」「**这个数字是怎么算出来的**」这三类内容
> 一律住在这里——它们只在**有人质疑某条条款**时才需要读，不需要每轮注入。
>
> **与 `incident-narratives-202607.md` 的分工**：那边是**事故叙事**（N1-N11，一次事故一条编号，
> dao.md 与 dispatch-clauses 双向引用）；这边是**条款的论证与订正史**，按 dao.md 的节组织
> （`§帅-n` / `§反-n` / `§归-n` / `§动-n` / `§Shell-n`），dao.md 正文里的指针形如「见 rationales §帅-1」。
> 两边都不是第二真相源：**判据的真相源永远是 dao.md 正文**，这里只放被搬出来的论证与经过。
>
> **本档不参与 always-on 注入，写多长都不花用户的每轮 token。** 故删减时优先删 dao.md 正文、
> 把原文搬来这里，而不是直接销毁——「为道日损」损的是注入面，不是记忆。

---

## §帅-1 · 档位实证调整：升档/降档两侧的实测数字

dao.md 帅节「档位实证调整」只留「任何档位变更都是试点+记质量对比」这条判据，两侧实测搬来这里：

**升档实证（2026-07-26 用户拍板尉档 Sonnet→Opus）**：同窗 7 官降档试点显示 Sonnet 整体合格且多次超出要求，唯一缺陷是目标压力下越权压缩判据；升档收益在**大上下文**（同窗三官实测单任务 380-434K token，读半仓再动刀的手术会撑爆小窗）与权衡质量。

**过载降级路径的出处**：同窗实证：Opus 档两遭 529 零落刀，降档后一次交付。

**workflow/agentType 那条的实测**：实测：dao 审计舰队用 `agentType:'Explore'` 未传 model，整队跑成 4.8 而非当前代 opus-5。

**限流类死亡重派判据的两句解释**（正文只留判据，理由在此）：每一次成功输出 = 池子可用性的实时探针。写「等 X 小时后重置再派」那是把死亡时刻的预告当成了重派时刻的实况。

## §帅-3 · 工具面门：为什么它「一路跑到最后才失败」

dao.md 正文只留「没跑的闸与过了的闸在台账上长得一样」这一句，展开在此：

官只能交静态读码，工时全额计费，而那道被规定「必须过」的闸**看起来过了、其实从没跑起来**——正是零检出/零存在那个病的编排层版本。

## §帅-4 · 「写入面归谁」：为什么理由是实验前提而不是礼貌

原正文逐字留档（dao.md 现留「射程判据 + 两句判据一句话 + 派单令那一行」）：

> 不是所有测量都需要这一行，需要的是**结论会被「期间发生过什么」影响的那一类**……理由不是礼貌，是**实验前提**：测量类任务的结论建立在「自变量唯一」上，而**指挥官继续写文档就是往它的自变量里掺东西**——帅的写入既不在对照组也不在实验组，官只会把它归因到自己的动作上，于是产出一个**看起来有实据的假阳性**。

另一半射程（不必写的那类）：「此刻这个值是多少」——**别人写文件影响不了它**。

**本条的出处正是「事后才想起」**：帅派完真机官后又改了 3 次文档，靠自曝+发消息作废那段观测才补回来。

## §帅-5 · 热重载型验证：「冻结 main」被违反 ≥3 次的经过

原正文（含被搬走的加粗小标题，逐字留档）：

> **实测反复失败的路径**：为此「冻结 main 推送」是**纪律型**约束，同窗被违反 ≥3 次（帅自己推 docs 两次、实现官瞬时在主仓编辑一次），每次都「无害地」污染了正在跑的真机走查（其中一次把未提交码编进了被测构建，使整批真机观测作废需重跑）。

## §帅-8 · 本轮瘦身顺带搬出来的零碎（各条正文仍在 dao.md）

- 热重载条的共享面举例：主仓树是共享的——帅会往里推文档、实现官的默认 cwd 就是主仓（创建自己的 worktree 前若先编辑，就污染了）。
- 验证资源独占条的处方补语：不要让每一路各自去发现自己抢不到然后各自挂账。
- OD 面板快照刷新条的收尾语：让用户在 OD 永远看到与仓库一致的稿。

## §帅-6 · 军衔四级制：校尉同模型时为什么不塌缩档位

协议差异是真实的（同一模型下 critical 底座与常规底座产出性质不同），故不塌缩档位。

## §帅-7 · 「判定 subagent 越权前先 Grep 它自己那份记录」的出处（帅位重伤全过程）

原条款正文（2026-07-30 立、2026-08-01 路径订正）曾嵌在长窗节 ④.5 里，本次瘦身把判据提为帅节独立条款、经过留档于此：

🔴 **这一条还带出一次帅位重伤，一并记死（2026-07-30 同窗实犯）**：改这条 dao 的是一位 dogfood subagent，**它有用户直接授权**（用户原话末句「你也修改下 dao 吧」）。而帅**没收到那条消息**（用户是直接发给那个 subagent 的），于是帅：①判它「越权改全局规则」②判它「伪造用户原话」③**回滚了用户授权的改动**④当着用户的面公开指控它伪造。**四步全错，而错因只有一个**：把「**我的上下文里没有这条消息**」当成了「**用户没说过这句话**」。⇒ 那正是 L31 那一族最贵的一次实例——**「我没看到」诚实回答的是「我没收到」，不是「它不存在」**；而 subagent 的上下文与帅的上下文**本来就不是同一份**，用户可以直接对它说话。

原条款的处方段逐字留档（dao.md 正文现以判据形式保留同一处方）：

> **处方（挂在必经动作上）**：判定 subagent「越权」之前，**先 Grep 它自己那份记录找授权来源**——路径 `~/.claude/projects/<slug>/<session-uuid>/subagents/agent-<agentId>.jsonl`（有界小窗口 `-o` + `head_limit`；`<slug>` 推导与取数工艺见 `ccswitch/workflows/README.md` §transcript 一路的取数路径）。**用户中途插话有固定签名 `The user sent a new message while you were working`，Grep 它即可定位。** ⚠️ **别去 Grep 顶层 `<session-uuid>.jsonl`**：那里装的是 subagent 交回的**报告原文**，用报告证明报告等于没证；用户直接对它说的那句话只在子代理自己的记录里。**在找到之前不许回滚、更不许公开指控。**

**2026-08-01 路径订正**：原写 `tasks/<agentId>.output`——它不是不存在，而是**对正常结束的 agent 恒 0 字节** ⇒ Grep 恒不命中，失效方向 fail-safe 但处方恒静默。用本条自己那次事故的原话双向回测：新路径 5 命中、旧路径 1 命中。

## §反-1 · 照做档/判断档「祈使句测法」的两向反例

**这是近似，两向都有反例**：措辞机械但内里是取舍的（「验到足够即可」换成「验到 3 组即可」仍是替用户定量），以及措辞含糊但其实只有一种执行方式的。

## §反-2 · 预算型护栏：为什么「越是纪律好的仓库越容易撞上它」

原正文逐字留档（dao.md 正文只留「它只在两边都合规时才发生」+「越是纪律好的仓库越容易撞上它」两句）：

> **这个失效模式最难受的性质是：它只在两边都合规时才发生**——任何一侧单独越线都会被当场拦下，恰恰是「两边各自都守规矩」的情况才能悄悄把和越过线，所以**越是纪律好的仓库越容易撞上它**，而事后看每一条提交都是清白的。

## §反-3 · `git fetch` 静默失败的实测形态

并发合并期间每合一次就旧一次。实测网络错误后 fetch 报错、后续 `merge origin/main` 照跑并报 `Already up to date`，**又一个语法绿**。

## §归-1 · 根路径消歧：两仓 `docs/` 互补的逐处实测

`evolution/` `playbooks/` `specs/` 只在 windsurf-dao，`rules/` `ops/` `POSITIONING.md` 只在项目侧 ⇒ 同一个前缀在不同句子里指两个仓，而**下文确有一句里同时出现两仓的 `docs/`**（支柱工作法那句：`POSITIONING.md` 是项目侧、`playbooks/pillar-recon/` 是 dao 侧，同一前缀同一句指两个仓）。

## §归-2 · dao-first 次序的实证，与两条清单配套原则的正文

**dao-first 的实证**：本窗先在 mousse 建 PR 模板 + issue 中心，再固化 dao 原则，结果模板/标签/看板全是 mousse 专属、新项目一个都拿不到。dao-first 则让 canonical 住在共享仓、任何项目从它取。

**共性 rule 备案的两条配套原则**（真相源在 `ccswitch/scaffold-manifest.json` 的 `_doc` 段，此处是被 dao.md 正文搬出来的转述）：**①文件名契约**——共性 rule 必须由约定文件名承载，内容可项目化、文件名不可，否则每次「缺项」报告都可争议；**②例外显式化 > 例外隐形**——例外逐条写 `exempt: [{repo, why}]`，元仓库不再整体豁免（原状是检查从不跑到立法者头上，于是没人发现它自己违规）。

**STAR/折叠证据条被搬出来的两处括注**（逐字留档；dao.md 正文保留三条硬要求与判据句）：

> ③**技术证据进 `<details>` 折叠段**：精度数字一个字不删、折叠起来，**人读顶部、AI 读全部、零丢失**（AI 本就读全文，折叠不伤它；不写人话才伤人）。判据：**没参与项目的人扫一眼顶部知不知道发生了什么。** 落地：PR 用 `.github/pull_request_template.md`（分层内建），issue 模板同理；这是「说人话」原则（本就贯穿续力选项/AskUserQuestion）在**人读载体**上的延伸，不是新原则。

另一处同源表述（工作项三态归位节的 issue 中心条）：**为什么不伤 AI**：AI 本来就读全文，折叠不影响它；受益的是人不用在密文里捞。

## §动-1 · 「③ 打磨自曝」的两个实证

2026-07-26 实证——刀F 主动否认自己的收益（「实码净变化为零，真收益是一处修全处到」）、清尾官自曝两处自伤并点名要复审官重点看某个 diff，这些自曝比成果本身更值钱，此前全靠个别官自发。

## §动-2 · windows-mcp 弃用理由（四条）

切换窗口焦点污染被测状态、全屏截图含任务栏、无 DOM 访问、位图证据不如 DOM 文本可复核且更烧 token。

## §Shell-1 · PR 合并链三条边界的成因

**①「事后提醒不是守卫」的机制**：PostToolUse 在命令跑完之后才触发。

**③「`--delete-branch` 的沉默不可信」的机制**：本地分支被 worktree 占用时它整体失败、错误只提本地 ⇒ 远程也没删。

## §Shell-2 · patch-id 复核：那次「疑似遗失工作」的实况

**`--is-ancestor` 为什么失明**：本例正是：孤儿 commit 与 main 上某个 commit 作者、时间戳、message 逐字相同，`--is-ancestor` 照样判「未并入」。

**为什么「开 PR 反而有害」**：本例若照令开 PR，其相对当前 main 的差异恰好是一个 release commit 的反向，等于提议回滚该版本与随行的六笔账。

**附一条第一人称**：立条款的人（我）当场就踩了。

## §瘦身-批3 · dao.md 重写批 3（2026-08-02）删掉的正文

> **这一节是「删了什么」的账**，不是新判据。dao.md 从 59,714 B 压到 10 KB 硬闸以内时，
> 下面这些段落被整段移出正文——每一段都标着它在 dao.md 里的原位置与现在的去向。
> **判据零丢失是本节的验收标准**：凡仍然是判据的，dao.md 正文里都留着压缩句；
> 这里放的是**论证、词表、编辑史与出处**。

### 一、编辑史（纯粹的「这一节是怎么变成现在这样的」，删除不损判据）

- **八句根基节末的括注**（原 dao.md L22）：「本节只放这八句。原先塞在这里的『产出归位提醒』
  『项目规范自动沉淀』两条属归属判据，已归『知识归位 · 写到哪』节。」
- **器 · 命令表节头的括注**（原 L191-192）：「本节自 2026-08-01 起独立成节。在此之前它整块挂在
  『品 · 产品之思』节底下——命令表与产品思考法毫无关系，是历史编辑掉进去的，按节找命令表根本
  找不到。本次纯搬动：正文一个字没改，只补了这个标题。」
- **谋节四步骤降级说明**（原 L151）：「四个步骤的详细铁律是 AI 内部读取件，不是用户命令
  （2026-07-27 用户拍板降级）：`ccswitch/skills/dao-{worktree,plan,review,brainstorm}/SKILL.md`
  仍在原地、仍由 `dao-superpowers` / `dao-loop` / 各 subagent 人格按路径 Read
  （`dao-plan-writer` / `dao-reviewer` 的方法论真相源就指着它们），只是**不再部署为 `/` 命令**
  ——四者在全部用户键盘记录里零调用，把它们摆在用户命令表上只是噪音。」
- **帅位模型无关的出处**（原 L159）：「帅强将轻是官方实证：Opus lead + Sonnet workers 胜纯 Opus
  单兵 90.2%（anthropic.com/engineering/multi-agent-research-system）」——判据「谁坐主会话谁为帅
  （模型无关，随代滚动）」仍在 dao.md。
- **桌面端基建自检**（原 L61）：「已并入共性 rule 备案清单，SessionStart 自动核对缺项。」
  ——它指的机制仍在（`scaffold-manifest.json` + `dao-scaffold-check` hook），这一行只是指针的指针。

### 二、归属层（意图升维）的信号词表（原 dao.md L31-35）

dao.md 只留「用户描述具体问题时先判归属：项目特有还是 dao 体系缺口；显式说『改 dao / 体系级 /
所有项目都要』就直接走 dao 级，不问」这一句。被搬出来的是**怎么在用户没明说时判**：

- **信号升维**：以下四类信号 ≥2 共现 ⇒ 一句话浮出归属层建议，用户确认后再走。
  ①**痛苦词**（吃力 / 不合理 / 烧钱 / 反复 / 割裂）②**工作流描述**（调试方式 / 开发体验 /
  工作模式 / 链路）③**工具方法论质疑** ④**跨项目可推广**（问题虽在项目 A，同类项目必然也有）。
- **浮出话术**：「这个在 [项目] 里能直接修，但 [原因] 同类项目都会遇到——建议先改 dao 体系，
  [项目] 的改动自然跟上。」
- **无信号 / 信号不足** ⇒ 不升维，做完后由「知识归位」兜底。

### 三、共性 rule 备案整段（原 dao.md L128）

机制本身没变，只是不再每轮注入：跨项目共性的 rule 与基建项一律登记在
`ccswitch/scaffold-manifest.json`（`universal`＝任何项目都该有 / `conditional`＝检测到 X 才必须有 Y，
`when` 须机器可判 / `product-type`＝对所有**产品型**项目合理、对内部工具仓不合理的中间态，
条件由类别内建：项目 `CLAUDE.md` 含「产品型项目」这一**自我声明**，opt-in、不引入 AI 判断），
由 `dao-scaffold-check` hook 逐条求值，缺项即报并给补齐指引。
**新增共性项改清单不改代码**；项目特有 rule 不进清单，归项目 `.claude/rules/`；需语义理解的深度
检查仍在 `dao-project-scaffold` skill 的 supporting files。两条配套原则（①文件名契约
②例外显式化 > 例外隐形）写在清单 `_doc` 段，**那里是唯一真相源**。

⚠️ **删这一段的代价照直写**：`--safe-mode`（hook 全关）时，这段是 SessionStart 核对之外唯一的
文字兜底；删掉之后 hook 不跑就没有任何东西提这件事。dao.md 知识归位节留了一句指针顶着。

### 四、器节被搬走的三段（原 dao.md L209 / L211 / L213）

- **AI 内部件**：五步链 `dao-worktree` / `dao-plan` / `dao-review` / `dao-brainstorm`
  （按路径 Read，见「谋 · 重器之门」）· `dao-serve`（worktree 起 dev server，11 次调用 100% 来自
  subagent；**mousse-cli 已明令绕开它**——它的清理步骤会 kill 用户实例）。
- **设计管线架构**：`/dao-design` 是唯一入口，原七个独立 design 类 skill 已合并为其 supporting
  files，按需 Read 加载。用户只记 `/dao-design [参数]`，参数路由覆盖全部子功能
  （`sync`＝漂移同步，`实现 X`＝代码实施，等）。
- **UI 视觉偏差**：UI 视觉偏差 + 有 `design/` 目录 ⇒ `/dao-design`，以原型为唯一真相源。

⚠️ **可达性矩阵原计划把这三段迁进各 skill 的 `description`**（那本就每轮注入，零新增成本）。
**本批没有做那一步**：改 skill frontmatter 会改用户可见的命令描述，属判断档。
dao.md 器节留了一行压缩指针，三段全文在此。**这是一笔挂着的账，不是已完成项。**

### 五、完成流水线 ②③④ 的细节（原 dao.md L38-46）

- **② 设计同步门控的四小步**（有设计稿？改了 UI 组件？两条都满足则同步；OD 面板快照附属）
  **全文现在住在 `ccswitch/hooks/dao-design-sync-gate.js` 的头注**，那里同时是「散文判据 /
  Stop hook / git pre-commit 闸」三通道分工的唯一真相源。dao.md 只留一句判据 + 指针。
- **③ 打磨自曝的论证**（「完成判据是打磨到位不是跑通了」的两个实证）已在本档 §动-1。
- **④ 微复盘三问的展开**：**自检**——对照原始要求/契约真完成否？欠账显式挂账不静默；
  **沉淀**——本任务暴露的教训/发现有值得留的吗？有则归位（evolution 账本 / TODO 池 / memory），
  无则不硬写；**规划**——由现状推演下一步最优一手：交互态呈为续力选项，自主态直接作为下一批
  输入。不新增文书，口头/选项级完成即可；重的复盘走 `/dao-evolution`。

### 六、「目的否」首版当轮被纠偏的经过（原 dao.md L29 的括注）

首版把触发面窄化成「用户抛出手段句式时」、把「呈对比」写成「AI 独立选一个最优解」，
用户当轮指出片面：**单实例句式被冻成触发条件**（与 L27「把单实例结论冻成通用问卷」同型）。
订正后的判据是「**用户提出的任何方案**（不限句式）先过四问，第四问是把最优解摆出来与用户的
方案**显式对比**，然后让用户判断用哪种」——拍板权在用户，AI 的义务是把对比摆全，不是替用户选。

### 七、本批的「先立后破」落点对照（可核）

| 原 dao.md 位置 | 去了哪 | 送达通道 |
|---|---|---|
| 帅节 合·分级验证 / 验证资源独占 / 热重载隔离 | `ccswitch/rules/dao-dispatch.md` §收将的产出 | 写派单令前 Read（身位差已在该节标注） |
| 帅节 判据/护栏类改动对抗验证前置 | `ccswitch/rules/dao-guard-writing.md` | 作用域档 `dao-scope-guard-writing.md`（Read `check-*`/`verify-*` 时自动送达） |
| Shell 节 Dogfood 自审 / TODO 三级准入 | `ccswitch/rules/dao-product.md` §产品型项目的两条收尾纪律 | 纯文字兜底（该节头注已标身位差） |
| 续力节 选项构成/质量要求/溢出策略 | `ccswitch/rules/dao-askuser.md`（批 1 已落） | 纯文字兜底（该档自陈） |
| 动节 ② 设计同步门控四小步 | `ccswitch/hooks/dao-design-sync-gate.js` 头注（批 1 已落） | Stop 事件 + git pre-commit |
| 上面第一到第六项 | 本节 | 只在有人质疑时读，无需投递 |

### 八、收闸那一刀：10,273 → 10,217 B（2026-08-04）

前一轮压到 **10,273 B**，离 10,240 B 硬闸差 33 B。这一刀**只削措辞、判据一条没动**，
五处逐条留档（左＝原文片段，右＝现文）：

| 处 | 原 | 现 | Δ | 削的是什么 |
|---|---|---|---|---|
| 知识归位 · 根路径消歧 | 指 dao 仓自己**那两个目录** | 指 dao 仓自己**的** | −12 B | 前半句刚列过 `docs/` `stacks/` 两个，「那两个目录」是同义反复 |
| 帅节 · 通报槽位 | **⚔️ **通报槽位每次必填 | 通报槽位每次必填 | −7 B | 同一行的模板里已有 `⚔️`，行首那个是重复装饰 |
| 器 · 命令表 | **七条：**`/dao-design` … | `/dao-design` … | −9 B | 顺带修一处不实：那行实际列了 **6** 个命令（原表 7 行里有两行是同一个命令的两种用法） |
| Shell · 合并链 | 只**被** nudge **提醒** | 只**有** nudge | −7 B | 同句后半「那是提醒不是守卫」已经说了是提醒 |
| 反 · 归 · 预算护栏 | 必须在**合并态求值**、不在增量上**求值**：对**总量**设限的闸（…）**要在合并后的树上跑一次** | 必须在**合并后的树**上求值、不在增量上：**总量**闸（…）**合并前重跑一次** | −21 B | 「合并态求值」与「要在合并后的树上跑一次」是同一句话说两遍；合并后保留了对比的两半（合并后的树 ↔ 不在增量上） |

**判据零丢失的自证方式**：12 个 H2 节名与八句根基逐字未变（与 `git show HEAD:ccswitch/dao.md`
逐句比对，见本批 PR body）；五处改动无一涉及 `[#slug]`、无一删除条款行、
台账对账 `mismatch=0 / orphan_*=0`。
