# 好实践收割台账

> `dao-harvest` workflow 的收割记录与**待批候选**索引。待批候选也进 `dao-consolidate` 扫描面——
> 拦在落地前比落地后再合并便宜。入库属立法动作：照做档帅可落，判断档呈用户（`dao-legislation.md`）。

## 2026-08-07 · 收官窗收割（**24 条已入库**，用户拍板全收）

- **区间**：2026-08-06 傍晚 ~ 08-07 01:39——五路实现官限流全灭 → 接手令批次 → 四路对抗验证 → 云端产物收编的完整叙事窗
- **取数**：帅会话 b1eef88e（8.0MB）9 轮有界 Grep + 平行会话两份 + GitHub 侧复核（PR #164/#165/#167/#168/#170、resume-project#1、trailer 实测）
- **判定**：富矿窗。候选 **13 条（7 新立 / 6 修订）**，四个点名主题全部有盘外可复核锚点
- **原始产出**（全部 candidates 条款原文 + 归属层 + 判重记录 + 核验）：[`harvest/20260807-close-window.json`](harvest/20260807-close-window.json)
- **候选主题速览**：接手令「接手点四格」槽位（5/5 实证）· 死前最后一句双向失真（意图句已做完 / 结论句没证据）· 前任结论当靶单独立重验 · 来路不明 commit 先读 Claude-Session trailer 定来源 · 「已订正」宣称按靶复审（变体必须只打订正面）· 证伪宣称的合并前处置=撤宣称不抢修 · 另有 7 条见原始产出
- **疑似聚合模式一则**（收割官注：不进 candidates，待 `pr-history-postmortem` 量化核实）：本窗多路对抗官各自打出「文字宣称与实况不符」类发现（#167 措辞收窄 / #168 归因错写 / #170 假宣称撤回）
### 入库实况（2026-08-07 落地，用户 issue #72 拍板三板）

**用户拍的三板**：①入库范围＝**过审的全收**（不只台账索引的那 13 条）②判断类**直入正式节 + 同批修立法规则**
③被实证打脸的那条草稿**处方反转改写后收**。

**一句话账**：四路共 **31 条候选**，核验通过 **17 条**；台账此前只索引了 transcript 一路的 13 条
（其中 4 条核验未过），**另三路 8 条过审件从未进过待批数**。本批把「过审的」与「仅因机械格式被打回的」
一并清完，**合并去重后落地 24 条**（16 新立 / 8 修订，另连带给 `[#反-预算护栏]` 记 n+1）。

| 落到哪 | 条数 | slug |
|---|---|---|
| `ccswitch/dao.md` 帅节 | 2 | `帅-越权先查`（修订）· `帅-撤宣称不抢修`（新立） |
| `ccswitch/rules/dao-dispatch.md` 派单契约门附条 | 2 | `派-接手点` · `派-先核再修` |
| `ccswitch/rules/dao-officer-clauses.md` 通用节 | 7 | `官通-坐标错位` · `官通-覆盖声明` · `官通-竞态直读` · `官通-划线保留` · `官通-跨仓双落点`（新立）；`官通-禁笃定措辞` n 4→5 · `官通-用户命令实跑` n 1→2（修订） |
| 同上·实现官节 | 2 | `官实-纯增量解冲突`（新立）· `官实-幂等接续` n ?→5（修订） |
| 同上·对抗验证官节 | 9 | `官抗-订正面变体` · `官抗-样本原文核对` · `官抗-负控独立归因` · `官抗-优先序变体` · `官抗-dryrun变异` · `官抗-断言名实核对`（新立）；`官抗-独立复跑`（扩射程）· `官抗-调用点覆盖率` n 5→6 · `官抗-语料非自证` n 11→12（修订） |
| `ccswitch/rules/dao-guard-writing.md` | 2 | `守-清单派生化`（新立）· `守-自检独立` n 1→2（修订） |

**合并 / 作废关系（四路判重的落地处置，别当成漏收）**：

- `transcript#2` ＋ `pr-commit#1` **二合一** ⇒ 同修 `[#官实-幂等接续]`（前者给「死前最后一句双向失真」这一新观察面，后者给 n=5 的批量基线）。
- `transcript#10` ＋ `intent-log#5` **二合一** ⇒ `[#派-先核再修]`（前者更完整、含「判不出禁静默降级」槽位；后者的独有增量「清白⇒归一化 / 不清白⇒阻断」并入正文）。
- `intent-log#3` **作废**：与 `pr-commit#4` 是同一修订（同一 PR #167 事件、同为 `[#守-自检独立]` n 1→2），后者格式合规，只落后者。
- `workboard#4` **不新立**：核心句与既有 `[#官通-同批查引用]` 逐字重合；其证据（`4b1c44a` 尾注 `(mousse #536)` 反向实例）**并入** `[#官通-跨仓双落点]` 的出处。
- `transcript#13` **反转改写后入库**：原候选写「零消费点变量从文档删除」，而真实 PR 做法是**标注「设了不生效」并保留原始意图记录**（5 个变量注释掉、移进专节、逐条补因由，一字未删）——照原文入库会教出与实证相反的动作。**此条是本批唯一处方被反转的件。**

**三路扩编 11 条的由来**：台账原先只照抄 transcript 一路的自陈（「13 条 7 新立/6 修订」），
而 pr-commit / workboard / intent-log 三路的过审件从未被计入。其中 workboard 4 条与
intent-log 修订批各自**全军死于同一个机械格式**（`@2026-08-07` 应为 `@08-07`；箭头夹注 `n=1→2`），
本批按核验官明言的「一次机械批改即可救」修好后收——**三路的失败各自高度同质，不是内容问题**。

**同批修法**：`ccswitch/rules/dao-legislation.md` §判断类候选不直接进条款库 补一条例外——
经对抗核验官逐条实证复核、且用户整批拍板的收割批次，其判断类条款可直入正式节不走观察区
（**整批拍板本身即比「复发一次」更强的门**）。**修法不绕法**：例外只免「先进观察区」这一步，
元字段 / 基线 / `触发:` 一样不免。

**同批补缺件**：`docs/rules/dispatch-clauses.md` 首次落盘（脚手架 `dispatch-clauses` 条目，
**乙档代做非甲档物化**——该条目没有 `canonical`）。缺它的真实后果是 `dao-harvest` 的判重步
Grep 一个不存在的文件 ⇒ 恒不命中 ⇒ **每条候选都被判 `is_new=true`**，是静默假通过不是静默跳过。
本仓的该文件刻意只装「判重面去哪儿 + 本仓落地坐标」，**不复制任何 dao 层条款正文**（同仓副本必漂移）。

**本次收割点**：`.harvest-marker` 首次落盘（`LAST_HARVEST_COMMIT=c9e0b71` / `PR #168` / `2026-08-07`）。
此前本仓没有这个文件，观察线一直报「无从计算」——**那不等于「从未收割」，只等于没有记录**。

**入库时的结构闸实测**（四道全绿）：`gen-clause-index.mjs` exit=0（entries 99→115、双向孤儿 0、
字段级比过 443 处）· `--check` exit=0 · `--reconcile` exit=0（两套解析 115=115）·
`check-clauses-structure.ps1` exit=0 · 常驻字节预算 11142→11641 B（闸值 71680）。

**照直写没做的**：①本批**未合并**——判据类改动对抗前置，合并由帅在对抗复核后走合并链
②`[#官通-禁笃定措辞]` 的台账 `baseline` 仍为 `null`：候选给的基线经核实是**另一条条款
（`[#官通-body-file回读]`）的基线**被误抄了过来，故不采用，也不替它编一个
③`[#自定@]` 标记**一条都没打**：本批 24 条全部经用户 2026-08-07 拍板，不是 AI 自定，
打上会污染「按日期整批撤回」那个回溯面（有 3 条候选自带 `[自定@08-07]`，已刻意去掉）。

🔴 **本批留下一笔账，别当它已清**：补 `docs/rules/dispatch-clauses.md` 这个动作
**打红了 `tests/subagent-clauses.tests.js` 14 条**——那个 hook「按 cwd 探到项目侧条款库就指它」，
而本仓的项目侧档是**指针档、没有条款正文**，于是注入末尾那句「条款库正文在这里」变得不准确。
**归因已实测**（移走 ⇒ PASS=102 FAIL=0；放回 ⇒ PASS=88 FAIL=14），**缓解已落**
（指针档顶部红字横幅一跳路由对），**修复未做**——改 hook 判据 + 改回归网期望属判据类改动，
须走对抗前置，不塞进文档落地批。⇒ **账 = issue #174 · owner = 帅 · 解冻条件 = 三条走法选定一条 +
hook 与回归网改完 + `run-tests.mjs --env` 拿到 exit 0**。

- **下一步**：对抗验证官复核本批 PR（重点靶＝反转改写件 `[#官通-用户命令实跑]`），复核过后走合并链 `--env` 全绿再合
