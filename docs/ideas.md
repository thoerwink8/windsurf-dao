# ideas

好想法、以后可能做的事，都堆在这里。它**就是故意允许变成垃圾堆的**——垃圾待在一个文件里，
不污染 GitHub issue 的工作队列（#556 拍板 2026-08-16）。

- **为什么不编号**：编号就能被引用，能引用就会互相缠绕成网。这里的每条都是死的孤岛：
  想做的时候整行拎走开单，不想做就让它躺着。
- **什么时候清理**：施工轮转第 4 步「重估」时过一遍（#547）；可整段删，不需要告别。
- 一行一条：想法 + 一句为什么 / 从哪来。不承载任何待办语义——「想做」的标志是把它开成 issue，不是留在这里。

- Orca 横幅本身（宿主注入输入框）仍在：#667 已删掉「靠它给帅收信」整层（人用窗不当 coordinator，信走 inbox.log / GitHub）。剩下的是上游有没有通知开关——没有则向上游反馈（来自 #464）
- 工人弹窗自动放行机制：用户已拍全自动化（黑名单兜底：删除类/强推类/动凭据类不自动放、上报），现状仍靠协调者人工见弹放行；reclaude 启动弹窗已有自动放行（#473）作部分垫片（来自 #464）
- memory 独立仓里 5 处旧目录名/空指针清理：清单见 #474 审官全量扫（真相源已搬 thoerwink8/windsurf-dao-memory，#529），轮到 memory 仓保养时逐条处理
- command-code 的 orca-cli skill 适配（`--skill` 挂载）：等 Orca 把 command-code 纳入 agent 识别清单后重估是否还需要——主体接入已随 PR #513 落地（来自 #508）
- 知识仓保持独立、不合进代码仓（用户 2026-08-16 拍板，已定）：memory 在代码仓时一天出四起事故（#518/#529/#543），draft PR、误带他人未提交、落错分支、worktree 归档卡壳（来自 #558）
- 判据：这东西需不需要分支 / 评审 / 多人协同编辑？三样都不需要的就别进代码仓——记录类天然是三不要，代码类天然是三都要（来自 #558）
- 未决：知识仓扁平（一份索引全项目共用）还是分层（按项目子目录 + global）——扁平费 token 不费脑，分层省 token 但「什么时候把共性抽上去」拷问三轮没答出来，且写下那一刻不知道它以后对谁有用；分层已是补丁链第 2 层，要做得从零重推（来自 #558）
- `docs/model-routing.toml` 的 `[providers.*].launch` 只有 `dao.mjs` 消费；`orca --agent grok` 走 regrok shim（不含 `--always-approve` / `--effort`），两条路不同源——`launch` 对 `--agent` 路径是指向空气的指针，要么让 `--agent` 也读它，要么删掉（来自 #595）
- 审官只标「同一处未修好 / 新引入」时，同型错误换个代码位置会被标成新引入，换人信号一次都不响：换人信号已经改成「乒乓两轮仍红」——`scripts/flow.mjs` 的 `pendingAction` 在 `redReviews >= 3` 时进 `pingpong` 并 `report-switch`；`host/skills/dispatch/templates/reviewer-book.md` 把「乒乓两轮仍红才上帅」写成硬规矩。#497 那种连红多轮，今天会被乒乓接住。同型词表 + 跨轮累计是增量能力，说不出这一批或下一批会做。（来自 #516）
- 仓库转 public + 混淆历史：必须用户在场走完整拷问，第一问还可能整单否掉。这一批和下一批都没有这场拷问，事实先躺着。（来自 #539）
- `roles` 长得像门闩、实际不进选型。主树和 origin/master 都还没接上：glm 有 bans 垫片挡着；把 roles 接进 `checkGates` 是选型设计改动，刚合的 #618 明确另说，没排进这一批或下一批。（来自 #521）
- 协调者的事实断言不能当既定前提。三条都还没写进会跑的模板：改模板没排期；关单还要等「附不出来源当场撤回」「审官前置归因与工人不一致」这类事件，不是这一批能做完的。（来自 #494）
- orchestration skill 在多个 agent 上缺装，换机带不走。仓内三件待办都还没有：这台机器已经装过；缺的是换机文档、cc-switch 覆盖隐患、真工人门铃验证。当前没有换机窗口，排不上队。（来自 #486）
- 新拍板不许顺车塞进在途 PR。规矩和机器闸都没落地：机器闸没人做；并入 #469 的路已断。纯靠记性的规矩，按它自己引用的 #477，不该继续占 issue 席位。（来自 #481）
- 把「出错后制度化」从「我记得不会再犯」改成「遵守方式是不是靠记性」。常驻面没改，用户也没拍板：改常驻面要用户拍板，拍板没落。这一批和下一批都不会改那一行。（来自 #477）
