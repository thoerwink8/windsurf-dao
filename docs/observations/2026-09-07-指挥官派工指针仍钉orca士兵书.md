# 机制巡检：执行体只剩 mirasim，指挥官派工指针和完工闸仍钉 orca 士兵书

## 结论

`#1115` 把派工脊切成只走 mirasim：`dao.mjs dispatch` 注入的第一行是「读 `soldier-book-mirasim.md`」。指挥官每 20 分钟派出去的 `--spec` 却把「闭环框架」写成 `soldier-book.md`（orca 卡态 / `orca worktree set` / 不带 `--executor` 的交卷）。两本书在同一条 prompt 里互相打架。完工信号闸只扫 orca 那本，mirasim 书改了字它仍绿。`#1115` 改了 A（派工脊），没改 B（指挥官指针 / AGENTS.md / worker-brief / 完工闸）。

## 证据

本轮 2026-09-08 00:43 CST，身份 `uid=999(orca)`，未用 sudo。`HEAD=918d0108e`，与 `origin/master` 相同。

### 1. 生产注入走 mirasim 书，指挥官 spec 把人送回 orca 书

`scripts/dao.mjs:1486` 现役派工：

```
const prompt = buildSoldierInject({ spec: args.spec, issue: args.issue, executor: 'mirasim' });
```

`scripts/lib/dispatch/template.mjs:110-111`：

```
const tpl = executor === 'mirasim' ? 'soldier-inject-mirasim.md' : 'soldier-inject.md';
```

`host/skills/dispatch/templates/soldier-inject-mirasim.md` 全文一行：

```
读 host/skills/dispatch/templates/soldier-book-mirasim.md spec={{SPEC}}{{ISSUE_REF}}
```

指挥官自己拼的 SPEC（`scripts/commander.mjs:954-957`）：

```
function dispatchSpec(issue) {
  return `本单职责见 issue #${issue} 正文（权威范围）；闭环框架见 host/skills/dispatch/templates/soldier-book.md。指挥官自动派工（#800）。`;
}
```

`scripts/commander.mjs:627-633` 的 `kind: 'dispatch'` 把这段原样塞进 `--spec`。本轮用生产函数实跑：

```
$ node --input-type=module -e 'import { buildSoldierInject } from "./scripts/lib/dispatch/template.mjs";
const spec = "本单职责见 issue #1 正文（权威范围）；闭环框架见 host/skills/dispatch/templates/soldier-book.md。指挥官自动派工（#800）。";
console.log(buildSoldierInject({ spec, issue: 1, executor: "mirasim" }));'
```

真实输出（一行）：

```
读 host/skills/dispatch/templates/soldier-book-mirasim.md spec=本单职责见 issue #1 正文（权威范围）；闭环框架见 host/skills/dispatch/templates/soldier-book.md。指挥官自动派工（#800）。 #1
```

工人先被叫去读 mirasim 书；mirasim 书自己写「具体职责以注入前言里的本单 spec 为准」（`soldier-book-mirasim.md:3`）；spec 又把闭环框架指回 orca 书。

### 2. 两本书教的交卷 / 开工不是同一套

orca 书 `host/skills/dispatch/templates/soldier-book.md:23-25` 开工第 3、5 步是 `orca worktree set`；交卷（同文件 47 行）是：

```
node scripts/dao.mjs worker-done --pr <PR号> --body-file <文件>
```

没有 `--executor mirasim`。同文件 50 行还写「判定绿之后的身份结算仍用 `notify --type worker_done`」。

mirasim 书 `soldier-book-mirasim.md:34`：「没有卡态可切（`orca worktree set` 那套不存在）」。交卷（同文件 59-63 行）：

```
node scripts/dao.mjs worker-done --pr <PR号> --body-file <文件> --executor mirasim
```

并写「`--executor mirasim` 一个字都不能少（2026-09-06 实咬）……不传就默认走 orca 那条脊……**它会报退出码 0**」。

`#1115` 之后 `scripts/dao.mjs:2967-2976` 已经是 `MIRASIM_IS_ONLY_PATH = true`，漏 `--executor` 也会走 mirasim，不再落 orca。mirasim 书里那句「不传就默认 orca」的前提，本轮 HEAD 上已经不成立。本轮没派一张真单去看工人读了哪本（硬边界不许派工），没验证现场工人跟的是哪一段。

返工那条路更歪一截：`scripts/commander.mjs:1234` 预检注入闸时调 `buildSoldierInject({ spec, issue: action.issue })`，**不传 executor**，量的是 orca 注入模板。真派工仍走 `dao.mjs dispatch`，热路写死 `executor: 'mirasim'`（`dao.mjs:1476-1486` 自己的注释：两本书前缀差 8 字节，闸和渲染必须同一本）。预检和热路用的不是同一本书。

### 3. 闸和指针层都只盯 orca 那本

完工信号闸 `scripts/lib/completion-signal-check.mjs:11-15` 的 `FILES.brief` 是 `host/skills/dispatch/templates/soldier-book.md`。全文 0 处 `soldier-book-mirasim`。mirasim 书把「完工」改成别的字，这一格仍绿。

同形漏改：

| 文件 | 本轮原文 |
|---|---|
| `AGENTS.md:1` | 工人读 `soldier-book.md`，审官读 `reviewer-book.md` |
| `host/skills/worker-brief/SKILL.md:3,6` | 先读 `soldier-book.md` 全文 |

`#1115`（`789d10803`）`git show --stat` 动了 `scripts/commander.mjs` 4 行，本轮 `git show 789d10803 -- scripts/commander.mjs | rg soldier-book` 0 行——`dispatchSpec` 从 `#800` 落地（`ba52bc5c`）到现在没改过。

测试锁的是另一面：`tests/executor-binding.test.js:519-531` 要求 `dao.mjs dispatch` 的 prompt **匹配** `soldier-book-mirasim.md`、**不匹配** `templates/soldier-book.md`。它喂的 spec 是「只做 prompt 路由判别，不启动会话」，不含指挥官那段「闭环框架见 soldier-book.md」。指挥官这段塞进去，prompt 就会同时命中两本书——那条 `doesNotMatch` 会红。本轮没跑这套测试（没验证它现在是不是仍绿）；按 519-531 行的断言和上面那次实跑输出，指挥官这条生产路径过不了它自己仓里的锁。

`tests/commander.test.js` 本轮 `rg dispatchSpec|soldier-book.md` 0 行，指挥官指针没有样本。

### 4. 不是已经报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-05-审官全灭-裸pi落错provider.md` | 审官起错 provider | 本条是工人任务书指针 |
| `2026-09-07-orca-serve仓删了机器还在.md` | 机器上的 orca-serve 没卸 | 本条是仓内两本书打架 |
| `2026-09-07-1056合了对账不推卡住的活人.md` | incomplete 当活人、nudge 没接 | 本条是派工注入的书 |

## 建议的最小改造

删掉「orca 士兵书还是现役闭环」这一层。指挥官 `dispatchSpec`、`AGENTS.md`、`worker-brief`、完工闸的 `FILES.brief` 改指 `soldier-book-mirasim.md`（审官对称改 `reviewer-book-mirasim.md`）。`buildSoldierInject` 预检带上 `executor: 'mirasim'`，和热路同一本。测试把指挥官这段 spec 喂进现在那条 `doesNotMatch templates/soldier-book.md`，锁住「生产路径不许再把 orca 书写进 spec」。orca 那两本标退役或删掉，免得下一处指针再抄到它们。

本轮不动手。改 `commander.mjs` / 任务书指针是改代码，越巡检边界。
