处置：已修 commit 2af1fba9

# 机制巡检：7440c6c5 把报帅 key 规范化了，测试闸比网关真闸松，中文 key 每轮仍被拒
> 处置：已修 commit 2af1fba9（master 同日合入，ASCII 闸对齐；本 PR 跟上）

## 结论

网关 `KEY_RE` 只收可打印 ASCII（`^[\x21-\x7E]{1,200}$`）。今早合进 master 的「规范化」（`7440c6c5`）只把空白折成 `-`，**中文原样留下**；配套测试用的 `isGatewayKeySafe` 只查「非空、无空白、≤200」，**不问是不是 ASCII**。于是测试全绿、生产每 20 分钟报帅仍 `missing_idempotency`。同一条闸把消歧官也掐死：`refiner:labels:<N>:待拍板` 永远过不了，`dao-refiner.service` 挂在 `systemctl --failed` 里，审计里 refiner **29 次全失败、0 次成功**。PR #1143 两天前就在修 ASCII，还开着，没进 master；进 master 的是另一份更弱的「修」。

## 证据

本轮 2026-09-10 12:29 CST，身份 `uid=999(orca)`，未用 sudo。`HEAD=55eb2e4f`（`[cc] feat(check): 合并闸检查器…… (#1110)`），与 `origin/master` 相同。

### 1. 真闸是 ASCII；测试闸不是

`scripts/lib/issue-gateway.mjs:30`：

```
const KEY_RE = /^[\x21-\x7E]{1,200}$/;
```

`:180-181` 不匹配就 `fail('missing_idempotency', 'idempotency_key 必须是 1–200 个可见字符、不能有空白')`——文案说「可见字符」，正则是 ASCII。

`scripts/lib/escalation-key.mjs:16,28-32`（`7440c6c5` 引入）：

```
const UNSAFE = /[^\p{L}\p{N}_.:#/-]+/gu;
const readable = raw.replace(UNSAFE, '-').replace(/-{2,}/g, '-').replace(/^-|-$/g, '').slice(0, keep);
return readable ? `${readable}-${digest}` : digest;
```

`\p{L}` 含汉字。`:36-38` 的「网关那侧的判据」：

```
export function isGatewayKeySafe(key) {
  const s = String(key ?? '');
  return s.length >= 1 && s.length <= 200 && !/\s/.test(s);
}
```

没有 ASCII。`tests/escalation-key.test.js:13-14,52-53` 把「规范化后合法」锁成 `isGatewayKeySafe(...) === true`，正控样本含中文 title 和 `！！！`。本轮 `node --test tests/escalation-key.test.js` 没跑（时间不够）；测试文件本身不引用 `KEY_RE`，也没有 `validateRequest`。

本轮用生产函数实跑：

```
$ node --input-type=module  # 调 escalationKeyOf + validateRequest
commander-escalate:查重标记-勿删-commander-inventory-escalate/missing-labels-da797140e2b3
  isGatewayKeySafe true
  KEY_RE false
  validateRequest {"ok":false,"error":"idempotency_key 必须是 1–200 个可见字符、不能有空白","stage":"missing_idempotency"}

refiner:labels:1177:待拍板
  isGatewayKeySafe true
  KEY_RE false
  validateRequest 同上

whitespace-title → commander-escalate:#1146-自动派工失败-ok-:false-16e2c78c89d2
  isGatewayKeySafe true
  KEY_RE false
```

「规范化后」三组样本测试闸全绿、真闸全红。中文 title 折完仍带「自动派工失败」。

### 2. 合进 master 之后，报帅还在被拒

`7440c6c5` 2026-09-10 03:58:42 +0800 合入（`git merge-base --is-ancestor 7440c6c5 HEAD` 退出 0）。`scripts/commander.mjs:2174-2183` 已经走 `escalationKeyOf`：

```
const keySeed = marker ? marker[0] : title;
const key = `commander-escalate:${escalationKeyOf(keySeed)}`;
```

marker 来自正文「查重标记（勿删）：…」，含中文。规范化之后的 key 就是上面那条 `commander-escalate:查重标记-勿删-…`。

`~/.dao/issue-gateway/audit/audit.ndjson`，时间戳 ≥ `2026-09-09T19:58:42Z`（合入时刻 UTC）之后：

- 事件 141 条
- `ok:false`：commander 106 + refiner 8（本轮后段再计 commander 累计 107）
- 失败元组只有一种：`missing_idempotency` / `idempotency_key 必须是 1–200 个可见字符、不能有空白`
- 失败 key 就 4 个：
  - 84× `commander-escalate:查重标记-勿删-commander-inventory-escalate/missing-labels-da797140e2b3`
  - 23× `commander-escalate:查重标记-勿删-commander-inventory-escalate/dispatch-failed-00a5db795e9d`
  - 6× `refiner:labels:1176:待拍板`
  - 2× `refiner:labels:1177:待拍板`

最新一条 commander 失败：`ts=2026-09-10T04:29:47.475Z`（本轮 12:29 CST 之前约 0 分钟量级；act 12:11 刚跑过）。本机 `~/.dao/commander/escalate-*.md` 371 份落盘正文，开单没出去。`escalateDedupKey` 本地账本是否因此也没记上 GitHub 单号——没验证。

`isGatewayKeySafe` 只在 `scripts/lib/escalation-key.mjs` 和 `tests/escalation-key.test.js` 出现，生产路径不调用。

### 3. 消歧官被同一条闸从第一张要打「待拍板」的单掐死

`scripts/refiner.mjs:156`：

```
idempotency_key: `refiner:labels:${n}:${plan.labelsToAdd.join(',')}`,
```

`labelsToAdd` 现场就是 `["待拍板"]`（本轮 `--dry-run --json`：#1177/#1176/#1173… 均 `labelsToAdd:["待拍板"]`）。`refiner:comment:1177` 是纯 ASCII，`KEY_RE` 为 true——到不了评论那一步，因为 `applyPlan` 先打标，失败就 `exit: 1`（`:235-238`）。

审计 host=`refiner`：**29 条、ok 0 条**。全是 `issue_edit_labels` + `missing_idempotency`。最近：`2026-09-10T03:41:53.018Z` `refiner:labels:1177:待拍板`。

单元与仓 md5 相同。本轮：

```
$ systemctl show dao-refiner.service -p Result,ExecMainStatus,ExecMainStartTimestamp,ActiveState
Result=exit-code
ExecMainStatus=1
ExecMainStartTimestamp=Thu 2026-09-10 11:41:41 CST
ActiveState=failed
```

timer 仍 `active (waiting)`，NEXT=`12:41:15`。`--dry-run` 在单元环境（`GH_CONFIG_DIR=/var/empty`）下 exit 0、`scanned:true`——读 GitHub 通，挂的是写下。`systemctl --failed` 本轮还有 `miraquota-contabo.service`（已报 `2026-09-09-凭据闸把额度采样的git推送掐死.md`）和 `dao-session-gc.service`（not-found，已在今早指挥官 timer 那条里点过、不另报）。

server-check / dao-check / 指挥官盘点都不扫 `--failed`（`rg "systemctl --failed|--failed" scripts/server-check.mjs scripts/dao-check.mjs scripts/lib/commander-inventory.mjs` 无命中）。这条「没人看 --failed」已在 09-09 额度采样那份里写过，这里只说明：消歧官挂了也没人看。

### 4. 不是已经报过的那几条；#1143 还开着但没进 master

| 已有观察 / 在途 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-09-凭据闸把额度采样的git推送掐死.md` | `GH_CONFIG_DIR=/var/empty` 掐 git 推送 | 消歧官 dry-run 在同一环境下 exit 0；挂的是网关 ASCII 闸 |
| `2026-09-08-指挥官单元不在凭据闸扫描面.md` | 凭据闸扫不到指挥官单元 | 本条是幂等键字符集 |
| PR #1143（OPEN，`updatedAt=2026-09-08T10:12:50Z`） | 「报帅 key 过网关 ASCII 闸——中文 marker 每轮被拒」 | `git merge-base --is-ancestor bc83fcf5 HEAD` 退出 1，**没进 master**。改的是 `commander.mjs` / `escalate-group.mjs`，不动 `escalation-key.mjs`、不动 refiner。其后合入的 `7440c6c5` 是另一份更弱的修，测试还把「非 ASCII 也算合法」锁绿 |
| `2026-09-10-指挥官timer停了自检仍绿.md` | timer dead、⑭ 只问 enabled | 本轮 `commander-act.timer` 已恢复 waiting，NEXT=`12:31:00`；病是报帅写路 |

`docs/observations/` 里 `rg 幂等|idempotency|ASCII|可见字符|查重标记` 无命中。

## 建议的最小改造

删掉「第二份网关判据」。`isGatewayKeySafe` 改为 `export { KEY_RE }` 再测，或直接 `validateRequest`；`escalationKeyOf` 的可读前缀只留 ASCII（`[A-Za-z0-9_.:#/-]`），汉字进摘要。`refiner.mjs:156` 的 key 不要拼中文标签名，改 `refiner:labels:<N>:<slug>`（`dai-pai-ban`）或只拼 issue 号。测试加一条：**生产 `validateRequest` 吃规范化后的 key 必须 ok**，正控就用本轮这两条真实失败 key。#1143 若还在修同一件事，先对齐这份已合入的弱规范化，别两套并行。

本轮不动手。改 key / 改测试是改代码，越巡检边界。
