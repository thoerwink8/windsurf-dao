处置：#1170

# 机制巡检：收件箱从 hook 挪到 dao-check，skill 没改、新载体也没人跑
> 处置：#1170/#1171 收件箱从 hook 挪到 dao-check——同一起因开了两单，待并单

处置：#1171

## 结论

2026-09-06 把收件箱从 Claude Code 的 `UserPromptSubmit` hook 挪到 `dao-check`（`d48ecc5d`），因为服务器上那个 hook 根本没装、现役执行体也没有这种钩子。skill 和 `global-CLAUDE.md` 仍写「每轮由全局 hook 提醒」；闸仍只验那份已死的 hook 文件。`dao-check` 只在 `land.mjs` **推默认分支**时才跑，机器上 `dao-land.timer` 从未装上，指挥官只在自己刚合完一张 PR 时调一次 land。于是「帅位每轮被提醒去读」这一层在这台机器上不存在。本轮 `assessInbox` 是 `block`：5 条未处置，1 条已超 24 小时。

## 证据

本轮 2026-09-09 12:36 CST，身份 `uid=999(orca)`，未用 sudo。`HEAD=4a6b0f2e`，与 `origin/master` 相同。

### 1. 代码 09-06 已经换载体，文档和闸还钉在旧载体上

挪走的提交：

```
$ git log -1 --format='%h %ci %s' d48ecc5d
d48ecc5d 2026-09-06 19:05:32 +0800 [cc] fix(inbox): 收件箱从 Claude Code 的 hook 挪到 dao-check——原载体在服务器上根本没装
```

`git show d48ecc5d --name-only` 没有 `host/skills/dao-inbox/SKILL.md`、没有 `docs/global-CLAUDE.md`。skill 最后一次改是 `f1e3db09`（2026-09-05，落地 hook 那天）。

现役原文：

- `host/skills/dao-inbox/SKILL.md:18`：「每轮由全局 hook 注入一行提醒」
- 同文件 33–35 行：「钩子：…登记在**全局** `settings.json` 的 `UserPromptSubmit`」「因为是全局钩子，**每个项目都会跑**」
- `docs/global-CLAUDE.md:28`：「每轮由全局 hook 提醒」

`dao-check.mjs:1084-1095` 自己写着为什么挪走，并假定新载体是「帅位每次 land 的必经之路」：

```
// 原设计：全局 settings.json 的 UserPromptSubmit hook 每轮提醒。**实测这台服务器上根本没装**
// …
// 所以挪到 dao-check：它是帅位每次 land 的必经之路，与客户端无关。
```

`checkInbox()` 在 `scripts/dao-check.mjs:1096` 定义，`:1806` 调用。

本机两个 settings 都没有 inbox：

```
$ python3 … ~/.claude/settings.json  # UserPromptSubmit 命令含 inbox-check / dao-inbox / inbox？
--- contains inbox-check False
--- contains dao-inbox False
--- contains inbox.mjs False

$ python3 … .claude/settings.json
hook keys: PreToolUse, SessionStart
# PreToolUse = dispatch-gate / ask-gate / tool-use-gate；SessionStart = onboard-session-hook
# 0 处 inbox
```

`~/.pi/agent/settings.json` 没有 `hooks` 键。`~/.mirasim/setting.json` 没有 `hooks` 键。`host/skills/dao-inbox/hooks/` 只有 `inbox-check.mjs`，没有 `hooks.json`。全仓（除 observation 副本）`inbox-check` 只出现在 skill、钩子自己、和测试。

闸 `tests/inbox.test.js:91-107` 三断言全锁那份钩子文件：存在、不许 `exit(2)`、从 `process.cwd()` 找仓。0 处 `settings.json`、0 处 `dao-check`、0 处 `checkInbox`。skill 继续写「登记在全局 settings」，测试继续绿。

### 2. 新载体的前提「每次 land 必跑 dao-check」不成立

`scripts/land.mjs:73-85`：只有 `ship.action === 'push'` 才跑 `dao-check`。快进远端、没东西可推、不在默认分支，都不跑。

小时触发本应是 `dao-land.timer`（#880 把 orca automations 换成这一套）。本轮：

```
$ systemctl is-enabled dao-land.timer; echo exit=$?
not-found
exit=4

$ ls /etc/systemd/system/dao-land.*
ls: cannot access '/etc/systemd/system/dao-land.*': No such file or directory

$ crontab -l
no crontab for orca
```

用 ⑧ 自己的纯函数对这台机器实跑：

```
$ node --input-type=module -e '… classifyLandTimer({ probed:true, isEnabled:"not-found", timersText })'
{ state: 'red', detail: 'dao-land.timer=not-found（不在 = 红）——sudo bash scripts/install-land.sh' }
```

server-check 在这台机器上没有心跳（已报 `2026-09-06-server-check无心跳.md`），`ls /home/orca/.dao/server-check` 仍是 `No such file or directory`——⑧ 会红，没人跑。

指挥官会调 land，但只在自己刚合完一张 PR 之后（`scripts/lib/commander-core.mjs:845` `kind: 'land'`）。人在 GitHub 上点 merge、巡检落 observation、没有新 PR 可合的时候，这条腿不响。指挥官盘点每 6 小时跑 6 项（`scripts/lib/commander-inventory.mjs:277-282`：孤儿 cwd / 终端 / timer / 探针 journal / 落地清单 / 过期 dispatch），没有 inbox。

本轮 `dao-patrol.timer` LAST=`12:24:46`（本会话），`commander-act.timer` LAST=`12:11:00`。两条都活着，都不读 `docs/observations/`。

### 3. 本轮收件箱已经在硬闸线上

```
$ node --input-type=module -e 'import { parseInboxDoc, assessInbox } from "./scripts/lib/inbox.mjs"; …'
{ mode: 'block', pending: 5, overdue: 1 }
```

5 条未处置（本轮 `parseInboxDoc` 读盘，`handled=false`）：

| 文件 | 距本轮 |
|---|---|
| `2026-09-07-回流提示层钉退役士兵书.md` | 30 小时（overdue） |
| `2026-09-08-指挥官单元不在凭据闸扫描面.md` | 24 小时 |
| `2026-09-08-控制面闸无写腿且挂在退役执行体.md` | 24 小时 |
| `2026-09-08-gw-remote-probe仍跑仓外旧脚本.md` | 12 小时 |
| `2026-09-08-orca读不了journal-探针连红闸没查成.md` | 6 小时 |

`assessInbox` 的 block 条件是「超 24 小时 / 堆到 5 条 / 未提交」。本轮正好堆到 5 且有 1 条超时。硬闸文本要写「本轮先处置」——但没有会话被注入这段字。现役工人是 mirasim 下的 pi（本会话 `available_skills` 来自 `~/.pi/agent/skills` 与 `~/.agents/skills`），没有 `UserPromptSubmit`。

### 4. 不是已经报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-06-server-check无心跳.md` | 22 项活体闸没人跑 | 本条对象是收件箱通道；无心跳只解释「⑧ 红了也没人看」 |
| `2026-09-06-land小时触发还钉orca-automations.md` | 当时 hourly 还钉 orca；处置 #880 把单元写进仓 | #880 合了，机器上 timer 仍 not-found；本条拿它当「dao-check 不会被定时唤起」的证据，不重报 land 没装 |
| `2026-09-06-四套单元未装-⑳把没装判没查成.md` | gh-events / miraquota-contabo / refiner / progress-watch | 对象名单不含收件箱 |
| `2026-09-08-控制面闸无写腿且挂在退役执行体.md` | #948 闸挂 Claude/Cursor PreToolUse | 同形（挂载面不在现役执行体），对象是收件箱，不是 git push |

`d48ecc5d` 的提交说明把「文档说有、实际没有」写成当天已修。它修的是判据函数的调用点（进了 dao-check），没修 skill / 全局约定，也没给新调用点配一条真会跑的腿。三天后 skill 仍教 hook，5 条 observation 没人被要求去读——同一形状又一次。

## 建议的最小改造

删掉「dao-check = 帅位每轮会看见收件箱」这一层。`checkInbox` 的调用点改到已经在跑的腿上：指挥官盘点（`commander-inventory` 每 6 小时，orca 身份，不靠 journal）或巡检会话开工（本任务书已经强制读 `docs/observations/` 那份名单，把 `assessInbox` 的 block 文本一并注入）。skill / `global-CLAUDE.md` 那句「每轮由全局 hook 提醒」删掉或改成新腿的名字。`tests/inbox.test.js` 不要再把「钩子文件还在」锁成绿——锁「现役执行体的挂载面」或「盘点/巡检会调用 `assessInbox`」；skill 还写 `UserPromptSubmit` 就红。

本轮不动手。改 skill / 测试 / 盘点是改代码，越巡检边界。
