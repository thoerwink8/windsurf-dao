---
status: done
---

---
status: new
---

# 机制巡检：#1174 说派工不读 newapi；现役 GPT 健康键探的仍是本机桥，上游还是 sslip.io + gptpool.key

## 结论

#1174 把 newapi 网关从派工选型退役，规则写「任何派工路径都不再读它」。现役 GPT 腿（sol / luna）落地是 `mirasim-relay`，健康/熔断键被故意复用成 `direct:codex@pqapi/responses`。这个键的探针不打 `api.pqapi.shop`，它读 `~/.codex/config.toml` 的 `base_url`。那份配置从 09-10 起就是 `http://127.0.0.1:4317/v1`。4317 上的 `responses-chat-bridge` 从 09-12 开机起没换过，上游写死 `https://156.224.28.95.sslip.io/v1/chat/completions`，钥匙是 `gptpool.key`，模型别名 `gpt-5.6`——和网关探针打的是同一台退役机。目录里启用的 GPT profile 是 `codex-relay-*`（mirasim-relay / cloud）；`codex-pqapi-sol` 是 `enabled: false`。健康表这一格绿，测的不是目录那条路，也不是 pqapi，是退役网关的本地转接头。dao-check / ⑳ 0 处问这个单元。

## 证据

本轮 2026-09-15 12:24–12:35 CST，身份 `uid=999(orca)`，未用 sudo。`HEAD=7c342657`（本轮已推的用量观察），与 `origin/master` 快进相同。未对 4317 / sslip.io 发模型请求。

### 1. 现役 GPT 健康键故意复用旧名；探针读的是本机 Codex 配置

`scripts/lib/provider-probe.mjs:93-106` 原文：

```
* `mirasim-relay` 与旧 `gpt` 共用这一条唯一 target：周期探针仍写这个 key，
* 网关退役只改了选型落地的 provider 字符串，健康/熔断闸不能跟着摘掉。
…
if (provider === 'gpt' || provider === 'mirasim-relay') {
  return 'direct:codex@pqapi/responses';
}
```

本轮直接读 `docs/model-routing.json` 在役 GPT 腿：

| id | 状态 | 供应商 | 落地.provider |
|---|---|---|---|
| gpt-5.6-sol@mirasim-relay/mirasim | 在役 | mirasim-relay | mirasim-relay |
| gpt-5.6-luna@mirasim-relay/mirasim | 在役 | mirasim-relay | mirasim-relay |
| gpt-5.6-sol@pqapi/orca | 停用 | pqapi | gpt |
| gpt-5.6-luna@gw/orca | 停用 | gw | gw |

`scripts/gw-remote-probe.mjs:114-129` `readCodexConn`：打开 `~/.codex/config.toml`，取 `[model_providers.<prov>]` 的 `base_url`，再 `fetch(baseUrl + '/responses')`（:143）。本轮 `~/.codex/config.toml`（mtime `2026-09-10 03:22`，#1174 合入之前）原文：

```
model_provider = "custom"
model = "gpt-5.6-luna"
[model_providers.custom]
name = "本机 Responses→GPT 池 Chat 桥（Windsurf→PQAPI→Mirasim）"
base_url = "http://127.0.0.1:4317/v1"
wire_api = "responses"
```

名字里写着 PQAPI，URL 是本机 4317。#1174 合入日是 09-14（`b5a41119`），这份 toml 没动。

健康表本轮 `updatedAt=2026-09-15T04:09:02.905Z`（= 12:09 CST，gw-remote-probe 刚跑完）：`direct:codex@pqapi/responses` **green 2449ms**。同一次：`leg:pqapi-sol` red HTTP 400，`leg:pqapi-free` red HTTP 503。准入用的是 `direct:` 那一格绿，不是这两条红腿。

### 2. 4317 的上游就是退役网关

```
$ ss -ltnp | grep 4317
LISTEN … 127.0.0.1:4317 … users:(("node",pid=2002151,…))

$ systemctl show responses-chat-bridge.service -p MainPID,NRestarts,ExecMainStartTimestamp,User,FragmentPath
MainPID=2002151
NRestarts=0
ExecMainStartTimestamp=Sat 2026-09-12 06:29:14 CST
User=orca
FragmentPath=/etc/systemd/system/responses-chat-bridge.service
```

活进程 environ（本轮 `tr '\0' '\n' < /proc/2002151/environ`，不落 key）：

```
RESPONSES_BRIDGE_PORT=4317
RESPONSES_BRIDGE_UPSTREAM=https://156.224.28.95.sslip.io/v1/chat/completions
RESPONSES_BRIDGE_KEY_FILE=/home/orca/.mirasim/keys/gptpool.key
RESPONSES_BRIDGE_MODEL=gpt-5.6
RESPONSES_BRIDGE_DEBUG=1
```

`scripts/gw-remote-probe.mjs:45` 的 `GATEWAY` 默认值就是 `https://156.224.28.95.sslip.io`。桥和 6 个 `gw:` 池打同一台机器；桥用的是 gptpool 那把钥匙。单元不在仓内 `host/machine/systemd/`，mtime `2026-09-09 04:22`。drop-in `debug.conf` 只开了 `RESPONSES_BRIDGE_DEBUG=1`。

`docs/execution-profiles.json` 本轮：启用 7 条。GPT 四条全是 `codex-relay-gpt-5.6-*` / `codex-relay-gpt-6-astra`，`provider=mirasim-relay`，`route=cloud`，`backend=mirasim`。`codex-pqapi-sol`（`:1259-1273`）`provider=pqapi`、`baseUrl=https://api.pqapi.shop/v1`、**`enabled: false`**。目录里启用的 GPT 路不是 pqapi 直连，也没有 4317。

### 3. 闸看不见这座桥；⑳ 从仓内文件名出发

`scripts/dao-check.mjs` / `scripts/server-check.mjs` 全文 0 处 `4317` / `responses-chat-bridge` / `sslip`。⑳ 本轮仓内 31 个单元 vs `/etc`：missing 空，正文 drift 只剩 `gw-remote-probe.service` 差半句注释（已报）。`responses-chat-bridge.service` 在 `/etc` 的 extra 名单里，不进 pairs。

没验证：mirasim 起的 GPT 工人 HTTP 是走 `relay.mirasim.ai` 还是也读 `~/.codex/config.toml` 进 4317——本会话没跟一次 GPT 工人的请求。按 `probeTargetOf`，派工准入看的就是 `direct:codex@pqapi/responses` 这一格；这一格的绿来自对 4317 的真请求。没验证：sslip.io 此刻还是不是 09-14 那台 newapi 网关的同一份进程——hostname 与探针默认值逐字相同。

### 4. 不是已报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-14-网关探针仍打退役池.md` | timer 仍对 6 个 `gw:` 池发真请求；native 永远 unscanned | 那条把 `direct:codex@pqapi/responses` 当成现役 GPT 的绿格子。本条是 **这格绿测的仍是退役网关的本地桥**，名字里的 pqapi 对不上目录、也对不上 upstream |
| `2026-09-08-gw-remote-probe仍跑仓外旧脚本.md`（处置：#1164） | 当时 ExecStart 还指 `~/bin` | 活探针已经是仓内脚本；本条对象是 **Codex 配置 + 桥单元**，不在那份 ExecStart 里 |
| `2026-09-14-飞书triage合了码没重启.md` | 飞书长连接 PID 没换 | 桥也是 09-12 开机那一个，但本条不是「合了码没重启」——桥的上游写在单元环境变量里，仓内根本没有这份单元 |

## 建议的最小改造

删掉「`mirasim-relay` 复用 `direct:codex@pqapi/responses` + 探针读 `~/.codex/config.toml` = 在探现役 GPT 路」这一层。

健康键跟目录启用 profile 走同一条 URL。启用的是 mirasim-relay cloud，就探那条；不要再拿 4317→sslip.io 的 200 给一张名叫 pqapi 的键涂绿。本机桥若只剩探针在用，停掉它，别留一把 gptpool.key 对着退役网关。不要在 `gateway-policy.json` 再给这座桥开白名单——#1174 已经写了派工不读 newapi，桥还在读。

处置：#1051（同一形状「闸看的那一层 ≠ 跑的那一层」，归类单；2026-09-16 帅位盘点，判据 review_rounds_max 未接线见 #1227）
