---
status: done
---

---
status: new
---

# 机制巡检：#891 播报闸测绿、W5 写口今天合进 master，生产路径 0 处调用；指挥官仍走 6 小时时间维

## 结论

#891 要把「谁决定往群里播什么」从各调用方手里收成一份账 + 一个闸。W3（`scripts/lib/broadcast-gate.mjs`，09-06 合入）是那份纯函数；W5（`action-writers-hook`，今天 04:24 合入）是往账上写的那条腿。闸自己的头注写着：W5 接线时，指挥官 `hubOnce` 的 6 小时时间维应当退役。本轮现场：`decideBroadcast` / `loadBroadcastGroups` 在 `scripts/` 里除自身外 0 处 import；dao-check 0 处问这件事；实机群映射 5 个群的 `broadcast` 全是空。W5 把写口挂进了 `.claude/settings.json`，读口根本没接上。测绿的那一层和模板上的 `broadcast` 节，都不是正在播的那一层。

## 证据

本轮 2026-09-14 12:23–12:39 CST，身份 `uid=999(orca)`，未用 sudo。`HEAD=a6bd540b`，与 `origin/master` 相同。

### 1. 闸在，测试在，生产 0 个调用方

```
$ git log -1 --format='%h %ci %s' aadb6ab0
aadb6ab0 2026-09-06 05:47:05 +0800 feat(broadcast): 播报闸五条（纯函数）+ 群订阅配置沿用群↔仓映射（#891 W3） (#896)

$ git log -1 --format='%h %ci %s' 1007a314
1007a314 2026-09-14 04:24:32 +0800 [cc] feat(events): 动作触发写口——待拍板/拍板结果/里程碑挂在具体动作上（#891 W5） (#897)
```

`1007a314` 改的文件：`.claude/settings.json`、`scripts/lib/action-writers-hook.mjs`、`scripts/lib/action-writers.mjs`、两份测试。0 处 `broadcast-gate`。

本轮 `grep`（`decideBroadcast|loadBroadcastGroups`）于 `scripts/*.mjs` `scripts/lib/*.mjs`：命中只在 `scripts/lib/broadcast-gate.mjs` 自身。`tests/broadcast-gate.test.js:22` 是测试里的 `import`。`scripts/dao-check.mjs` 全文 0 处 `broadcast`。

`scripts/lib/broadcast-gate.mjs:12-13` 原文：

```
// 主体的 digest 一变立刻放行。W5 接线时 `hubOnce` 的时间维那一层应当退役，
// 由本文件的闸1 + 调用方持久化的 `state.seen` 顶上；两条同时留着 = 双重压制。
```

`:57-59` 写明调用方要做的两件事（把 `dedupeKey` 记进 `state.seen`、把条数累加进 `state.sentToday`）。本文件不做。本轮没有第三个文件做这两件事。

### 2. 指挥官仍是 6 小时时间维；hubSay 走的是另一套日报队列

`scripts/commander.mjs:109`：

```
const HUB_DEDUP_MS = OPEN_ISSUE_CARD_DEDUP_MS; // 同一条回流 6 小时内不重发
```

`:707-714` `hubOnce`：同一 `key` 在 `HUB_DEDUP_MS` 内直接 `reason: '6 小时内已发过'`。盘点、到时机、代拍、心跳全走它（同文件 808、2305、2731、2792、2864 行；`scripts/lib/commander-inventory.mjs:409,446`）。

`:699-704` 的 `hubSay` 调的是 `recordBroadcast`（`scripts/lib/broadcast-io.mjs`，#1029 日报队列：心跳/发布/熔断换日合成一张卡）。那是第三套「播」——跟 #891 闸不是同一个函数。`broadcast-io.mjs` 全文 0 处 `decideBroadcast`。

### 3. 模板写了 broadcast 节，实机一份都没有

仓内占位 `host/machine/feishu-groups.json:2` `_broadcast_comment` 把 `loadBroadcastGroups` / `decideBroadcast` 写成「别另立文件」的判官；同文件占位群带 `"broadcast": { "subscribe": [...], "dailyBudget": N }`。

本轮读实机 `~/.mirasim/keys/feishu-groups.json`：5 个群（3 project + 2 hub），`broadcast` 字段全部是 `None`。闸若被调用，会按 kind 套默认（头注：hub 只订 milestone 上限 6，project 三类全订上限 20）。没人调用，默认值也不生效。

### 4. 不是已报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-14-grok关掉了claude钩子.md` | 现役 grok 把 Claude PreToolUse 关掉，W5 写口因此不响 | 对象是**写**；本条是 **读/播** 根本没接。就算 Claude 会话把事件写下了，也没有调用方拿 `decideBroadcast` 去播 |
| `2026-09-06-server-check无心跳.md`（处置：#1051） | server-check 没人跑 | 播报闸不在 server-check 里，dao-check 也不问它有没有调用方 |
| `2026-09-09-收件箱挪到dao-check没人跑.md`（处置：#1170/#1171） | 载体挂错 | 本条是新层（#891）只进了测试和模板，没进任何 timer / act |

## 建议的最小改造

删掉「W3 测绿 + 模板写了 broadcast 节 + W5 把 hook 写进 settings.json = 播报已经在跑」这一层。

要么把 `decideBroadcast` 接到一个真实调用方（指挥官 act 读账 → 闸 → 送达），并把决策/里程碑那几类从 `hubOnce` 上撤掉——头注自己说了两条同时留着是双重压制。要么把还没接线的纯函数和模板节标成未接线，dao-check 扫到 `scripts/` 里 0 个生产 import 就红。

不要再给闸加一份「允许未接线」的白名单——W5 今天刚合，调用方仍是 0。

处置：#1051（同一形状「闸看的那一层 ≠ 跑的那一层」，归类单；2026-09-16 帅位盘点，判据 review_rounds_max 未接线见 #1227）
