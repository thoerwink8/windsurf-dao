# 机制巡检：#1056 合了，对账循环不推卡住的活人；`routeSilent` 是死代码，垫片也没在跑

## 结论

#1056 的对账循环已经合进 master 并关单，差集动作只做一件事：账上有未结派工、名单里**没有**活会话 → 重派。`incomplete`（一轮跑完在等下一句话）被 `isLiveSession` 当成还活着，所以**不会**进差集。真正「说一句继续」的入口写在 `routeSilent`，注释自称「垫片退役后这一档就是对账循环的差集动作入口」——指挥官既不 import 它、`execAction` 也没有 `nudge` 这一档，测试只锁纯函数。垫片 `dao-nudge-stalled.timer` 机器上仍是 `disabled` + 从未触发。于是发现面（progress-watch）还在，处置面整层空着：卡住的活人既不会被推一把，也不会被重派。

## 证据

本轮 2026-09-07 18:38 CST，身份 `uid=999(orca)`，未用 sudo。`HEAD=515953302`，与 `origin/master` 相同。

### 1. #1056 自己把「推一把」划到下一跳，然后关了

PR #1057 合入 `e3a3c66fd`（2026-09-06T23:40:18Z），issue #1056 于 2026-09-07T00:23:12Z 被 `dao-marshal[bot]` 关上。本轮 `gh issue view 1056`：`state=CLOSED`。

合入 PR 正文「仍会再犯的边界」原文（`gh pr view 1057`）：

> 「说一句继续」仍是垫片 `nudge-stalled.mjs`。对账循环的差集动作是重派（人已经不在名单里）；incomplete 仍算活执行者，不会被差集重派抢树。垫片退役是下一跳，不是本单范围。

#1056 时间线上 2026-09-06T14:36:51Z 有 marshal 评论：「垫片登记：`dao-nudge-stalled.timer` 已上机，**本单合并时整套退役**」。单关了，退役脚本没人跑，接班人也没接「推一把」这一档。

### 2. 对账循环把 incomplete 当活人，差集因此跳过

`scripts/lib/session-reconcile.mjs:18-21` 的终态集合：

```
const DEAD_STATES = new Set([
  'completed', 'complete', 'done', 'finished',
  'failed', 'error', 'aborted', 'cancelled', 'canceled',
]);
```

同文件 `isLiveSession`（85–99 行）：不在这个集合里 → `{ live: true }`。`incomplete` 不在里面。

`planReconcile`（207–273 行）对每张未结派工问 `hasLiveExecutor`；`live.live` 为真就 `continue`，不进 `redispatches`。本轮用这台机器上的真实规则读源码路径，没有跑指挥官 dry-run（没验证它打印出来会不会跳过某一张具体单）。按 85–99 行和 256–263 行，incomplete 会话会挡住重派。

`scripts/lib/commander-core.mjs:869-919` 第⑤段只把 `plan.redispatches` 变成 `kind: 'dispatch'`。没有 `kind: 'nudge'`。

`scripts/commander.mjs:625-696` 的 `execAction` switch：`dispatch` / `attach-reviewer` / `merge` / `land` / `rework` / `rereview` / `wake-brain` / `notify-hub` / `escalate` / `add-label` / `retry-drain` / `open-issue` / `reap-ticket` / `mark-exhausted` / `noop`。没有 `nudge`。未知 kind 走 default 失败。

指挥官对 liveness 的唯一 import（`scripts/commander.mjs:32`）：

```
import { progressSignature } from './lib/liveness.mjs';
```

本轮 `rg -n routeSilent scripts/commander.mjs scripts/lib/commander-core.mjs tests/commander.test.js tests/session-reconcile.test.js`：0 行。`routeSilent` 的调用点只在 `tests/liveness.test.js`（锁「工人 incomplete 先推一句继续」）和它自己的定义处。

`scripts/lib/liveness.mjs:163-172` 注释原文：

```
 * 垫片 `nudge-stalled.mjs` 退役后，这一档就是对账循环的差集动作入口。
 * 推不动才由调用方升到重派；本函数不越级。
export function routeSilent(...)
```

测试绿着，生产路径从未调它。

### 3. 垫片单元还在仓里，机器上 disabled，从未跑过

`host/machine/systemd/dao-nudge-stalled.service:3-22` 仍写「随 issue #1056 合并时整套退役」「退役：#1056 落地后，删本单元 + 删 scripts/nudge-stalled.mjs」。`NEW-MACHINE.md:378` 同一句：「垫片，随 #1056 对账循环落地时整套删掉」，同时又给安装命令 `sudo bash scripts/install-nudge-stalled.sh`。

机器上：

```
$ systemctl show dao-nudge-stalled.timer -p UnitFileState,ActiveState,SubState,NextElapseUSecRealtime,NextElapseUSecMonotonic,LastTriggerUSec,WantedBy
UnitFileState=disabled
ActiveState=inactive
SubState=dead
NextElapseUSecRealtime=
NextElapseUSecMonotonic=infinity
LastTriggerUSec=
WantedBy=

$ systemctl show dao-nudge-stalled.service -p ExecMainStartTimestamp,Result
ExecMainStartTimestamp=
Result=success
```

`LastTriggerUSec` 空、`ExecMainStartTimestamp` 空：这台机器上的 nudge oneshot **一次都没被 timer 拉起来**。谁 `disable` 的，本轮读不到 journal（orca 不在 `systemd-journal` 组），没验证。

对照：同目录 `dao-progress-watch.timer` 本轮 `NEXT=18:33`（已过）、`Last=18:13`，发现面还在跑。

### 4. 现场：今天仍有 incomplete，其中含「pi turn stalled past 30 minutes」

`/home/orca/.mirasim/sessions` 本轮扫 `record.json`：566 份，`runState` 计数 `incomplete=293` / `completed=266` / `running=7`。2026-09-07 当天更新的 incomplete 140 份。死因 Top：`Selected model is at capacity` 120、`stalled past 30` 105（全量，含跨天）、当天树面工人侧包括 `dao-1116`（7 份，最新 09:07Z，`pi turn stalled past 30 minutes`）、`dao-1122` / `dao-1125` / `dao-1024-2`。

这些会话按第 2 节的规则是「活人」，对账循环不会重派；按第 3 节垫片没在跑，也不会被推一把。progress-watch 只叫醒帅位（`host/machine/systemd/dao-nudge-stalled.service:13-16` 自己写的分工），帅位不在就整夜没人动手——正是装垫片的那次提交要堵的洞。

本轮没对某一棵树跑 `nudge-stalled.mjs --go`（硬边界不许动手）。没验证这些 incomplete 此刻推一句是否还能活；2026-09-06 五个工人推完回 running 的记录写在垫片文件头，不是本轮复测。

### 5. 不是已经报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-07-nudge装了却disabled-⑱看不见.md` | ⑱ 的取数面把 disabled 读成绿；当时 #1056 还开着，垫片按设计该跑 | #1104 已让 ⑱ 去扫 `/etc`；本条是 #1056 **关了之后**，接班人根本不做「推一把」，垫片也没退役也没启用。闸绿不绿不是本条 |
| `2026-09-06-屏面指纹层整层退役.md` | stall 发现面整层删了 | 发现面换成 progress-watch 还在；缺的是处置 |
| `2026-09-06-board-gc与stall仍问orca.md` | 采样问错真相源 | 本条采样已经走 mirasim record；缺的是动作接线 |
| `2026-09-06-四套单元未装-⑳把没装判没查成.md` | 仓有机无 | nudge **文件在** `/etc`，是 disabled |

两处规矩打架：单元文件 / NEW-MACHINE / #1056 登记评论说「合了就退役」；PR #1057 说「退役是下一跳，本单不接推一把」。关单脚本只认「署了 + 已合 + check 绿」，于是下一跳被关单一起带走了。

## 建议的最小改造

删掉「`routeSilent` 写了就算接上了」这一层。要么指挥官每轮对 `scanLiveness` 的 silent 清单调 `routeSilent`，`nudge` 走现成的 `nudge-stalled.mjs`（或同一条 send），要么把垫片 timer 重新 enable 并删掉「随 #1056 退役」那句——两边只留一条腿。`isLiveSession` 把 incomplete 当活人可以保留（避免抢树），但必须有第二只手去推；否则「当活人」=「没人理」。#1056 已关，下一跳要另开单，别再写进一张已经 CLOSED 的单的正文里指望有人看到。

本轮不动手。启用 timer 要 root；把 `routeSilent` 接到指挥官是改代码，都越巡检边界。
