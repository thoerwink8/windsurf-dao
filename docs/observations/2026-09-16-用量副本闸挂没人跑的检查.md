---
status: new
---

# 机制巡检：#1231 把「特权副本跟仓内不一致就红」写进了没人跑的 server-check 第 24 格；活副本仍是 09-11，采集每 5 分钟 exit 2

## 结论

#1231（`61db0d895`，19:32 CST）要修两件已经报过的事：90 秒墙钟把导出杀掉，以及 root 导出跑 09-11 旧副本。合进去的是仓内单元改 300 秒、装机脚本改成按 import 闭包拷文件、还有一道 `checkUsageInstallCopy`。本轮现场：这道闸对着活机器当场 `red`（缺 `execution-catalog.mjs` / `provider-probe.mjs` / `smol-toml.cjs`，另两份字节旧）。它只挂在 `server-check.mjs` CHECKS 第 24 格；`dao-check` / 指挥官盘点 / `land.mjs` 0 处调用。server-check 这台机器上没人跑（09-06 已报）。测试把接线锁在 server-check 文本上，测绿。`/etc` 两份单元仍是 09-11 的 `TimeoutStartSec=90`；`/usr/local` 副本 mtime 仍 09-11 23:16。root 导出本轮 10 秒 success，但 inbox `complete:false published:0`；orca 采集器跑仓内新码，`status: inbox_export_incomplete`，exit 2，进 `--failed`。闸绿在「函数写了、测试锁了文本」，活腿还在每 5 分钟红一次。

## 证据

本轮 2026-09-16 22:24–22:35 UTC（= 06:24–06:35 CST），身份 `uid=999(orca)`，未用 sudo。`HEAD` 开工时 `b97b4e720`，本条落盘时 `f002d355e`（本轮前一条巡检观察已推）。与 `origin/master` 快进关系未再分叉。未改 `docs/observations/` 以外的文件。未 `systemctl` 启停。

### 1. 闸对着活数据是红；会跑的检查 0 处调用它

```
$ git log -1 --format='%h %ci %s' 61db0d895
61db0d895 2026-09-16 19:32:27 +0800 [cc] fix(usage): 用量状态与故障分开，导出有界增量且装机跟 import 闭包 (#1231) (#1301)
```

`scripts/install-execution-usage.sh:21-23` 自己写：「Hand-writing two files left execution-catalog.mjs behind and the live copy could not be reinstalled (#1231).」然后按 `usageExportInstallFiles` 拷闭包。本轮没人跑过这条装机脚本——副本目录 mtime 仍是 `Sep 11 23:16`。

本轮直接调仓内 `checkUsageInstallCopy()`（读 `/usr/local/lib/dao-execution-usage` vs 仓内 `scripts/`，不出网）：

```
{
  "state": "red",
  "detail": "用量特权副本与仓内不一致：缺 lib/execution-catalog.mjs、lib/provider-probe.mjs、lib/smol-toml.cjs，旧 execution-usage-export.mjs、lib/execution-usage.mjs。装：sudo bash scripts/install-execution-usage.sh"
}
```

`md5sum` 对照（本轮稍早）：

```
45849f9f…  /usr/local/lib/dao-execution-usage/lib/execution-usage.mjs   2026-09-11 23:16:10 root:root
261d08b9…  scripts/lib/execution-usage.mjs                               2026-09-16 19:32:37
95561a22…  /usr/local/lib/dao-execution-usage/execution-usage-export.mjs 2026-09-11 23:16:10
6d16b5c9…  scripts/execution-usage-export.mjs                            2026-09-16 19:32:37
MISSING    /usr/local/.../lib/execution-catalog.mjs
```

接线：

```
scripts/server-check.mjs:1434  ['(24) 用量特权副本跟仓内 import 闭包一致（#1231）', checkUsageInstallCopy]
tests/server-check.test.js:260  'CHECKS (24) 用量特权副本走 checkUsageInstallCopy'
```

`grep checkUsageInstallCopy|judgeUsageInstallCopy scripts/dao-check.mjs scripts/lib/commander-inventory.mjs scripts/land.mjs`：0 行。

`systemctl list-timers --all` / `list-unit-files` 本轮仍 0 行 `server-check`（与 `2026-09-06-server-check无心跳.md` 同形，不另报那条）。dao-check / land / 盘点跑了也不会问这一格。

### 2. 仓内单元 300 秒；机器上仍 90 秒。导出 success 但 inbox 不完整；采集 exit 2

仓内 vs `/etc`（`diff` 本轮原文，/etc mtime `2026-09-11 23:16`）：

```
TimeoutStartSec=300    host/machine/systemd/dao-execution-usage.service
TimeoutStartSec=90     /etc/systemd/system/dao-execution-usage.service
TimeoutStartSec=300    host/machine/systemd/dao-execution-usage-export.service
TimeoutStartSec=90     /etc/systemd/system/dao-execution-usage-export.service
```

活单元 `TimeoutStartUSec=1min 30s`。`dao-sync` 只拉代码；`scripts/server-sync.sh` 的 `BOT_PATHS` 仍只有飞书四条路径，0 处 `install-execution-usage`。

本轮 06:33 这一拍：

```
dao-execution-usage-export.service
  ExecMainStartTimestamp=Thu 2026-09-17 06:33:03 CST
  ExecMainExitTimestamp=Thu 2026-09-17 06:33:13 CST
  Result=success  ExecMainStatus=0   User=root
  ExecStart=... /usr/local/lib/dao-execution-usage/execution-usage-export.mjs

dao-execution-usage.service
  ExecMainStartTimestamp=Thu 2026-09-17 06:33:14 CST
  ExecMainExitTimestamp=Thu 2026-09-17 06:33:37 CST
  Result=exit-code  ExecMainStatus=2   User=orca
  ExecStart=/usr/bin/node /srv/projects/windsurf-dao/scripts/execution-usage.mjs --collect ...
```

导出 10 秒跑完，不是 90 秒 SIGTERM（那一档 09-16 已报；本轮墙钟没咬到）。inbox 本轮原文：

```
/var/lib/dao-execution-usage/root-inbox/manifest.json
  rows: 40429  complete: false  published: 0
  exportedAt: 2026-09-16T22:33:13.880Z
```

采集器吃这份 inbox，`~/.dao/execution/usage/collection.json` 06:33:22：

```
complete: false  committed: 12  imported: 0  duplicates: 46360  sources: 1899
gaps: []
status: ["inbox_export_incomplete", "source_scan_pending"]
collectedAt: 2026-09-16T22:33:22.516Z
```

`scripts/execution-usage.mjs:49-56`：`--collect` 时 `partitionUsageCodes(...).gaps` 非空就 exit 2。新码把「inbox 不完整」收成 status/fault；旧导出口仍写出 `complete:false published:0` 的 4 万行 inbox。两边协议对不上的结果就是：导出绿灯、采集每 5 分钟进 `--failed`。

`systemctl --failed` 本轮有 `dao-execution-usage.service`。failed-units 闸把「配对 timer 响过」收成 flaky 绿（09-12 已报），land 推之前这一格不挡。本条对象不是那一格的 SKIP，是 **#1231 的新闸看见红却没人跑，所以采集还在每拍 exit 2**。

没验证：journal 里 06:33 采集器 stderr 原文（orca 读不了 journal）。按 status 字段和 exit 2，出口是脚本自己的采集故障，不是 SIGTERM 15。

### 3. 不是已报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-15-用量root副本冻在旧归属.md`（处置：#1051） | 副本把 grok 记成 openai；当时导出 8 秒 success | 归因函数仍旧是真的；本条是 **#1231 专门为这件事写的闸已经红了，挂在没人跑的检查上** |
| `2026-09-16-用量Timeout90秒杀掉导出.md`（处置：#1231） | 当时导出被 90 秒 SIGTERM，采集从 23:29 起没写完 | 本轮导出 10 秒 success，没被墙钟杀；仓内已经改 300 秒，**机器没装**。对象是接线，不是这一拍的 timeout |
| `2026-09-06-server-check无心跳.md` | server-check 22 格没人跑 | 当背景：第 24 格是 #1231 新挂上去的。不另报「没人跑」本身 |
| `2026-09-14-1226处置进master代码没进.md` | 处置写进 master，PR 还没合 | #1231 **已经合进 origin/master**，缺的是装机和会跑的人去调闸 |

## 建议的最小改造

删掉「把副本一致性闸写进 server-check + 测试锁 CHECKS 文本 = 特权副本已经跟 HEAD 对齐」这一层。

`dao-sync` 快进到 `scripts/lib/execution-usage.mjs` / 导入口 / 用量单元文件时，跑一次已经写好的 `install-execution-usage.sh`（它本就是为 #1231 闭包拷贝存在的），装完 `TimeoutStartUSec` 不是 90 秒、副本 md5 对得上才算这一轮成功。闸接到已经在跑的盘点或 dao-check（本机路径、不出网），红了挡 land 推送。不要再给 server-check 加第 25 格——09-06 就报过它没人跑。
