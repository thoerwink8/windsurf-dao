---
status: done
---

# 机制巡检：#1339 把「引用 CI、不要自己重跑」的优先证据写成裸 `gh pr checks`，审官环境里这条命令确定性失败

处置：本 PR（review-standard 第 1/3 条改走 node scripts/gh-as.mjs reviewer）

## 结论

#1339（`6e82c4d40`，今天 14:57）要把审官 158 秒重跑全量、票落不到 PR 上的死循环拆掉：标准页第 3 条改成「优先引用 CI，不要自己重跑」。合进去的只有 `host/skills/dispatch/review-standard.md` 一页。优先证据写的是裸 `gh pr checks <PR>`；第 1 条取 head 仍是裸 `gh pr view`。审官书 09-13 已经写明：审官跑在 mirasim 里，`GH_CONFIG_DIR=/var/empty`，**不许裸 `gh`**，裸 `gh pr view` 读不到 PR——当时就是每个审官开工第一步停手。本轮这个巡检会话继承同一份空目录、没 `GH_TOKEN`：裸 `gh pr checks` / `gh pr view` 两次都是 exit 4（`set the GH_TOKEN environment variable`）。同一条 `node scripts/gh-as.mjs reviewer -- pr checks 1339` 能列出 CI 绿和 run URL。标准页教的那条优先路在审官环境走不通；退路正是「CI 没跑 / 对结论存疑 → 自己跑」——#1339 要删的那 158 秒。dao-check 分级闸只扫 P1/P2/P3 四个字，0 处核「标准页的 gh 命令是不是走封装」。

## 证据

本轮 2026-09-17 18:23–18:35 CST，身份 `uid=999(orca)`，未用 sudo。`HEAD=f8ce55bb4`（`[cc] docs: 盘面排障产物归位…… (#1351)`），与 `origin/master` 相同。本进程：`GROK_AGENT=1`，父进程 `mirasim-server`（MainPID=3953318，`Environment=GH_CONFIG_DIR=/var/empty`）。未改 `docs/observations/` 以外的文件。未开单、未评 PR、未启停单元。

### 1. 今天合进去的优先证据是裸 `gh`；审官书禁止同一条路

```
$ git log -1 --format='%h %ci %s' 6e82c4d40
6e82c4d40 2026-09-17 14:57:18 +0800 [cc] fix(审查标准): 第 3 条改成引用 CI 证据——审官重跑全量是走不到投票的主因 (#1339)
```

`git show --stat`：只改 `host/skills/dispatch/review-standard.md`（+13 / −2）。审官书、士兵书、`gh-as.mjs`、测试 0 处同改。

标准页 `:9` 与 `:15-18` 原文：

```
1. **审的就是这份代码**：`gh pr view <PR号> --json headRefOid` 取 A，……
3. **检查真跑过**：……证据二选一，**优先前者**：
   - **CI 已在本 commit 跑过**：引用 `gh pr checks <PR>` 的结论与 run URL 即可，**不要自己重跑**。
     本仓 `.github/workflows/check.yml` 跑的是 `dao-check.mjs --all-tests`（显式全量），
     与审官自己跑的默认档覆盖相同。
   - CI 没跑 / 没有 CI / 对结论存疑：自己在被审 commit 上跑，正文贴**命令 + 输出末行**。
   两种都没有 = 没跑。
```

审官书 `host/skills/dispatch/templates/reviewer-book-mirasim.md:28-37` 原文（09-13 实咬之后的现役文字）：

```
被审对象 = PR 的最新 HEAD 与 diff（**读也要走封装**）：
`node scripts/gh-as.mjs reviewer -- pr view <p= 的 PR号> --json headRefName,headRefOid,mergeable,files`。
……
> **读 PR 也必须走 `gh-as.mjs reviewer`，不许裸 `gh`。** 你跑在 mirasim 服务端里，那个 unit 设了
> `GH_CONFIG_DIR=/var/empty`（挡 `~/.config/gh` 的个人登录），所以裸 `gh pr view` 必然报「没有凭据」——
> 而那不是 GitHub 或网络的问题，是绕过了封装。2026-09-13 实咬：本页第 28-29 行原先教的就是裸 `gh pr view`，
> 于是每个审官开工第一步就读不到 PR、当场停手报「没查成」——写操作包了封装、读操作没包，是同一件事的两半。
> `gh-as.mjs` 用 GitHub App 的 token，不读 hosts.yml，在同一个空配置目录下**实测正常**。
```

同页 `:31` 把质量清单指到标准页，「本框架不复制」。审官必须两份都读。书修了 `pr view`，没写 `pr checks`；标准页今天把 `pr checks` 写成优先证据，仍是裸 `gh`。

照标准走优先路 = 裸 `gh pr checks`。照书走 = 不许裸 `gh`，而书上没有 `pr checks` 的封装写法。两份都读 = 第 3 条优先路与「不许裸 gh」互相打架。

### 2. 本轮审官同款环境下，裸 `gh` 确定性失败；封装能列出 CI

本进程环境（与审官同父）：

```
GH_CONFIG_DIR=/var/empty
GH_TOKEN 未设
HOME=/home/orca  uid=999(orca)
GROK_AGENT=1
```

`/var/empty` 本轮 `ls`：`No such file or directory`。`mirasim-server.service`：`Environment=HOME=/home/orca MIRASIM_OPEN_BROWSER=0 GH_CONFIG_DIR=/var/empty`。

```
$ gh pr checks 1339 --repo thoerwink8/windsurf-dao
gh: To use GitHub CLI in automation, set the GH_TOKEN environment variable.
# exit=4

$ gh pr view 1339 --repo thoerwink8/windsurf-dao --json number
gh: To use GitHub CLI in automation, set the GH_TOKEN environment variable.
# exit=4

$ node scripts/gh-as.mjs reviewer -- pr checks 1339 --repo thoerwink8/windsurf-dao
check	pass	46s	https://github.com/thoerwink8/windsurf-dao/actions/runs/35174687941/job/105053667511
notify	pass	5s	https://github.com/thoerwink8/windsurf-dao/actions/runs/35192179780/job/105106993123
# exit=0
```

CI 在 #1339 这个 commit 上是绿的，run URL 也在。标准页要的那份证据存在。取数命令写成裸 `gh`，在这个环境取不到。按第 3 条退路，取不到 = 「CI 没跑 / 对结论存疑」→ 自己跑 `dao-check`。提交说明原文把这 158 秒写成「审官会话最大的单块耗时」「13 次都在投票前会话就结束」。

没验证：活着的审官会话里 `gh` 的 stderr 是不是同样这一句（本会话不许 attach 别人的会话）。按 `GH_CONFIG_DIR` 从同一份 `mirasim-server` 继承、且 `GH_TOKEN` 被卸掉（#792 同一把尺），裸 `gh` 没有第二条活路。本轮第二次 `gh-as reviewer -- pr checks` 曾回 `Post "https://api.github.com/graphql": unexpected EOF`（exit 1），第三次又是上面那张绿表。封装偶发 EOF 不是本条对象；对象是裸 `gh` 两次都是确定性 exit 4。

`.github/workflows/check.yml:73` 确是 `node scripts/dao-check.mjs --all-tests`。`scripts/dao-check.mjs:2126`：`--all-tests` 与默认相同（老调用留下的别名）。「CI 覆盖等于默认档」这一句当前成立。洞不在 CI 没跑，在取数命令在审官环境里跑不了。

### 3. 闸绿的那一层：书上有 P1/P2/P3；0 处核 gh 命令走哪条身份

`scripts/lib/review-tier-check.mjs:7-10` 自己写只做两件事：标准页有 P1/P2/P3、有人读 `review_rounds_max`。全文 0 处 `gh pr checks`、0 处 `gh-as`。`tests/` 全文 0 处 `gh pr checks`、0 处「不要自己重跑」。#1339 的提交说明把「规矩写了没人核」当成要防的 #1051；实际核的仍是四个字在不在。

### 4. 不是已报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-12-巡检会话继承mirasim的空gh目录.md` | 巡检会话 `git push` 被父进程的 `GH_CONFIG_DIR=/var/empty` 掐死 | 对象是 **#1339 今天写进标准页的优先证据命令**。空目录是审官跑裸 `gh` 必失败的原因，不是本条新报的洞 |
| `2026-09-16-红项分级P2仍挡合.md` | 分级闸绿、合门/返工仍只认 GitHub 红绿 | 对象是 P1/P2 档位没接到合门。本条是 **第 3 条取 CI 证据的那条命令** |
| 审官书自己写的 09-13 实咬 | 书里第 28-29 行曾经教裸 `gh pr view`，已经改成 `gh-as` | 书改了。**标准页第 1 条仍是裸 `gh pr view`，第 3 条今天又新写了一条裸 `gh pr checks`**。书不复制标准页，所以 09-13 那次修够不着今天这一笔 |

## 建议的最小改造

删掉「标准页写 `gh pr checks` / `gh pr view` = 审官能引用 CI」这一层。

第 1 条和第 3 条的取数命令与审官书同一条封装：`node scripts/gh-as.mjs reviewer -- pr view …` / `… -- pr checks …`。不要自己重跑的前提是这条命令在 `GH_CONFIG_DIR=/var/empty` 下能列出结论和 run URL——本轮可以。不要再加一条只扫「标准页出现 `gh pr checks` 这几个字」的测试：那会把今天这句裸命令锁成绿。
