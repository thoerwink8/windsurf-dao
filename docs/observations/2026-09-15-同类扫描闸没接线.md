---
status: new
---

# 机制巡检：#930 同类扫描闸测绿、审官书第 10 条写了，生产路径 0 处调用；今天合进去的 fix PR 缺段也没人拦

## 结论

#930（`24966b19`，09-14 05:04）要把「修一处必答全仓还有几处」从靠人撞见改成交卷前必答：写了纯函数 `gateSameClassScan`，测试对着函数绿，审官标准第 10 条和工人任务书都写了这一段。本轮现场：`gateSameClassScan` 在 `scripts/` 里除自身外 0 处 import；`handoff-check` / `commander` / `dao-check` 全文 0 处。测试锁的是「书上有这四个字」，不锁有没有人调这个函数。今天 18:12 合进 master 的 #1114（`fix(hub)`）和昨天的 #1232（`fix(lease)`）标题是修复类、正文没有「同类扫描」段，把原文喂给这个函数当场 `violation`——合进去了。闸看的那一层是测绿 + 书上有字，拦的那一层还是「审官想不想起这一条」。

## 证据

本轮 2026-09-15 18:24–18:37 CST，身份 `uid=999(orca)`，未用 sudo。`HEAD=bed97d37`，与 `origin/master` 相同。本进程：`grok agent --no-leader stdio`。未改 `docs/observations/` 以外的文件。

### 1. 合进去的是函数 + 测试 + 两份书，没有调用方

```
$ git log -1 --format='%h %ci %s' 24966b19
24966b19 2026-09-14 05:04:03 +0800 [cc] feat(review): 审官标准第10条「同类扫描」——修一处必答全仓还有几处 (#930)

$ git show --stat --format='' 24966b19
 host/skills/dispatch/review-standard.md            |  11 ++-
 .../dispatch/templates/soldier-book-mirasim.md     |   3 +-
 scripts/lib/dispatch/same-class-scan.mjs           |  58 ++++++++++++
 tests/same-class-scan.test.js                      | 105 +++++++++++++++++++++
 4 files changed, 175 insertions(+), 2 deletions(-)
```

`scripts/lib/dispatch/same-class-scan.mjs:6-8` 原文：

> 本闸只管**机械可判**的那部分：修复类 PR 的正文里，「同类扫描」段在不在、有没有给出命令与输出。
> 「扫得对不对、结论站不站得住」是判断题，归审官（review-standard.md 第 9 条）——
> 机器拦「没写」，人拦「写得不对」，两层不重叠也不互相顶替。

本轮 `grep`（`gateSameClassScan|same-class-scan`）于 `scripts/*.mjs` `scripts/lib/*.mjs`：命中只在 `scripts/lib/dispatch/same-class-scan.mjs` 自身。`scripts/dao-check.mjs` 0 处。`scripts/commander.mjs` 0 处。`scripts/handoff-check.mjs` / `scripts/lib/handoff-check.mjs` 0 处。`gateSameClassScan` 的调用点只在 `tests/same-class-scan.test.js`。

`tests/same-class-scan.test.js:90-103` 锁的是现役文件里有「同类扫描」四个字（`review-standard.md` + `soldier-book*.md`），断言原文自己写「否则只能靠审官判红后返工」。0 处要求某个生产脚本 `import` 这个函数。测绿只证明书没丢、函数对着夹具能分四态。

### 2. 今天合进去的修复类 PR，函数当场判红，合并闸没问

本轮 `GH_NO_COLOR=1` 取 PR 正文，喂给仓内这个函数（未发模型请求）：

| PR | 标题 | mergedAt | 正文有「同类扫描」 | `gateSameClassScan` |
|---|---|---|---|---|
| #1114 | `[cc] fix(hub): 日报卡脚注从 note 换成 markdown……` | 2026-09-15T10:12:43Z（= 18:12 CST，本轮前 12 分钟） | 否 | `violation`，缺「同类扫描」段 |
| #1232 | `[cc] fix(lease): 失败的清理不再重置宽限窗` | 2026-09-14T05:18:16Z | 否 | 同上 |

#1114 标题命中 `FIX_TITLE_RE`（`^\[[a-z]+\]\s*fix\b`）。正文有目标 / 验收标准 / 机制判定 / 进展，没有「同类扫描」段。函数返回：

```
{"state":"violation","missing":["「同类扫描」段"],
 "reason":"修复类 PR 缺「同类扫描」段——审官标准第 9 条：修一处就交卷，同类问题换个地方再咬"}
```

它还是合进了 `origin/master`（本轮 `HEAD=bed97d37` 就是这张单）。指挥官合 PR 走 `handoff-check --gate merge`（`scripts/commander.mjs:64,1090` 只复用 `judgeBaseFreshness` 那一档）。`handoff-check.mjs` 头注写明核的是「本分支与 master / 与远端的关系」，`--body-file` 只给删除说明用，不跑同类扫描。

同一次抽查里 #1283 / #1279 / #1261 正文**有**这段——工人任务书有时能让人写上。有人写 ≠ 没写会被拦。没写的两张照样合。

文件头和测试把这条写成「审官标准第 9 条」；现役 `host/skills/dispatch/review-standard.md:40` 第 9 条是交卷闸，同类扫描是第 10 条。编号对不上，调用方也没接上——同一份提交只把函数和书放进仓，没接到已经在跑的那几道闸。

### 3. 不是已报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-14-播报闸没接线.md` | `decideBroadcast` 测绿、生产 0 处 import | 对象是播报；本条是 **#930 同类扫描闸**同样的「函数在、书在、没人调」，今天还有合进去的 fix PR 当样本 |
| `2026-09-07-审官撞满载时的换人机制是死代码.md` | 满载换人函数没人调 | 对象是换人；本条是交卷正文闸 |
| `2026-09-07-交卷闸把机械对齐变成审官轮次.md` | handoff-check ① 被审官拿来判红 | 交卷闸在跑，只是 ① 不该进审官轮次；本条是 **另一条机械闸根本没接到交卷/合并** |
| `2026-09-07-回流提示层钉退役士兵书.md` | 回流段提示钉在退役书上 | 本条测试已经改钉 `soldier-book-mirasim.md`；缺的不是书，是调用 |

## 建议的最小改造

删掉「写了 `gateSameClassScan` + 测试绿 + 书上有字 = 第 10 条已经在拦」这一层。

把它接到已经在跑的交卷/合并闸上（`handoff-check` 已经吃 `--body-file`，指挥官合 PR 已经跑 `--gate merge`）：修复类、缺段 = 机械红，合不进去。不要再加一条只测函数的测试，也不要把审官书当唯一防线——#1114 刚证明审官书拦不住。编号跟现役标准对齐成第 10 条，别留着文件头写第 9 条。
