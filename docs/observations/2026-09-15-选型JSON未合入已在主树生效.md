---
status: done
---

---
status: new
---

# 机制巡检：#1274 还开着，生产主树工作区已经按它的并发上限在派工

## 结论

`docs/model-routing.json` 是选型真相源，头注写明「算法每次读工作区，本地改即生效」。#1274（`dao-caps-measured`，`7bad9e8d`）要把 6 条在役腿的 `并发上限` 从数字改成「不限」，PR 仍 OPEN、未合进 `origin/master`。同一份字节已经躺在生产主树 `/srv/projects/windsurf-dao/docs/model-routing.json` 的未提交工作区里。指挥官 `act` 的 `WorkingDirectory` 就是这棵主树，本轮 00:31 跑完的那次已经读到「不限」。闸看的那一层是工作区这份脏文件，会绿；`origin/master` 上 `composer-2.5` 仍是 1，整条 `mirasim` 渠道按 `Math.min` 被压在并发 1。未合入的改规则已经在生产生效，没有任何检查问工作区 vs HEAD。

## 证据

本轮 2026-09-15 00:24–00:35 CST，身份 `uid=999(orca)`，未用 sudo。`HEAD=4a4c4cdf`，与 `origin/master` 相同。本进程：`grok agent --no-leader stdio`，父进程 `mirasim-server/0.0.310`。开工时 `git status` 就有 ` M docs/model-routing.json`（mtime `2026-09-14 23:38:37 +0800`）；本轮只往 `docs/observations/` 写，没有把这份脏文件加进提交。

### 1. PR 未合，生产工作区已经是那份字节

```
$ env -u GH_CONFIG_DIR gh pr list --head dao-caps-measured --state all --json number,state,mergedAt,title
[{"mergedAt":null,"number":1274,"state":"OPEN",
  "title":"[cc] fix(并发): 在役腿并发上限按实测重写——静态数字换成「负载准入兜底」"}]

$ git merge-base --is-ancestor 7bad9e8d origin/master; echo $?
1

$ git log -1 --format='%h %ci %s' 7bad9e8d
7bad9e8d 2026-09-14 23:46:56 +0800 [cc] fix(并发): 在役腿并发上限按实测重写——静态数字换成「负载准入兜底」
```

`7bad9e8d` 只改 `docs/model-routing.json`（45 行：+28 / −17）。工人树 `/home/orca/mirasim-worktrees/windsurf-dao/dao-caps-measured` 干净、跟踪 `origin/dao-caps-measured`。该树里这份文件与生产主树工作区 **md5 相同**（`3c4001a6ffac3ce32a23c00d43a50084`）；`HEAD:docs/model-routing.json` 是 `38cab09d015235892a8e8f2fefe0fcca`。

本轮直接对比 6 条在役腿的 `并发上限`：

| 腿 | HEAD（origin/master） | 生产工作区 / #1274 |
|---|---|---|
| `claude-fable-5@mirasim/mirasim` | 5 | 不限 |
| `claude-opus-5@mirasim/mirasim` | 5 | 不限 |
| `composer-2.5@cursor-native/mirasim` | **1** | 不限 |
| `gpt-5.6-luna@mirasim/mirasim` | 5 | 不限 |
| `gpt-5.6-sol@mirasim/mirasim` | 5 | 不限 |
| `gpt-6-astra@mirasim/mirasim` | 5 | 不限 |

没验证：是谁在 23:38 把这份字节写进主树工作区（工人树自己的提交在 23:46，主树文件更早 8 分钟）。按 md5，写进去的就是 #1274 那份。

### 2. 生产路径读的就是这份脏文件；渠道上限已经从 1 变成不限

`scripts/lib/model-routing-json.mjs:3,10,13-16`：

```
// 2026-08-22 拍板：算法每次读工作区 docs/model-routing.json，本地改即生效。
export const ROUTING_JSON = join(resolve(import.meta.dirname, '..', '..'), 'docs', 'model-routing.json');
export function loadRoutingJsonRaw(file = ROUTING_JSON) {
  …
  doc = JSON.parse(readFileSync(file, 'utf8'));
```

`scripts/commander.mjs:623,638`：`const raw = loadRoutingJsonRaw();` → `channelCaps = buildChannelCaps(routingLegs)`。单元：

```
$ systemctl show commander-act.service -p WorkingDirectory,ExecStart,ExecMainStartTimestamp,ExecMainExitTimestamp,Result
WorkingDirectory=/srv/projects/windsurf-dao
ExecStart=… /srv/projects/windsurf-dao/scripts/commander.mjs act
ExecMainStartTimestamp=Tue 2026-09-15 00:31:00 CST
ExecMainExitTimestamp=Tue 2026-09-15 00:34:49 CST
Result=success
```

`scripts/lib/channel-concurrency.mjs:85-89`：`供应商` 或 `执行侧` 为 `mirasim` → 渠道键 `mirasim`。`composer-2.5@cursor-native/mirasim` 的执行侧是 mirasim，和 claude-opus / gpt-5.6-sol / luna / grok-4.6@xai-native 进同一条渠道。同文件 145 行：同一渠道有显式数字时取 `Math.min`。本轮直接调 `buildChannelCaps` + `validateLegCaps`：

```
HEAD  { states: { mirasim: "capped" },    caps: { mirasim: 1 },    pending: 0, bad: 0 }
WT    { states: { mirasim: "unlimited" }, caps: { mirasim: "不限" }, pending: 0, bad: 0 }
```

HEAD 上 grok / sol / luna 的 relay 腿已经写着「不限」，但 `composer-2.5` 的 1 把整条 `mirasim` 渠道压成 1。工作区把这条 1 拿掉之后，闸对这条渠道不再限流。`dao-check` 的并发上限格读的也是工作区：本轮 `loadRoutingJsonRaw()` → `inService 11 / pending 0 / bad 0 / mirasim=不限`，会绿。`scripts/dao-check.mjs` 全文 0 处把 `docs/model-routing.json` 的工作区跟 `HEAD` 或 `origin/master` 对。

`dao-sync.service` 本轮 00:31:00–00:31:01 `Result=success` `ExecMainStatus=0`。`scripts/server-sync.sh` 的 `git merge --ff-only` 不碰未提交改动；脏文件还在，`git status --porcelain` 仍是 ` M docs/model-routing.json`。

### 3. 不是已报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-14-1226处置进master代码没进.md` | 观察文件写了「处置：#1226」，修法还在 OPEN 的 PR，机器上多了一只仓里没有的钟 | 对象是 skills-heal 单元；本条是 **选型 JSON 未合入却已经在生产工作区生效** |
| `2026-09-13-收件箱不查未推送.md` | 观察已经 commit 没 push | 本条是决策字段 **连 commit 都没有**，读路径还把它当真相 |
| `2026-09-13-控制面稳定来源指回工作树.md` | 工人树的 git hooksPath 指回自己 | 对象是钩子路径；本条是指挥官 / 派工读选型表 |
| `2026-09-07-审官撞满载时的换人机制是死代码.md` | 满载换人函数没人调 | 对象是换人；本条是渠道上限被未合入的脏文件放开 |

## 建议的最小改造

删掉「生产主树工作区 = 已生效的选型真相源」这一层。指挥官 `act`、派工门、dao-check 的并发上限格，读 `HEAD:docs/model-routing.json`（或 `origin/master` 那份），不要读未提交的工作区。正在改这条 JSON 的 worktree 自己读自己的工作区——那是 #1274 该待的地方。

不要在主树上给「允许脏着的决策文件」开白名单：#1274 自己还没合，composer 那条 1 已经不在生产闸里了。

处置：#1051（同一形状「闸看的那一层 ≠ 跑的那一层」，归类单；2026-09-16 帅位盘点，判据 review_rounds_max 未接线见 #1227）
