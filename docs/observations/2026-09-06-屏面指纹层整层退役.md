---
status: done
---

# 卡死发现：屏面指纹整层退役，两个审官撞额度顶等人拍

留给另一位帅位（marshal-0906 那条会话）与后来者。用户 2026-09-06 拍板，
代码走 **draft PR #1067** 等审官（`land.mjs` 在编排态拒绝派生分支直推 master——
这条 observation 单独上 master，因为收件箱不在 master 上等于没写）。

## 一句话

`agent-stall-watch` 那一层（702 行屏面指纹 + 自动换审官）整层删了，卡死发现只剩
`progress-watch` 的盘面推进量。你写的 `nudge-stalled.mjs` **不受影响**（它自己读
mirasim 的 record.json，不 import 被删的东西），只把文件头那个失效指针改对了。

## 你可能正好在意的三件事

1. **dao-agent-stall.timer 要卸。** 你 20:21 把它 enable + start 回来了，但它 20:32 那轮
   `ExecMainStatus=2`（没查成）——mirasim 采样面在 systemd 环境下拿不到 4316 的 token。
   现在脚本本体已删，卸载动作放进了 `scripts/install-progress-watch.sh`（装新的顺手卸旧的）。
   主树同步到 `1672f5a1` 之后跑一次那个安装脚本就切完了。

2. **你的实证改了我的判据认知。** 「卡住的工人没死，是跑完一轮在等一句话」——这条让
   「静默 N 分钟 = 死」的老判据彻底站不住，也是删掉屏面指纹层的另一条理由。
   两个发现面互补，别合并：progress-watch 看盘面对象（PR / issue / 复审票），
   看不见「某个会话在等话」；record.json 才知道。已写进两边的文件头。

3. **自动换审官这条路现在没有执行者。** 它整个活在被删的 `switchReviewer` 里。
   这与 memory `reviewer-dead-no-auto-recreate`（#740 拍板：审官死了报出来，重拉是人做的）
   和 `quota-hit-is-a-decision-not-a-fallback`（撞额度顶要停下来报，不许自己换通道）
   方向一致，不是净损失——但要知道它没了。

## 当下盘面：两个审官卡在额度 —— 写完 40 秒就被另一位帅位处置了

**处置：f2fb0607（另一位帅位，20:51）**。它查得比我深：不是「等人拍」，根因是
① `~/.dao/provider-breaker.json` 三条熔断 open 的理由（健康表 red）早已翻绿，熔断器只等
cooldown 不回头看健康表；② `judgeReviewerSessionReuse` 只看 `view.phase` 不看 mirasim 写的
`runState: incomplete`，于是死审官被判在役、复用、PR 永远合不了；③ `nudge-stalled` 原来只认
`dao-<N>` 工人树，漏了 `dao-review-pr-<N>`。熔断解开 + 推一把后两个审官 60 秒内回 running。

下面是我 20:45 写的原文，留作判例（我的判断停在「撞额度顶不许自动换通道」那条 memory 上，
对，但不完整——真正卡住的是熔断器判据失效，那不是「换通道」问题）。

`~/.mirasim/sessions` 逐树取最新记录（2026-09-06 20:45 实测）：

| 树 | 执行体 | 状态 | 静默 | 原因 |
| --- | --- | --- | --- | --- |
| dao-review-pr-1018 | codex | incomplete | 66 min | `Selected model is at capacity. Please try a different model.` |
| dao-review-pr-1040 | codex | incomplete | 38 min | 同上 |

按上面两条 memory，**不自动换模型、不自动重拉卡**，报到这里等人拍。
`nudge-stalled.mjs` 对它们无效：这不是「等一句话」，是上游没容量。

顺带看到但没动：`dao-1012` 与 `dao-1012-2` 是同一个 issue 的两棵树，
九棵 `dao-review-pr-88x/89x` 审官树 completed 已 45 小时——像是该收的树，归 board-gc（#1065）。

## 落点

- PR #1067（draft，待审官）：`1672f5a1` 删整层 + progress-watch 接手
  （补丁链全表在该 commit 正文，`chain:agent-stall#7`）、`47baed92` 修 nudge-stalled 的失效指针
- server-check ⑮ 换成守 `dao-progress-watch.timer`，三个退役件任一还在就红
- 合并前 `dao-agent-stall.timer` 仍在机器上每 15 分钟 exit 2；卸载动作在
  `install-progress-watch.sh` 里，合并后跑一次那个脚本就切完
