---
status: done
处置: "#1337（合进 master 不等于生产在跑）——本条是甲：长连接不重启跑旧码"
---

# 机制巡检：#1316 假活补丁合进 master 的同一分钟磁盘更新了，活着的事件桥还是 12:14 那份本地 isChildAlive

## 结论

#1316（`5bf174dbe`，18:26:44）要把「外部 SIGTERM 之后 `isChildAlive` 仍当子进程活着、重连被 skip」改掉：判据从 `exitCode === null && !killed` 改成只认 `exitCode` / `signalCode`。测试对着**磁盘上的新文件**绿（不许桥脚本里再留一份本地假活函数）。`dao-sync` 18:31 已经把这份字节快进进生产主树。活着的 `dao-gh-events` 从 **12:14:35** 起没换过 PID（`NRestarts=0`），Node ESM 只在启动时 import 一次。12:14 那份代码里 `isChildAlive` 还是桥脚本自己的本地函数，认 `killed`、不认 `signalCode`——正是 #1316 要删的那一层。`dao-sync` 的重启名单只有飞书四条路径，事件桥是同类长连接，合了码不会 `try-restart`。闸 ⑳ 只比单元文件正文；事件桥那一格测绿锁的是 server-check 文本和磁盘源码，0 处问活 PID 的启动时刻是不是比它加载的脚本旧。

## 证据

本轮 2026-09-16 18:23–18:32 CST，身份 `uid=999(orca)`，未用 sudo。开工时 `HEAD=0915c848b`，随后 `dao-sync` 快进到 `5bf174dbe`（`git reflog`：`HEAD@{0}: merge origin/master: Fast-forward`），与 `origin/master` 相同。本进程：`grok agent --no-leader stdio`，父进程 `mirasim-server/0.0.310`。未改 `docs/observations/` 以外的文件。未向活着的事件桥发信号、未 `systemctl` 启停。

### 1. 磁盘是 18:26 的新判据；活进程是 12:14 的旧函数

```
$ git log -1 --format='%h %ci %s' 5bf174dbe
5bf174dbe 2026-09-16 18:26:44 +0800 [grok] fix(gh-events): 外部信号退出后按已死重连，不再假活 (#1316)

$ stat -c '%y %n' scripts/gh-event-bridge.mjs scripts/lib/gh-events.mjs
2026-09-16 18:26:49.033600221 +0800 scripts/gh-event-bridge.mjs
2026-09-16 18:26:49.033600221 +0800 scripts/lib/gh-events.mjs

$ systemctl show dao-gh-events.service -p MainPID,NRestarts,ExecMainStartTimestamp,ActiveState,Result
MainPID=1247369
NRestarts=0
ExecMainStartTimestamp=Wed 2026-09-16 12:14:35 CST
ActiveState=active
Result=success
```

状态文件 `~/.dao/gh-events.json`：`pid=1247369`，`startedAt=2026-09-16T04:14:35.789Z`（= 12:14:35 CST），`forward.restarts=0`。本轮 `node scripts/gh-event-bridge.mjs status`：`[ok] 事件桥在守着`，exit=0。通道这会儿是通的；通不等于吃到了 18:26 那份补丁。

#1316 提交说明原文：

> isChildAlive 只看 exitCode 和 killed，外部 SIGTERM 后两者仍是空/假，重连被 skip。
> 2026-09-16 故障演练：12:11:58 forward-exit，12:12:03 spawn-skipped。

活单元 12:14:35 拉起，比这次演练晚约 2 分钟——当时重启是为了从 skip 里爬出来，吃到的是 08:09 的 #1299（`6e40a8721` 是 `147b65f2c` 的祖先）。#1316 不是祖先：

```
$ git merge-base --is-ancestor 6e40a8721 147b65f2c; echo $?
0
$ git merge-base --is-ancestor 5bf174dbe 147b65f2c; echo $?
1
```

12:11 那份提交里，桥脚本自己还留着本地函数（`git show 147b65f2c:scripts/gh-event-bridge.mjs` 第 139–141 行）：

```
function isChildAlive(c) {
  return !!(c && c.exitCode === null && !c.killed);
}
```

磁盘上现在是 #1316 改过的（`scripts/lib/gh-events.mjs:328-333`）：

```
export function isChildAlive(c) {
  if (!c) return false;
  if (c.exitCode != null) return false;
  if (c.signalCode != null) return false;
  return true;
}
```

同文件 `:324-325` 自己写「外部 SIGTERM/SIGKILL：exitCode 仍是 null、killed 仍是 false，只有 signalCode 能说明已经退了」。`scripts/gh-event-bridge.mjs:53` 现在从 lib import `isChildAlive`；`:137` 再 export；本地 `function isChildAlive` 已经不在。活进程 12:14 起、中间 `NRestarts=0`，顶层 import 不会重跑。

没验证：不能把 1247369 的 V8 堆拆出来看它手里那份函数字节（orca 也读不了 journal 里 12:11 演练原文）。按 ESM 启动时求值 + 启动时刻早于 #1316 六小时 + 启动时刻所用的提交里仍是本地 `!c.killed`，活进程走的是旧函数。没验证：下一次外部 SIGTERM 会不会再 skip——本会话不许向这个单元发信号。

### 2. 快进发生了，重启名单没有事件桥；闸问的不是活 PID

`scripts/server-sync.sh:2-9` 自己写「合了 ≠ 生效」，然后把要重启的路径写成：

```
BOT_PATHS='^(scripts/feishu-triage\.mjs|scripts/lib/feishu-triage-core\.mjs|scripts/lib/plain-words\.mjs|host/skills/feishu-triage/)'
```

`:56-59` 命中才 `sudo -n /usr/bin/systemctl try-restart feishu-triage`。全文 0 处 `gh-event-bridge`、0 处 `dao-gh-events`。#1316 只改 `scripts/gh-event-bridge.mjs` / `scripts/lib/gh-events.mjs` / `tests/gh-events.test.js`，进不了这张名单。本轮 reflog 已经快进到 `5bf174dbe`，`try-restart dao-gh-events` 没发生——PID 仍是 1247369。

`scripts/dao-check.mjs` / `scripts/lib/commander-inventory.mjs` 全文 0 处 `BOT_PATHS`、0 处 `gh-event-bridge`、0 处 `dao-gh-events`。⑳（`scripts/server-check.mjs:972-1024` `collectUnitDriftPairs` / `classifyUnitDrift`）比的是仓内单元文件 vs `/etc` 正文（含 drop-in），不问 `ExecMainStartTimestamp` 相对源文件 mtime。事件桥的单元文件两边都停在 09 月（仓内 `host/machine/systemd/dao-gh-events.service` mtime `2026-09-08`，`/etc` 那份 `2026-09-09`），⑳ 这一格看不见脚本换了、进程没换。

`tests/gh-events.test.js:262-268` 把接线锁在 **server-check 的 CHECKS 数组**上（「re-export 了函数但没人调，等于没接线」）。同文件 `:759-768` 锁的是**磁盘源码**：

```
it('桥必须用 lib 的 isChildAlive；本地假活判据和 stop 忽略 signalCode 都不许还在', () => {
  const src = fs.readFileSync(.../gh-event-bridge.mjs);
  const lib = fs.readFileSync(.../gh-events.mjs);
  assert.match(lib, /export function isChildAlive/);
  assert.match(src, /isChildAlive/);
  assert.equal(/function isChildAlive/.test(src), false);
```

本轮这些文本都在，测绿。0 处要求 `dao-gh-events` 的 `ExecMainStartTimestamp` 晚于 `scripts/gh-event-bridge.mjs` 的 mtime。`tests/*.js` 全文 0 处断言 `BOT_PATHS` 含事件桥。

`/etc/sudoers.d` 本轮 `ls` 是 Permission denied，sudoers 白名单里有没有 `try-restart dao-gh-events` **没验证**。按 `server-sync.sh` 原文，就算白名单有这条，这份脚本也不会去调。

### 3. 不是已报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-14-飞书triage合了码没重启.md` | BOT_PATHS **有**飞书，try-restart **请求过**，被 `\|\| echo` 吞成 success，PID 从 09-12 开机没换 | 本条是 **重启名单根本没有事件桥**。今天 #1316 合进去不会触发那一行 try-restart |
| `2026-09-15-事件桥通道死了父进程仍绿.md` | 当时 `gh webhook forward` 子进程不在，ping 503 分钟没回来；脚本 mtime 还停在 09-06 | 本轮 ping 通、子进程在、status=ok。洞是 **修假活的补丁没进这个 PID**，不是通道此刻已断 |
| `2026-09-15-用量root副本冻在旧归属.md` | root 导出跑 `/usr/local` 冻住的副本；旁注 BOT_PATHS 只有飞书 | 对象是特权副本刷新。本条是 **长连接 PID**，oneshot 每轮重读文件，事件桥不重读 |
| `2026-09-14-1226处置进master代码没进.md` | 观察写了处置，修法还在未合入分支 | #1316 **已经合进 origin/master**，机器磁盘也是新字节，缺的是活进程 |

## 建议的最小改造

删掉「快进到仓内脚本 + 测试锁磁盘源码 = 长连接已经在跑新判据」这一层。

`dao-sync` 的重启名单跟「启动时 import 一次、不退出就不重读」的单元走：`scripts/gh-event-bridge.mjs` / `scripts/lib/gh-events.mjs` 变了，就 `try-restart dao-gh-events`。重启之后 PID 没变 / sudo 非零 = 这一轮非零，别 echo 成 success。不要再加一条只读文件文本的测试把 `isChildAlive` 锁成绿——那正是本轮测绿、活进程仍假活的那一层。

⑳ / dao-check 问活 PID 的启动时刻是不是比它加载的那几份脚本新；旧了就红。飞书那条 09-14 已经建议过同一问，事件桥今天证明名单外的长连接连「请求重启」都不会发生。
