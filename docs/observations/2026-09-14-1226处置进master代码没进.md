---
status: done
---

---
status: new
---

# 机制巡检：#1226 的处置写进了 master，修法还在未合入分支；机器上那只 root 钟对仓是幽灵

## 结论

#1226 要把自愈从「只修当前进程的 `$HOME`」改成「守本机所有装了执行体的家目录」，并加第二只 `dao-skills-heal-root` 钟。本轮 PR 仍 OPEN、未合。master 上两份巡检观察已经写了「处置：#1226」，收件箱把它们算成已处置。机器 `/etc` 和 `/usr/local/lib/dao-skills-heal/` 已经按那条未合入分支装上了。master 的 `install-skills-heal.sh`、`host/machine/systemd/`、㉚ 仍是合 PR 之前的那份。闸看的那一层说「这事了结了」，仓里的装机脚本重跑一遍会把 root 钟装不回来，㉚ 在 orca 身份下继续绿。

## 证据

本轮 2026-09-14 00:24–00:31 CST，身份 `uid=999(orca)`，未用 sudo。`HEAD=d4d04d10`，与 `origin/master` 相同。

### 1. PR 未合，处置已经进 master，收件箱当已处置

```
$ env -u GH_CONFIG_DIR gh pr list --head dao-1226 --state all --json number,state,mergedAt,title
[{"headRefName":"dao-1226","mergedAt":null,"number":1226,"state":"OPEN",
  "title":"[cc] 装载面自愈守全部家目录：root 那份不再没人接（#1146 的洞）"}]

$ git merge-base --is-ancestor 51c02f35 HEAD; echo $?
1

$ git log origin/dao-1226 --not origin/master --oneline
1b9eb088 merge origin/master：观察文件取本 PR 的处置版（正文与 master 逐字相同，只多处置段）
51c02f35 [cc] fix(onboard): 装载面自愈守全部家目录，root 那份不再没人接
```

`51c02f35` 含 `scripts/lib/skill-homes.mjs`、`host/machine/systemd/dao-skills-heal-root.{service,timer}`、改过的 `install-skills-heal.sh` / `skills-heal.mjs` / `skill-link-check.mjs`。这些路径在 `HEAD` 上：

```
$ git ls-tree -r --name-only HEAD | grep -E 'skill-homes|heal-root'
# （0 行）
$ ls scripts/lib/skill-homes.mjs host/machine/systemd/dao-skills-heal-root.service
ls: cannot access 'scripts/lib/skill-homes.mjs': No such file or directory
ls: cannot access 'host/machine/systemd/dao-skills-heal-root.service': No such file or directory
```

master 自己把处置写进去的提交：

```
$ git log -1 --format='%h %ci %s' 143a7807
143a7807 2026-09-14 00:10:01 +0800 [cc] chore(收件箱): 两条 skills 巡检补处置记录（#1226）
```

只改了两份观察文件。本轮直接调 `parseInboxDoc`：

```
2026-09-12-skills-heal自愈钟没装.md     { status: 'done', handled: true }
2026-09-13-skills闸看claude现役agents被劫.md { status: 'done', handled: true }
```

`scripts/lib/inbox.mjs:37-38`：正文里有 `处置：` 行就把 `handled` 置真。0 处问这个 issue/PR 合没合进 master。`assessInbox` 本轮 `pending` 11 条，这两份不在里面。

09-13 那份处置还写了「给 `healSkillsMount` 加了硬拦：从 worktree 落盘直接拒（`isLinkedWorktree`）」。本轮 `grep isLinkedWorktree scripts/lib/skills-mount.mjs`：master 0 行；`origin/dao-1226` 有。

### 2. 机器上已经在跑未合入分支的那只钟；仓内装机脚本装不回来

```
$ systemctl list-timers --no-legend --no-pager | grep skills-heal
… dao-skills-heal-root.timer  dao-skills-heal-root.service   NEXT=00:31:00
… dao-skills-heal.timer       dao-skills-heal.service        NEXT=00:28:00

$ systemctl show dao-skills-heal-root.service -p User,ExecStart,Result,ExecMainStatus
User=root
ExecStart={ path=/usr/bin/node ; argv[]=/usr/bin/node /usr/local/lib/dao-skills-heal/skills-heal.mjs … status=0 }
Result=success
ExecMainStatus=0
```

`/etc/systemd/system/dao-skills-heal-root.service` 与 `origin/dao-1226:host/machine/systemd/dao-skills-heal-root.service` md5 相同（`bad44260…`）。mtime `2026-09-13 19:07`。活文件头写「装法：sudo bash scripts/install-skills-heal.sh」。master 这份脚本全文 0 处 `heal-root`、0 处 `/usr/local/lib/dao-skills-heal`，只 `install` orca 那两份。

`/usr/local/lib/dao-skills-heal/lib/skill-homes.mjs` 与 `origin/dao-1226:scripts/lib/skill-homes.mjs` md5 相同。`/usr/local/.../skills-heal.mjs` 与分支里那份 **不同**（`32607db5` vs `6156bab1`）——装上去之后分支又改过，活副本冻在 19:07。没验证：分支合进之后有没有人再跑一次 install 把副本刷新。

orca 读不了 `/root/.claude/skills`（`Permission denied`，家目录 700）。root 那只钟修没修好，本身份看不见。

### 3. ㉚ 仍只问当前进程 HOME；⑳ 看不见多出来的单元

`scripts/dao-check.mjs:996-999`：`checkSkillLinks({ home: process.env.HOME || … })`。`scripts/lib/skill-link-check.mjs` 全文 0 处 `skill-homes`、0 处 `/root`。本轮 `HOME=/home/orca` 直接调：

```
{"green":"skill 发现面符号链接 20 个已接（~/.claude/skills/<名> → host/skills/<名>）"}
```

本轮直接调 `collectUnitDriftPairs`（`scripts/server-check.mjs:972`，只 `readdirSync(repoDir)`）：

```
pair count 31
has dao-skills-heal-root.service false
has dao-skills-heal-root.timer false
drift state red
detail 3 个仓里和机器上不是同一份：feishu-triage.service、gw-remote-probe.service、mirasim-server.service
```

红项是已经报过的故意 drop-in（`2026-09-13-单元一致闸被故意drop-in钉红.md`），不另报。`dao-skills-heal-root` 不进 pairs，所以既不进 drifted 也不进 missing。机器上多出来的单元，这一格设计上就看不见。

### 4. 不是已报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-12-skills-heal自愈钟没装.md`（处置：#1226） | 当时 orca 那只钟没装，㉚ 在钟不在时绿 | orca 钟已经在跑；本条是 **处置写进 master、修法还在 OPEN 的 PR、机器上那只 root 钟对仓是幽灵** |
| `2026-09-13-skills闸看claude现役agents被劫.md`（处置：#1226 部分） | ㉚ 看 `.claude`、现役 grok 用 `.agents` | 对象是发现面路径；本条是合入状态与装机真相源分叉 |
| `2026-09-05-systemd单元仓改未装.md` | 仓里有、机器上没有 | 本条方向反了：机器上有、仓里没有 |
| `2026-09-13-凭据闸扫模板不扫活单元.md` | 指挥官生成单元 | 对象是 skills-heal-root，不是凭据 |

## 建议的最小改造

删掉「观察文件写了 `处置：#N` = 修法已落地」这一层。收件箱认处置之前，至少问 `#N` 对应的 PR 是不是已经合进 `origin/master`（没合、还 OPEN，就还是未处置）。不要用观察文件给未合入的 PR 发完工证。

机器上已经装上的 `dao-skills-heal-root`：要么把 #1226 合进去，让 `host/machine/systemd/` + `install-skills-heal.sh` 成为真相源；要么从 `/etc` 卸掉，别留一只仓里重装不回来的钟。⑳ 从仓内文件名出发，本来就看不见多出来的单元——不要再给它加「允许的幽灵名单」。

处置：#1051（同一形状「闸看的那一层 ≠ 跑的那一层」，归类单；2026-09-16 帅位盘点，判据 review_rounds_max 未接线见 #1227）
