# 机制巡检：#888 回流的提示层钉在已退役的 orca 士兵书上，现役工人看不到
> 处置：#1168 回流提示层钉在退役士兵书上——指挥官方已开单

处置：#1168

## 结论

回流机制（#888）的三层发现面，提示层写在 `soldier-book.md` 交卷自查第 5 条；那本书顶部已经标「已退役」，工人被要求读 `soldier-book-mirasim.md`。现役这本书全文 0 处「回流」，自查第 5 条是「机制判定」。机械闸只抓「写了段没人接」，设计页把「压根没写」交给审官必核清单，而 `review-standard.md` 九条里也没有回流。于是提示层、动作层入口、审官清单三条发现面同时空着，只剩「有人碰巧写了段」才会红。

## 证据

本轮 2026-09-08 06:37 CST，身份 `uid=999(orca)`，未用 sudo。`HEAD=0c325f970`，与 `origin/master` 相同。

### 1. 现役工人读的书里没有「回流」

`AGENTS.md:1`：工人读 `soldier-book-mirasim.md`。指挥官 `scripts/commander.mjs:943` 的 `dispatchSpec` 也指这一本。

`host/skills/dispatch/templates/soldier-book-mirasim.md:36-51` 交卷前自查六条：测、dao-check、对照验收、handoff-check、机制判定、再 worker-done。第 5 条是机制判定，不是回流。

```
$ rg -n "回流" host/skills/dispatch/templates/soldier-book-mirasim.md
# exit 1，0 行
$ git log --all -S '回流' --oneline -- host/skills/dispatch/templates/soldier-book-mirasim.md
# 空：这本书从未出现过「回流」二字
```

### 2. 回流自问还在已退役的那本里，编号还被闸当现役引用

`host/skills/dispatch/templates/soldier-book.md:1-3`：

> **已退役。** 执行体是 mirasim。工人读 `soldier-book-mirasim.md`。本页只作判例档案，不要再注入、不要当现役闭环。

同文件 37-39 行仍是交卷自查第 5 条「回流自问（#888）」。

`scripts/lib/harvest-check.mjs:6` 注释仍写「士兵书自查第 5 条」。在现役书上第 5 条是机制判定；在退役书上才是回流。闸的测试 `tests/harvest.test.js` 不打开任何士兵书（`rg soldier-book tests/harvest.test.js` exit 1）。

设计页 `docs/decisions/2026-09-04-harvest-reflow.md:32`：

> 1. **提示层**：`host/skills/dispatch/templates/soldier-book.md` 交卷自查第 5 条（有通用产物 → 写段）。

#1115 / #1134 改了派工脊和指挥官指针，没改这一层。

### 3. 「压根没写」本来交给审官清单，清单也没有

同设计页 70-71 行：已知不覆盖「士兵压根没意识到产物通用」——「这层交给审官必核清单」。

`host/skills/dispatch/review-standard.md` 判绿前必核 9 条：审的就是这份代码 / 文件在树里 / 检查真跑过 / 判别性实验 / 回归基线 / 通过记录同基线 / 新增检查拦截证据 / 机制判定 / 交卷闸。0 条回流。`rg -n "回流" host/skills/dispatch/review-standard.md` exit 1。审官 mirasim 书同样 0 处。

动作层 `host/skills/dispatch/SKILL.md:181` 只在「PR 正文有 `## 回流` 段」时要收口官接单——段不出现就无动作。机械层 `judgeHarvest` 对没写段的 PR「不参与判定」（`tests/harvest.test.js` 写明；设计页也写「宁可漏报，不做没写回流段的自动识别」）。

三层叠加：工人没被要求写、审官没被要求核、闸看见没写就当没有。这不是设计里那条「工人没意识到」的已知漏报，是提示层指错了书，连「意识到了也看不到该写」都没覆盖。

## 建议的最小改造

删掉「回流提示层钉退役士兵书」这一层：把退役书 37-39 行那条「回流自问」搬到 `soldier-book-mirasim.md` 交卷自查（现役第 5 条是机制判定，回流另开一条，不要再叫「第 5 条」），`harvest-check.mjs` 注释和设计页提示层指针改指 mirasim 书。审官必核清单若仍要接「压根没写」那一档，在 `review-standard.md` 加一条可打勾的回流核；不加就把设计页「交给审官必核清单」那句删掉，别让两处规矩互相指。测试打开现役士兵书断言含「回流自问」/ `## 回流`，打开退役书不再当提示层真相源。
