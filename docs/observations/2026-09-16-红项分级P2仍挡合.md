---
status: done
处置: "#1227（分级在执行层未生效，已认账并摆出两条修法）；根因已另出盲设计题重推"
---

# 机制巡检：#1324 红项分级合进 master 六小时了，dao-check 判「已接线」，合并闸和返工闸仍只认 GitHub 红绿

## 结论

#1324（`db5528f1d`，00:13 CST）要把「53 轮 0 绿」的死循环装出口：P1 挡合、P2 落单不挡、P3 不追；到 `review_rounds_max` 只能改判或拆单。合进去的是三份 markdown、一道只扫「书上有字 + 有人读了那个数字」的 dao-check 闸，以及返工任务书里多出来的一句提示。本轮现场：`inspectReviewTiers` 回 `kind: ok`（「红项分级已接线」）。`approvedToLand({ latestRed: true })` 当场 `false`，函数不读 review 正文。指挥官 `decide` 见当前 head 上 `latestRed` 就 `pushRework`，同样不读 `P1`/`P2`。`fuseChoiceRequired` 的唯一调用点在 `reworkBriefText`，到了第 7 轮照样派返工，只是任务书多一行「不许再写请修 P2」。审官书自己把 P2 写在「红 → `--request-changes`」那一节下面，士兵书却说「P2 不用在本 PR 修」。照书做：审官打红（含 P2）→ 合并被挡、返工派出 → 工人不修 P2 → 下一轮还是红。分级制度要停的那条死循环，GitHub 状态机还在转。

## 证据

本轮 2026-09-16 22:24–22:32 UTC（= 06:24–06:32 CST），身份 `uid=999(orca)`，未用 sudo。`HEAD=b97b4e720`（`release: v2.8.0`），与 `origin/master` 相同。本进程：`grok agent --no-leader stdio`。未改 `docs/observations/` 以外的文件。未开单、未评 PR。

### 1. 闸绿的那一层：书上有字 + 有人读了上限

```
$ git log -1 --format='%h %ci %s' db5528f1d
db5528f1d 2026-09-17 00:13:44 +0800 [cc] feat(review): 红项分级 P1挡合/P2落单/P3不追 + 熔断二选一（用户拍板走甲） (#1324)
```

提交说明原文把接线闸写成防 #1051 的那一层：「不接线的那一档正是本单要防的：规矩写了没人核 = 机制装了没生效」。闸实际核的是 `scripts/lib/review-tier-check.mjs:7-10`：

```
本库只做可机械判定的两件事：
  ① 标准页真的有分级节（P1/P2/P3 三档齐全）、审官书/士兵书真的指到它；
  ② release-policy 的 review_rounds_max 真的有生产代码读它
```

`scripts/dao-check.mjs:2190` 每次都跑 `checkReviewTiers()`。本轮直接调：

```
inspectReviewTiers { kind: "ok", line: "红项分级已接线：标准页三档齐全 + 审官书/士兵书都指到 + 熔断上限被生产代码读到" }
loadReviewRoundsMax → { cap: 6 }
```

`tests/review-tier.test.js` 夹具只铺 markdown + `policy.budget.per_issue.review_rounds_max` 属性访问。0 处断言 `approvedToLand` / `pushRework` / `--request-changes` 对 P2 怎么走。

生产消费者是 `scripts/lib/review-cap.mjs`。`fuseChoiceRequired` 本轮全仓调用点：

```
scripts/commander.mjs:57   import
scripts/commander.mjs:1977 const fuse = cap != null ? fuseChoiceRequired(action.redRounds, cap) : null;
```

`:1986` 把它收成返工任务书里的一行：`fuse.required` 为真才追加「**已到上限**：剩下的只能改判或拆单，不许再写一轮「请修 P2」」。`scripts/lib/commander-core.mjs` 0 处 `fuseChoiceRequired`。熔断到了 6 轮，指挥官照样派第 7 个返工工人。

### 2. 跑的那一层：GitHub 红 = 挡合 + 派返工，正文里的 P2 没人读

`host/skills/dispatch/review-standard.md:69-73` 表：

| 级别 | 挡不挡合并 |
|---|---|
| P1 | 必须本 PR 修完 |
| P2 | **不挡**，同轮落单 |
| P3 | 不挡 |

同页 `:75-76`：「变的是『发现问题』与『挡住合并』解绑：发现照记，只有 P1 挡。」

合门函数不读正文。`scripts/lib/land-decision.mjs:119`：

```
if (latestRed === true || redAtHead === true) return false;
```

本轮直接调（不读任何 review body）：

```
approvedToLand({ latestRed: true, redAtHead: true, greenAtHead: false }) → false
approvedToLand({ latestRed: false, redAtHead: false, greenAtHead: true }) → true
```

指挥官合并出口把 `latestRed` 原样喂进去：`scripts/lib/commander-core.mjs:1673-1708`（`latestRed = mergeA.scanned && mergeA.latestRed === true`，再 `approvedToLand({ latestRed, redAtHead: latestRed, ... })`）。返工出口 `:2004-2020`：

```
当前 head 上最后一条判别态是红 → 派一个返工工人
if (!a.latestRed) continue;
pushRework(pr, { brief, ... why: `PR #${pr.number} 审官判红……` });
```

`latestRed` 来自 GitHub review **state**（`CHANGES_REQUESTED`），不是来自正文里的 `P1`/`P2`。本轮 `grep`：

- `scripts/lib/land-decision.mjs` / `approved-merge.mjs` / `commander-core.mjs` / `review-state.mjs`：0 处 `\bP1\b` / `\bP2\b`
- `scripts/*.mjs` `scripts/lib/*.mjs` `scripts/lib/dispatch/*.mjs`：0 处 `另单`
- `scripts/commander.mjs` 除返工任务书那一行提示外，0 处 `P1`/`P2`/`P3`

标准页 `:80-86` 要求 P2 落成真单、判绿前核 `另单：#NNNN` 打得开。生产路径 0 处读这一行。

### 3. 两份任务书互相打架；士兵照做会把死循环续上

`host/skills/dispatch/templates/reviewer-book-mirasim.md:56-65` 把 P1/P2/P3 **全部写在「红 → `--request-changes`」这一节下面**：

```
- **红**（有要返工的项）：走 `--request-changes`，正文把每条……标级别 P1/P2/P3
  - P1 = 挡住合并
  - P2 = 不挡合，但必须当场落单
  - P3 = 不挡不追
```

同页 `:65-66` 才说判绿走 `--approve`，且「判绿前核一遍另单」。P2-only 的票，照「有要返工的项」走红，照「不挡合」该走绿——同一页两套出口。

士兵书 `host/skills/dispatch/templates/soldier-book-mirasim.md:77-83`：

```
P2 = 不挡合：……你不用在本 PR 修它
只改 P1、不动 P2/P3，不算偷工
```

闭环若按字面跑：审官把 P2 写进 `--request-changes` → GitHub state=红 → 合门 `false`、`pushRework` → 工人不修 P2、再 `worker-done` → 审官还看见 P2 → 再红。#1324 提交说明里要停的「每轮修完上一轮又暴露下一层」，机制出口仍是这一圈。

没验证：#1324 合入之后有没有审官真的按「P2 打进 `--request-changes`」落过票。本轮 `gh` 只读到 9 张 OPEN PR 的 `reviewDecision` 全是 `null`（#1284/#885/#1273/#1267/#1322/#1320/#1277/#1287/#1266），没有一份当前 head 上的 CHANGES_REQUESTED 正文可拆。没验证不等于闸会读 P2——函数签名里没有 body。

### 4. 不是已报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-15-同类扫描闸没接线.md` | `gateSameClassScan` 测绿、生产 0 处 import | 本条 `inspectReviewTiers` **有人调**（dao-check 每次跑），绿的是「书上有字」；缺的是合门/返工认档位 |
| `2026-09-07-交卷闸把机械对齐变成审官轮次.md` | handoff-check ① 被拿去判红 | 对象是交卷 ①；本条是 **#1324 新装的分级在合并闸上是空操作** |
| `2026-09-14-播报闸没接线.md` | `decideBroadcast` 测绿、生产 0 import | 对象是播报；本条熔断函数有调用点，但调用点只写任务书，不挡 `pushRework` |
| 多条处置里「判据 review_rounds_max 未接线见 #1227」 | 合入前的缺口 | #1324 **已经合进 origin/master**；接线闸绿了，跑的那一层还是红=挡 |

## 建议的最小改造

删掉「dao-check 看见 P1/P2/P3 四个字 + 有人读了 `review_rounds_max` = 分级已经在挡合」这一层。

合门和返工只认 P1：当前 head 上最后一条若是 `--request-changes`，正文里没有 P1（或未标级别按 P1）才 `latestRed`；纯 P2/P3 不当红、不 `pushRework`、`approvedToLand` 可以绿。审官书「红」那一节只留 P1；P2 走 `--approve` 加 `另单：#N`（落不了单仍按 P1）。熔断接到 `decide`：`redRounds >= cap` 不再派返工，改走改判/拆单那两条出口，不要只写在任务书里。不要再加一条只扫 markdown 的测试把「已接线」锁成绿——那正是本轮闸绿、死循环还在转的那一层。
