---
status: done
---

---
status: new
---

# 机制巡检：#1174 说健康表改探真路、网关探针留作人工诊断；timer 每 30 分钟仍对退役网关打 6 个池，现役 grok/composer 永远 unscanned

## 结论

今天下午合进 master 的 #1174（`b5a41119`）把 newapi 网关从派工选型退役：启用职责树落地改成 `xai-native` / `mirasim-relay` / `cursor-native`，规则写明「`[providers.gw]` 启动模板与网关探针留作人工诊断，任何派工路径都不再读它」。同一次提交给健康表加了第四类 native，声称「改探真路」。

本轮现场：`gw-remote-probe.timer` 仍每 30 分钟自动跑，探测面仍从网关仓 `gateway-policy.json` 的 `pools` + `probe.targets` 派生，6 个 `gw:` 池对 `https://156.224.28.95.sslip.io/v1/chat/completions` 发真流式请求（本轮合计约 24 秒）。派工现役模型经 `probeTargetOf` 映射到的是 `native:xai-native` / `native:cursor-native` / `direct:codex@pqapi/responses`，0 条映射到 `gw:`。native 那两条只验凭据文件在不在，健康表里永远 `unscanned`，消费端把 unscanned 当成「空闲」。dao-check 全文 0 处 `gw`。闸看的那一层是「探针 exit 0 + 表有 updatedAt」，打的那一层已经不是派工在走的路。

## 证据

本轮 2026-09-14 18:24–18:35 CST，身份 `uid=999(orca)`，未用 sudo。`HEAD=b5a41119`，与 `origin/master` 相同。本进程：`grok agent --no-leader stdio`。

### 1. 规则写「人工诊断、派工不读」；启用树里已经没有 provider=gw

`docs/model-routing.json:405` 原文：

> newapi 网关已从派工选型退役：树里不许再出现 provider=gw 的启用条目；[providers.gw] 启动模板与网关探针留作人工诊断，任何派工路径都不再读它。

本轮直接调 `rankOrderFromTree` + `rankListFromTree`（`scripts/lib/model-routing-json.mjs:80-87`，`禁用 !== true` 才进顺位）：

```
写码 ['grok-4.6']
方案 ['gpt-5.6-sol']
审查 ['gpt-5.6-luna', 'gpt-5.6-sol', 'grok-4.6']
判断 ['gpt-5.6-sol', 'composer-2.5']
UI   ['composer-2.5']
ENABLED GW  （0 行）
```

同一批启用条目喂 `probeTargetOf`（`scripts/lib/provider-probe.mjs:96-111`）：

```
grok-4.6      xai-native      → native:xai-native
composer-2.5  cursor-native   → native:cursor-native
gpt-5.6-sol   mirasim-relay   → direct:codex@pqapi/responses
gpt-5.6-luna  mirasim-relay   → direct:codex@pqapi/responses
```

`provider === 'gw'` 才会返回 `gw:` 键。启用树已经不走这条。

### 2. timer 不是人工，探测面仍从网关仓策略派生 6 个池

```
$ systemctl list-timers --no-legend --no-pager | grep gw-remote-probe
Mon 2026-09-14 18:39:00 CST  …  Mon 2026-09-14 18:09:00 CST  gw-remote-probe.timer  gw-remote-probe.service

$ systemctl show gw-remote-probe.service -p Result,ExecMainStatus,ExecMainStartTimestamp,ExecMainExitTimestamp,ExecStart
Result=success
ExecMainStatus=0
ExecMainStartTimestamp=Mon 2026-09-14 18:09:00 CST
ExecMainExitTimestamp=Mon 2026-09-14 18:09:28 CST
ExecStart={ path=/usr/bin/node ; argv[]=/usr/bin/node /srv/projects/windsurf-dao/scripts/gw-remote-probe.mjs … }
```

28 秒跑完、exit 0。仓内 `host/machine/systemd/gw-remote-probe.timer:21`：`OnCalendar=*:09/30`。同文件 13–14 行仍写「它写 `~/.dao/provider-health.json`，派工读它判可用性」。

探测面：`scripts/lib/gateway-policy.mjs:87-110` `probePlan` 从 `policy.pools` + `policy.probe.targets` 生成 `gw:` / `leg:` / `direct:`。`checkProbeTargets`（:73-76）空数组直接抛——池探测面不能拆掉，拆了整轮探针起不来（native / direct / legs 绑在同一条主流程上）。本轮 `loadPolicy()` 命中 `/srv/projects/ai-gateway-stack/docs/gateway-policy.json`（`/opt/gateway-policy.json` 不在；`~/bin/gateway-policy.json` 也在，md5 不同，排在后面没被读）。`probePlan` 实打：

```
nPools 6
gw:gptpool/gpt-5.6
gw:grokpool/grok-4.6
gw:dspool/deepseek-v4-flash
gw:windsurf/kimi-k3-high
gw:cursor/cursor-grok-4.6-high-fast
gw:opencode/glm-5.3-flash
nLegs 8
nDirect 1  direct:codex@pqapi/responses
```

`scripts/gw-remote-probe.mjs:45`：`GATEWAY = https://156.224.28.95.sslip.io`。`:69-76` `probePool` 用 `~/.mirasim/keys/<组>.key` 对 `/v1/chat/completions` 发流式请求。`:241` 无条件 `for (const t of wantPool) results.push(await probePool(t))`。#1174 给这份文件加了 native（:179-209、:238-243），**没删 pool 循环**。

健康表本轮 `updatedAt=2026-09-14T10:09:00.383Z`（= 18:09 CST，就是这一轮），6 个 `gw:` 全 green，耗时 1253–15038ms（cursor 那条 15 秒）。6 条合计约 24 秒。`native:xai-native` / `native:cursor-native` 都是 `unscanned`，原文：`凭据文件在（~/.grok/auth.json），但文件在 ≠ 会话健康——不真跑一条验不出，不报绿`。

`scripts/gw-remote-probe.mjs:297-299`：默认退出码 0 的失败条件仍是「pool 结果全是无 key」。native / direct 探挂不算探针失败。`--failed` 里没有这个单元。

### 3. 派工读健康表，但读的不是那 6 个 gw: 键；native unscanned 当空闲

`scripts/lib/provider-health.mjs:3` 头注仍写「派工只读」。`:111-171` `availabilityFor` 按 `probeTargetOf` 的 key 去表里取。`:169-171`：target 不在表里或 `unscanned` → `availability = 空闲`（不拦）。

于是：

| 现役模型 | 探针 key | 本轮健康表 | 派工看见 |
|---|---|---|---|
| grok-4.6（写码主路） | `native:xai-native` | unscanned | 空闲 |
| composer-2.5（UI 主路） | `native:cursor-native` | unscanned | 空闲 |
| gpt-5.6-sol / luna（方案 / 审官） | `direct:codex@pqapi/responses` | green | 空闲 |
| （无） | `gw:grokpool/grok-4.6` 等 6 条 | green | 没有模型映射到这些键 |

`scripts/dao-check.mjs` 本轮 `grep -c -E 'gw:|provider=.gw|newapi'` → `0`。2963 行里不问「启用树还有没有 provider=gw」「probePlan 的 pools 还对不对得上派工落地」「native 是否永远 unscanned」。#1174 自己报「dao-check 250 项全绿」。

`leg:pqapi-sol` / `leg:pqapi-free` / `leg:mirasim-bridge` 本轮 red，熔断表对应 key 到明天还 open。`probeTargetOf` 不把 `mirasim-relay` 映射成 `leg:`（只映射 `direct:codex@pqapi/responses`，本轮 green、熔断 closed）。这三条腿红不进模型准入。不另报——对象是渠道熔断，不是本条的探测面。

### 4. 不是已报过的那几条

| 已有观察 | 管的是 | 本条不是 |
|---|---|---|
| `2026-09-08-gw-remote-probe仍跑仓外旧脚本.md`（处置：#1164） | 当时 ExecStart 还指 `~/bin`，⑳ 看不见 drop-in | 活 ExecStart 已经是仓内脚本；本条是 **#1174 退役之后，仓内脚本仍自动打退役池** |
| `2026-09-05-gw-remote-probe单调时钟.md` | timer 只有单调时钟，停了不再响 | 本轮 NEXT 是 18:39，墙钟在，它一直在响 |
| `2026-09-14-grok关掉了claude钩子.md` | 现役 grok 不跑 Claude PreToolUse | 对象是钩子挂载面；本条是 **健康表探测面还钉网关仓策略**。钩子关了和 native 永远 unscanned 叠在同一条 grok 主路上，但根因不是同一层 |
| `2026-09-14-播报闸没接线.md` | #891 纯函数 0 个生产调用 | 对象是播报；本条是探针 |

## 建议的最小改造

删掉「`probe.targets` 非空 + timer exit 0 = 健康表还在为派工探真路」这一层。

探测面跟派工落地用同一份 `probeTargetOf`：启用树映射不到的 `gw:` 池，timer 里不要再打。真路是 native 的，要么接受「不探会话健康」（那就把 unscanned 从「空闲」里拆出来，选型输出必须写「没探成」），要么停掉这条自动钟，把网关池探针真的留给人偶尔跑——规则自己写了「人工诊断」。

不要在 `gateway-policy.json` 里加一份「允许继续探的退役池」名单——#1174 刚把派工从这些键上撤走，再给探针留名单是第二层，第一层还在每 30 分钟对退役网关发真请求。

处置：#1051（同一形状「闸看的那一层 ≠ 跑的那一层」，归类单；2026-09-16 帅位盘点，判据 review_rounds_max 未接线见 #1227）
