{
  "updated": "2026-09-03",
  "说明": "派工运行期策略。派前探一针（#842）：起 agent 前用同路径流式探一次，红了换下一位。编排层熔断（#843）：窗口内连撞 N 次自动 cooldown，到点半开一针。指挥官（#849）：单轮派单上限 + 派前校验模型在当前选型且健康表非红。真相源，改动后 dao-check 校验取值范围。后台改的是这份文件 → PR。",
  "preflight": {
    "enabled": true,
    "timeoutMs": 5000,
    "maxCandidates": 4,
    "useHealthTable": true
  },
  "breaker": {
    "windowHours": 24,
    "failuresToTrip": 3,
    "cooldownHours": 24,
    "halfOpenProbes": 1,
    "overrides": {}
  },
  "commander": {
    "maxDispatchPerRound": 2,
    "requireModelInRouting": true
  }
}
