# v2.11 期的优先级（一眼看懂）

> 机器可读的范围与出口判据在 `docs/stages/v2.11.json`；**本页是它的人读镜像**（同一份事实，两种形态）。
> 容器是 GitHub 里程碑 **#3「v2.11 期：指挥官+Fusion 落地」**。规矩：一版同时只有一个 OPEN 版本。

## 一、现在做（不做完不往下走）

| #       | 事项                                                           | 为什么是「现在」                                                                             | 依赖         |
| ------- | -------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | ------------ |
| **T40** | 修抖动测试 `mirasim-ws-probe.test.js`                          | **已三次**在 CI 红、本地全过。本仓规矩：抖动闸最后一定被关掉——它现在是机械面最该治的一个     | —            |
| **T32** | P2/P3 → 债册子（+ `type`/`effort` 字段 + SLA 计算 + 类单逻辑） | **现在 P2/P3 是静默丢失的**（`advisory` 算完没有任何消费方）——这是真洞，不是优化             | —            |
| **T7**  | 持久上下文（返工**续跑同一会话**）                             | 质量项 + 成本项：Devin「REJECT 当场修」便宜就便宜在上下文还热；也是 T33 与 T39 阶梯 ③ 的前提 | #1466 已合 ✓ |

## 二、紧接着（现在做完就上）

| #       | 事项                                                                                         | 为什么在这个位置                                                   | 依赖      |
| ------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ | --------- |
| **T33** | lead 自审（两级审查）+ 返工只带「急与便宜的」                                                | 让异厂审查看到**更干净的产物** → findings 下降 → 债自然变少        | **T7**    |
| **T34** | 风险分层（T0 文档 / T1 孤立代码 / T2 机制协议安全）+ 机械项进闸                              | 严格程度**正比于爆炸半径**，解「每个改动都付整条闭环」的困局       | **T35** ✓ |
| **T41** | 零依赖语法闸（`node --check` 改动文件）                                                      | 本轮被咬：`cli.mjs` 语法坏而 **fleet 测试 67/67 全绿**（不加载它） | T35 ✓     |
| **T37** | 目录改造（约定脊柱 `host/conventions/` + 回流收件箱 `host/reflow/` + 符合性检查 + 建仓模板） | 让「机制自上而下/自下而上传播」成为机制而不是自觉                  | —         |

## 三、靠后（本版内，但不阻塞出口）

| #       | 事项                                         | 说明                                          |
| ------- | -------------------------------------------- | --------------------------------------------- |
| T31     | 伞单索引闸（索引里出现 `#issue` 即红）       | 防伞单膨胀；与 T30 同批                       |
| T36     | 跨仓符合性检查 + 三颗牙                      | 依赖 T37 的约定脊柱先落地                     |
| T26     | 看板**数据面**（三墙的机器可读投影）         | UI/动作面留下一版（#818）                     |
| T29     | 会话路径体检的 **VPS 侧**对比 + 两件便宜大件 | **等链路恢复**（当时连续 `Connection reset`） |
| T39     | 自愈阶梯 ③ **换腿** + 指挥官主动巡检         | 换腿依赖 T7；巡检依赖 T26 的投影              |
| T24/T25 | 腿的生命周期探针 + 到期处置                  | **挂 #1395**（不是本版范围，已有单在管）      |

## 四、已完成（本版内已合入 master）

T1 首个全闭环 ✓ · T4 工作区复位 ✓ · T19/T20 续跑优先 ✓ · T21 容量长退避 ✓ · T22 卫生闸 ✓ · T27 并发显式化 ✓（压测见「靠后」）· T28 机械可读原则 ✓ · T30 网关里程碑动作 ✓ · T35 机械项工具（dao-fix + prettier）✓ · T38 E2E 规矩 ✓ · T42 改代码方式 ✓（已按此执行）

## 五、出口（四条，**代码判**，不靠人喊）

1. 范围内条目**全部关闭**，或在 `deferred`/`moved` 里**显式记账（带理由）**；
2. 闸全绿：PR 积压=0、仓库卫生=绿、机器清单=绿；
3. **冒烟任务**走完 fleet 全闭环（issue → PR → 合并 → 关单 → `completed`）；
4. 触发发布列车（tag + 版本号）。

## 六、边界怎么定（T43）

**内容为主 + 列车班次兜底**：出口四条机械满足 → 可关；**发布列车班次到点也关**（未完成顺延记账）。
**指挥官是执行者不是裁判**：它发现「出口满足」或「班次到点」就执行关版/开新版，不许凭感觉提前或延后。
