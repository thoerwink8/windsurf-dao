# 2026-09-03 全员切 pi gw（GPT 走 Codex）

拍板原文在 issue #822。本页不复制会过期的 cli_model。

## 谁提的，什么场景

用户 2026-09-03 12:20：除了 gpt 必须走 Codex，其他都切 pi，pi 遇到问题全力优化。12:55 再明确：Codex 只给 GPT，Grok Build / cursor-agent / devin / claude CLI 不留顺位 2，从选型移除；降级交给网关池。

## 删哪一层 / 今天还会造吗

派工层不再做模型级 CLI 备胎（Grok Build 顺位 2、cursor 支路）。只留一种工人启动形态：pi + 网关组前缀。GPT 审官主路仍 Codex——那是用户例外，不是第二套工人栈。

## 拍板

- 非 GPT：启用中的厂商必须是 `gw`，cli_model 带组前缀（`gw/grok-4.6`、`gw-dspool/…`、`gw-windsurf/…`、`gw-sub/…`）。
- GPT：厂商必须是 `gpt`（Codex）。方案/查证/判断里的 GPT 条目同样，不只审官。
- 审官顺位 1 仍 `gpt-5.6-sol` / Codex。顺位 2 luna 仍 `gw-windsurf/gpt-5.6-luna`。
- devin、ox-alpha-free 从选型禁用（无网关渠道 / 服务器 og 403）。登记留在 JSON/yml 防战绩断号。
- CLI 卸载（Grok Build、cursor-agent、devin、claude CLI）等本单验收（真实小单全程 pi 跑通）之后，验收前不动 NEW-MACHINE / server-check。

## 前置实测

前置 1（档位）帅已绿，见 #822 18:05 评论 / ai-gateway-stack DECISIONS §60。

前置 2（2026-09-03 本机，独立核对产物，不信模型自报）：

| 渠道 | 命令 | 用时 | write | `node --test` | `gh issue view 822` |
|---|---|---|---|---|---|
| grok | `pi --provider gw --model grok-4.6` | 10s | ok | ok | number=822 |
| deepseek | `pi --provider gw-dspool --model deepseek-v4-flash` | 21s | ok | ok | number=822 |
| glm | `pi --provider gw-windsurf --model glm-5-2` | 23s | ok | ok | number=822 |

三绿才改 routing。原始 jsonl 在本机 `/tmp/pi-gw-samples/`（不进 git）。

## 已知后果

工人切 `gw` 之后，luna / kimi / glm / grok 审官也是 `gw`，#679 同厂闸会拒。跨厂审官只剩 gpt/codex 主路。pqapi 挂时 luna 降级救不了 gw 工人——要修另开单，不在本单改闸。

## 怎么验

- `node --test tests/model-routing.test.js`（扫完 0 条违规 vs 没扫到；红/绿/空夹具有判别力）
- `node --test tests/dianjiangtai.test.js tests/dao.test.js tests/reviewer-vendor-gate.test.js`
- `node scripts/dao-check.mjs`
- 合入后再派一张真实小单全程 pi（建卡→写码→PR→审官）——本决策页不假装已经跑过
