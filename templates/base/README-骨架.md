# {{项目名}}

## 一句话说明

{{这个项目是做什么的。用大白话写，别堆术语：一个没参与过的人读完这句话，应该知道"哦，这原来是个做 XX 的东西"}}

## 怎么跑

{{从零到跑起来的完整步骤。假设读者手里只有这份代码，照着敲就能跑}}

```sh
# 例：装依赖 → 起服务
npm install
npm run dev
```

{{有外部依赖（数据库、API 密钥等）就在这里写清楚怎么准备}}

## 怎么测

{{自动化测试命令 + 手动验收要点。验收标准写成能执行验证的动作，别写"应该正常"}}

```sh
# 例：
npm run check    # 类型 + 测试 + lint 一把过
```

手动验收：{{打开哪个页面、点哪里、预期看到什么}}
