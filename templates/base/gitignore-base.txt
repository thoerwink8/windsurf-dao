# ============ 通用段（所有项目都适用，拷进新项目后改名 .gitignore） ============

# 依赖
node_modules/
.pnp
.pnp.js

# 构建产物
dist/
build/
out/
target/
*.tsbuildinfo

# 环境变量（含密钥，绝不进 git）
.env
.env.*
!.env.example

# 本地临时文件
_tmp/
*.tmp
*.temp
~$*
*.log

# 测试产物
coverage/
test-results/
playwright-report/

# 本地数据库
*.sqlite
*.sqlite-journal
*.db

# 操作系统/编辑器
.DS_Store
Thumbs.db
.idea/
# .vscode/ 默认不忽略：团队共享的调试配置可以进 git，看情况取舍

# 密钥与证书（红线：任何签名/凭据类文件一律不进 git）
*.pem
*.key
*.p12
*.jks
*.p8
*.mobileprovision
