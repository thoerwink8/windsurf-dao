# ngrok 配置模板。复制为本机 %LOCALAPPDATA%\ngrok\ngrok.yml 后，把 <YOUR_NGROK_AUTHTOKEN> 换成你的真实 token。
# 真实 authtoken 不要提交到 git。
version: "3"
agent:
    authtoken: <YOUR_NGROK_AUTHTOKEN>
endpoints:
  - name: daohub
    url: https://encircle-wasting-paging.ngrok-free.dev
    upstream:
      url: 9910
