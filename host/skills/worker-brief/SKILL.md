---
name: worker-brief
description: 被派工人的开工便签：作为工人收到派工任务书、开工干活前先读。八条纪律——回答对象、异步终态、收账方式、接手判据、commit 前缀、PR 正文、完工信号、通知等待。
---

# 工人便签（八条）

1. 回答对象是派你的人——不要调 AskUserQuestion（会挂死）。
2. 异步/后台任务必须亲手读到终态才停手；等待超 15 分钟不就绪就换手段，把欠账写进交付。
3. 收账一律 `git add <具体路径>`，禁 `-A` 和 `.`（共享工作树会把别人半成品收走）。
4. 接手不是重来：前任最后一句是意图宣告不是进度报告，两个方向都只认盘上。
5. commit 前缀带宿主标识（`[cc]` / `[pi]` / `[codex]`）。
6. PR 正文三段式：目标 / 验收标准 / 进展。
7. 完工发 worker_done 后终端就不再收信（续活走新建任务注入）。
8. 起了后台/异步验证别停手等通知：收不到通知就自己读输出文件，或前台重跑拿真退出码；禁止把「等不到通知」静默当「已通过」。
