_tmp/
# 根目录一次性临时文件（commit 消息草稿、benchmark 脚本等，用完即删；崩溃残留不进版本库）
/_tmp-*
_scratch/
# 流转器游标缓存（GitHub 才是真相源，本地只记已处理信号与终端句柄，可丢可重算）
_flow/
# PR 正文草稿（进 PR body 的文书，不进 git）
.flow-pr-body.md
# 完工报告草稿：工人写它、`dao.mjs worker-done --body` 读它，读完使命就结束，从来不该被提交。
# 不 ignore 的后果实测过（2026-09-06）：它以「未跟踪文件」的身份留在工人树里，
# 而 git worktree remove 见到未跟踪文件就拒删 → board-gc 每一张干完的卡都回收不掉 →
# 僵尸卡越积越多 → agent-stall-watch 再把它们当「静默会话」报警。
# 一个完工标记，把清理路整条堵死。修法不是给删树加 --force（那条禁令有它的道理），是让草稿别装成待提交。
worker-done-*.md
# 测试夹具与复现沙箱（跑完即删；崩溃残留也不该进版本库）
# 刻意不放 _tmp/：被测 hook dao-rule-echo.js 的 EXCLUDE 把 _tmp/_scratch 判为非规则面
tests/.fixtures-scope*/
tests/.repro-sandboxes/
_dao_backup/
# playwright MCP 的 output dir：页面跳转就会在**仓根**长出它（页面快照 yml + 控制台日志），
# 不必等到截图。清理钩子 dao-playwright-cleanup.js 只管 %LOCALAPPDATA% 那份，管不到这里。
# 用户 2026-08-10 拍板（issue #269 ㈢）：这类自动产物永久不进版本库。
# 会红的闸：tests/hard-gates.tests.js 的 G4 节断言 `git check-ignore` 对它 exit 0。
.playwright-mcp/
.dao-targets
# 主仓的 manual 锁绝不能进仓：settings.local.json 优先级高于随仓 .claude/settings.json，
# 一旦入库所有 checkout（含工兵树）都被锁回 manual，随仓 auto 权限面整体失效。
.claude/settings.local.json
.DS_Store
Thumbs.db
image/
__pycache__/
*.pyc
.codegraph/

# 飞书适配器（#801）live 模式的 SDK 依赖（服务器 npm install 产物，测试/CI 不装）
host/machine/feishu-triage/node_modules/

# 审官草稿（#601）：判定正文进 GitHub review，树里这份只会挡住 orca worktree rm
# 收窄到根目录；不许靠「删树时按白名单删文件」
/review-*.txt
/.tmp-review-*/

# 密钥不进 git（真实值，换机手动复制）
config-sync/**
*.pem
*.token

# devin 交互形态任务书（dao.mjs 写进工人树，--prompt-file 备用通道；
# 不进 git——脏树会挡 orca worktree rm，内容是内部指令不该进 PR）
_prompt.txt
